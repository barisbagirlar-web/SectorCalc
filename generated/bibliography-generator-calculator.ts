// Auto-generated from bibliography-generator-calculator-schema.json
import * as z from 'zod';

export interface Bibliography_generator_calculatorInput {
  pageCount: number;
  referencesPerPage: number;
  citationDensity: number;
  formattingTimePerRef: number;
  styleComplexity: number;
  overheadTime: number;
  referencesFromExternal: number;
  autoGeneratedPerRef: number;
  dataConfidence?: number;
}

export const Bibliography_generator_calculatorInputSchema = z.object({
  pageCount: z.number().default(10),
  referencesPerPage: z.number().default(2),
  citationDensity: z.number().default(75),
  formattingTimePerRef: z.number().default(5),
  styleComplexity: z.number().default(1.2),
  overheadTime: z.number().default(15),
  referencesFromExternal: z.number().default(5),
  autoGeneratedPerRef: z.number().default(1),
});

function toNumericFormulaValue(value: number): number {
  return Number.isFinite(value) ? value : Number.NaN;
}

function evaluateAllFormulas(input: Bibliography_generator_calculatorInput): Record<string, number> {
  const results: Record<string, number> = {};
  try { const v = (input.pageCount * input.referencesPerPage * input.citationDensity / 100) + input.referencesFromExternal; results["totalReferences"] = typeof v === "number" && Number.isFinite(v) ? v : Number.NaN; } catch { results["totalReferences"] = Number.NaN; }
  try { const v = (toNumericFormulaValue(results["totalReferences"])) * input.formattingTimePerRef * input.styleComplexity; results["totalManualTime"] = typeof v === "number" && Number.isFinite(v) ? v : Number.NaN; } catch { results["totalManualTime"] = Number.NaN; }
  try { const v = (toNumericFormulaValue(results["totalReferences"])) * input.autoGeneratedPerRef; results["totalAutoTime"] = typeof v === "number" && Number.isFinite(v) ? v : Number.NaN; } catch { results["totalAutoTime"] = Number.NaN; }
  try { const v = (toNumericFormulaValue(results["totalManualTime"])) + (toNumericFormulaValue(results["totalAutoTime"])) + input.overheadTime; results["totalTime"] = typeof v === "number" && Number.isFinite(v) ? v : Number.NaN; } catch { results["totalTime"] = Number.NaN; }
  try { const v = (toNumericFormulaValue(results["totalReferences"])) / ((toNumericFormulaValue(results["totalTime"])) / 60); results["referencesPerHour"] = typeof v === "number" && Number.isFinite(v) ? v : Number.NaN; } catch { results["referencesPerHour"] = Number.NaN; }
  return results;
}


export function calculateBibliography_generator_calculator(input: Bibliography_generator_calculatorInput): Bibliography_generator_calculatorOutput {
  const values = evaluateAllFormulas(input);
  const totalWasteCost = toNumericFormulaValue(values["totalReferences"]);
  const breakdown = {
    
  };
  const hiddenLossDrivers: string[] = [];
  const suggestedActions: string[] = ["Review inputs and verify results against site standards."];
  const dataConfidenceAdjusted =
    typeof input.dataConfidence === "number"
      ? totalWasteCost * (input.dataConfidence / 100)
      : totalWasteCost;
  return {
    totalWasteCost,
    breakdown,
    hiddenLossDrivers,
    suggestedActions,
    dataConfidenceAdjusted,
    premiumRequired: false,
    premiumFeatures: [],
  };
}


export interface Bibliography_generator_calculatorOutput {
  totalWasteCost: number;
  breakdown: {  };
  hiddenLossDrivers: string[];
  suggestedActions: string[];
  dataConfidenceAdjusted: number;
  premiumRequired: boolean;
  premiumFeatures: string[];
}
