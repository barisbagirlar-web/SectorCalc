// Auto-generated from bibliography-generator-calculator-schema.json
import * as z from 'zod';

export interface Bibliography_generator_calculatorInput {
  pageCount: number;
  referencesPerPage: number;
  citationDensity: number;
  formattingTimePerRef: number;
  styleComplexity: number;
  overheadTime: number;
  referencesFromExternal: number;
  autoGeneratedPerRef: number;
}

export const Bibliography_generator_calculatorInputSchema = z.object({
  pageCount: z.number().default(10),
  referencesPerPage: z.number().default(2),
  citationDensity: z.number().default(75),
  formattingTimePerRef: z.number().default(5),
  styleComplexity: z.number().default(1.2),
  overheadTime: z.number().default(15),
  referencesFromExternal: z.number().default(5),
  autoGeneratedPerRef: z.number().default(1),
});

function evaluateAllFormulas(input: Bibliography_generator_calculatorInput): Record<string, number> {
  const results: Record<string, number> = {};
  try { const v = (input.pageCount * input.referencesPerPage * input.citationDensity / 100) + input.referencesFromExternal; results["totalReferences"] = Number.isFinite(v) ? v : 0; } catch { results["totalReferences"] = 0; }
  try { const v = (results["totalReferences"] ?? 0) * input.formattingTimePerRef * input.styleComplexity; results["totalManualTime"] = Number.isFinite(v) ? v : 0; } catch { results["totalManualTime"] = 0; }
  try { const v = (results["totalReferences"] ?? 0) * input.autoGeneratedPerRef; results["totalAutoTime"] = Number.isFinite(v) ? v : 0; } catch { results["totalAutoTime"] = 0; }
  try { const v = (results["totalManualTime"] ?? 0) + (results["totalAutoTime"] ?? 0) + input.overheadTime; results["totalTime"] = Number.isFinite(v) ? v : 0; } catch { results["totalTime"] = 0; }
  try { const v = (results["totalReferences"] ?? 0) / ((results["totalTime"] ?? 0) / 60); results["referencesPerHour"] = Number.isFinite(v) ? v : 0; } catch { results["referencesPerHour"] = 0; }
  try { const v = $(results["totalTime"] ?? 0); results["__totalTime_"] = Number.isFinite(v) ? v : 0; } catch { results["__totalTime_"] = 0; }
  try { const v = $(results["referencesPerHour"] ?? 0); results["__referencesPerHour_"] = Number.isFinite(v) ? v : 0; } catch { results["__referencesPerHour_"] = 0; }
  return results;
}


export function calculateBibliography_generator_calculator(input: Bibliography_generator_calculatorInput): Bibliography_generator_calculatorOutput {
  const values = evaluateAllFormulas(input);
  const totalWasteCost = values["totalReferences"] ?? 0;
  const breakdown = {
    
  };
  const hiddenLossDrivers: string[] = [];
  const suggestedActions: string[] = [];
  const dataConfidenceAdjusted =
    typeof (input as Record<string, unknown>).dataConfidence === "number"
      ? totalWasteCost * (((input as Record<string, unknown>).dataConfidence as number) / 100)
      : totalWasteCost;
  return {
    totalWasteCost,
    breakdown,
    hiddenLossDrivers,
    suggestedActions,
    dataConfidenceAdjusted,
    premiumRequired: false,
    premiumFeatures: [],
  };
}


export interface Bibliography_generator_calculatorOutput {
  totalWasteCost: number;
  breakdown: {  };
  hiddenLossDrivers: string[];
  suggestedActions: string[];
  dataConfidenceAdjusted: number;
  premiumRequired: boolean;
  premiumFeatures: string[];
}
