{
  "toolName": "binomial-probability-calculator",
  "inputs": [
    {
      "id": "n",
      "label": "Deneme Sayısı (n)",
      "type": "number",
      "unit": "",
      "default": 10,
      "businessContext": "Toplam bağımsız deneme sayısı. Pozitif tam sayı olmalıdır."
    },
    {
      "id": "k",
      "label": "Başarı Sayısı (k)",
      "type": "number",
      "unit": "",
      "default": 5,
      "businessContext": "Elde edilmesi istenen başarı sayısı. 0 ile n arasında tam sayı olmalıdır."
    },
    {
      "id": "p",
      "label": "Başarı Olasılığı (p)",
      "type": "number",
      "unit": "",
      "default": 0.5,
      "businessContext": "Tek bir denemede başarı olasılığı. 0 ile 1 arasında olmalıdır."
    },
    {
      "id": "precision",
      "label": "Ondalık Basamak",
      "type": "number",
      "unit": "",
      "default": 4,
      "businessContext": "Sonuçta gösterilecek ondalık basamak sayısı. Varsayılan 4."
    }
  ],
  "formulas": {
    "logCombination": "n * Math.log(n) - n + 0.5 * Math.log(2 * Math.PI * n) - (k * Math.log(k) - k + 0.5 * Math.log(2 * Math.PI * k)) - ((n - k) * Math.log(n - k) - (n - k) + 0.5 * Math.log(2 * Math.PI * (n - k)))",
    "exactProbability": "Math.exp(logCombination + k * Math.log(p) + (n - k) * Math.log(1 - p))",
    "probability": "Number((Math.exp(logCombination + k * Math.log(p) + (n - k) * Math.log(1 - p))).toFixed(precision))"
  },
  "outputs": {
    "primary": "probability",
    "breakdown": [
      "logCombination",
      "exactProbability"
    ]
  },
  "category": "Teknik & Mühendislik",
  "sector": "Teknik & Mühendislik",
  "premiumRequired": false,
  "catalogCategory": "math-statistics",
  "sectorSlug": "ecommerce",
  "categorySlug": "technology-ai-cloud-cyber"
}
