{
  "toolName": "baby-growth-percentile-calculator",
  "inputs": [
    {
      "id": "measurement",
      "label": "Measurement Value",
      "type": "number",
      "unit": "",
      "default": 5,
      "businessContext": "The child's measurement (e.g., weight, length, or head circumference)."
    },
    {
      "id": "L",
      "label": "L (Lambda)",
      "type": "number",
      "unit": "",
      "default": 1,
      "businessContext": "L parameter from the LMS growth reference (e.g., WHO/CDC) for the specific age, gender, and measurement type."
    },
    {
      "id": "M",
      "label": "M (Median)",
      "type": "number",
      "unit": "",
      "default": 7,
      "businessContext": "M parameter (median) from the LMS growth reference, in the same unit as the measurement."
    },
    {
      "id": "S",
      "label": "S (Coefficient of Variation)",
      "type": "number",
      "unit": "",
      "default": 0.1,
      "businessContext": "S parameter (generalized coefficient of variation) from the LMS growth reference."
    },
    {
      "id": "measurementType",
      "label": "Measurement Type",
      "type": "number",
      "unit": "",
      "default": 1,
      "businessContext": "Indicator: 1 = Weight, 2 = Length, 3 = Head circumference. Used only for contextual clarity."
    }
  ],
  "formulas": {
    "zscore": "measurement !== 0 ? (Math.abs(L) < 1e-10 ? Math.log(measurement / M) / S : (Math.pow(measurement / M, L) - 1) / (L * S)) : null",
    "percentile": "zscore !== null ? (() => { const a1=0.254829592; const a2=-0.284496736; const a3=1.421413741; const a4=-1.453152027; const a5=1.061405429; const p=0.3275911; const sign = zscore < 0 ? -1 : 1; const x = Math.abs(zscore)/Math.sqrt(2); const t = 1.0/(1.0 + p*x); const y = 1.0 - (((((a5*t + a4)*t) + a3)*t + a2)*t + a1)*t*Math.exp(-x*x); return 0.5 * (1.0 + sign * y) * 100; })() : null"
  },
  "outputs": {
    "primary": "percentile",
    "breakdown": [
      "zscore"
    ]
  },
  "category": "Ölçüm & Dönüşüm",
  "sector": "Teknik & Mühendislik",
  "premiumRequired": false,
  "catalogCategory": "math-statistics",
  "sectorSlug": "ecommerce",
  "categorySlug": "technology-ai-cloud-cyber"
}
