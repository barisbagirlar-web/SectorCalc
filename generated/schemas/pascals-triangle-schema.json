{
  "toolName": "pascals-triangle",
  "inputs": [
    {
      "id": "rowIndex",
      "label": "Row Index (0-based)",
      "type": "number",
      "unit": "",
      "default": 5,
      "businessContext": "The row number in Pascal's triangle, starting from 0."
    },
    {
      "id": "colIndex",
      "label": "Column Index (0-based)",
      "type": "number",
      "unit": "",
      "default": 2,
      "businessContext": "The column position within the specified row, starting from 0."
    },
    {
      "id": "numRows",
      "label": "Number of Rows to Generate",
      "type": "number",
      "unit": "",
      "default": 5,
      "businessContext": "Total number of rows to generate for the triangle."
    },
    {
      "id": "maxValue",
      "label": "Maximum Value to Display",
      "type": "number",
      "unit": "",
      "default": 100,
      "businessContext": "Only display entries with value less than or equal to this threshold."
    }
  ],
  "formulas": {
    "binomialCoefficient": "function binomial(n, k) { if (k < 0 || k > n) return 0; if (k === 0 || k === n) return 1; k = Math.min(k, n - k); let c = 1; for (let i = 0; i < k; i++) { c = c * (n - i) / (i + 1); } return c; }",
    "generateTriangle": "function generateTriangle(rows) { let triangle = []; for (let n = 0; n < rows; n++) { let row = []; for (let k = 0; k <= n; k++) { row.push(binomial(n, k)); } triangle.push(row); } return triangle; }",
    "filterByMaxValue": "function filterByMaxValue(triangle, maxVal) { return triangle.map(row => row.filter(val => val <= maxVal)); }"
  },
  "outputs": {
    "primary": "binomialCoefficient(rowIndex, colIndex)",
    "breakdown": [
      "generateTriangle(numRows)",
      "filterByMaxValue(generateTriangle(numRows), maxValue)"
    ]
  },
  "category": "Teknik & Mühendislik",
  "sector": "Teknik & Mühendislik",
  "premiumRequired": false,
  "catalogCategory": "everyday-life",
  "sectorSlug": "daily-renovation",
  "categorySlug": "packaging-local-business"
}
