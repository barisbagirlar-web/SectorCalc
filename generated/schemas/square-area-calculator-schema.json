{
  "toolName": "square-area-calculator",
  "inputs": [
    {
      "id": "sideLength",
      "label": "Side Length",
      "type": "number",
      "unit": "m",
      "default": 1,
      "businessContext": "Nominal side length of the square in base units (typically meters)."
    },
    {
      "id": "tolerancePlus",
      "label": "Tolerance (+)",
      "type": "number",
      "unit": "m",
      "default": 0,
      "businessContext": "Positive tolerance on side length (added to nominal)."
    },
    {
      "id": "toleranceMinus",
      "label": "Tolerance (-)",
      "type": "number",
      "unit": "m",
      "default": 0,
      "businessContext": "Negative tolerance on side length (subtracted from nominal, entered as positive number)."
    },
    {
      "id": "unitMultiplier",
      "label": "Output Unit Multiplier",
      "type": "number",
      "unit": "",
      "default": 1,
      "businessContext": "Multiplier to convert square base units to desired output unit (e.g., 1 for m², 10000 for cm², 10.7639 for ft²)."
    },
    {
      "id": "decimals",
      "label": "Decimal Places",
      "type": "number",
      "unit": "",
      "default": 2,
      "businessContext": "Number of decimal places for rounding results."
    }
  ],
  "formulas": {
    "nominalArea": "Math.round((sideLength ** 2) * unitMultiplier * Math.pow(10, decimals)) / Math.pow(10, decimals)",
    "minArea": "Math.round(((sideLength - toleranceMinus) ** 2) * unitMultiplier * Math.pow(10, decimals)) / Math.pow(10, decimals)",
    "maxArea": "Math.round(((sideLength + tolerancePlus) ** 2) * unitMultiplier * Math.pow(10, decimals)) / Math.pow(10, decimals)"
  },
  "outputs": {
    "primary": "nominalArea",
    "breakdown": [
      "minArea",
      "maxArea"
    ]
  },
  "category": "Ölçüm & Dönüşüm",
  "sector": "Teknik & Mühendislik",
  "premiumRequired": false,
  "catalogCategory": "math-statistics",
  "sectorSlug": "ecommerce",
  "categorySlug": "technology-ai-cloud-cyber"
}
