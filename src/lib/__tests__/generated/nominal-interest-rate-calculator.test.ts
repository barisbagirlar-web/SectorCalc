// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNominal_interest_rate_calculator,
  type Nominal_interest_rate_calculatorInput,
} from "@generated/nominal-interest-rate-calculator";

describe("nominal-interest-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "realRate": 2,
    "inflationRate": 3,
    "taxRate": 25,
    "feePercentage": 0.5
  } as Nominal_interest_rate_calculatorInput;
    const result = calculateNominal_interest_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
