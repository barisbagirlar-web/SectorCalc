// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateUs_dress_size_to_eu_calculator,
  type Us_dress_size_to_eu_calculatorInput,
} from "@generated/us-dress-size-to-eu-calculator";

describe("us-dress-size-to-eu-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "usDressSize": 4,
    "ageGroup": 0,
    "brandAdjustment": 0,
    "precision": 0
  } as unknown as Us_dress_size_to_eu_calculatorInput;
    const result = calculateUs_dress_size_to_eu_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
