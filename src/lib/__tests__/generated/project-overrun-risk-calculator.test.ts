// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProject_overrun_risk_calculator,
  type Project_overrun_risk_calculatorInput,
} from "@generated/project-overrun-risk-calculator";

describe("project-overrun-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "plannedDuration": 12,
    "actualDuration": 6,
    "plannedCost": 1000000,
    "actualCost": 600000,
    "percentComplete": 50,
    "scopeChangeFrequency": "moderate",
    "teamExperience": "intermediate",
    "qualityDefectRate": 0.05
  } as unknown as Project_overrun_risk_calculatorInput;
    const result = calculateProject_overrun_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
