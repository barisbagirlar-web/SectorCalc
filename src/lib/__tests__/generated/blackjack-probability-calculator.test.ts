// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBlackjack_probability_calculator,
  type Blackjack_probability_calculatorInput,
} from "@generated/blackjack-probability-calculator";

describe("blackjack-probability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numberOfDecks": 6,
    "acesRemaining": 24,
    "tensRemaining": 96,
    "otherCardsRemaining": 192
  } as unknown as Blackjack_probability_calculatorInput;
    const result = calculateBlackjack_probability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
