// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWedding_alcohol_calculator,
  type Wedding_alcohol_calculatorInput,
} from "@generated/wedding-alcohol-calculator";

describe("wedding-alcohol-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "guestCount": 100,
    "alcoholPercentage": 80,
    "durationHours": 5,
    "drinksPerHour": 1.5,
    "beerRatio": 0.4,
    "wineRatio": 0.4,
    "spiritsRatio": 0.2
  } as unknown as Wedding_alcohol_calculatorInput;
    const result = calculateWedding_alcohol_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
