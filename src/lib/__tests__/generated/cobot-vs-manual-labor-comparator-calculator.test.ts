// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCobot_vs_manual_labor_comparator_calculator,
  type Cobot_vs_manual_labor_comparator_calculatorInput,
} from "@generated/cobot-vs-manual-labor-comparator-calculator";

describe("cobot-vs-manual-labor-comparator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_manual_labor_cost": 45000,
    "number_of_workers": 2,
    "cobot_purchase_price": 35000,
    "cobot_annual_maintenance": 3000,
    "cobot_lifespan_years": 8,
    "discount_rate": 8,
    "labor_productivity_factor": 0.85,
    "cobot_uptime_percent": 95
  } as unknown as Cobot_vs_manual_labor_comparator_calculatorInput;
    const result = calculateCobot_vs_manual_labor_comparator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
