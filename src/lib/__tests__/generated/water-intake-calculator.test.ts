// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWater_intake_calculator,
  type Water_intake_calculatorInput,
} from "@generated/water-intake-calculator";

describe("water-intake-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numEmployees": 100,
    "workDaysPerYear": 250,
    "avgWaterUsePerPersonPerDay": 50,
    "processWaterIntensity": 10,
    "annualProductionUnits": 50000,
    "leakageFactor": 5,
    "recyclingRate": 20,
    "seasonalFactor": 1
  } as unknown as Water_intake_calculatorInput;
    const result = calculateWater_intake_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
