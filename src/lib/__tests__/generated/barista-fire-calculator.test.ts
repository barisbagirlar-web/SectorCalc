// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBarista_fire_calculator,
  type Barista_fire_calculatorInput,
} from "@generated/barista-fire-calculator";

describe("barista-fire-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentAge": 30,
    "retirementAge": 65,
    "currentSavings": 50000,
    "monthlySavings": 1000,
    "annualReturn": 7,
    "withdrawalRate": 4,
    "annualExpenses": 40000,
    "partTimeIncome": 12000
  } as unknown as Barista_fire_calculatorInput;
    const result = calculateBarista_fire_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
