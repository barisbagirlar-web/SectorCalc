// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMileage_reimbursement_calculator,
  type Mileage_reimbursement_calculatorInput,
} from "@generated/mileage-reimbursement-calculator";

describe("mileage-reimbursement-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 100,
    "ratePerKm": 0.5,
    "tolls": 0,
    "parking": 0,
    "flatAllowance": 0
  } as unknown as Mileage_reimbursement_calculatorInput;
    const result = calculateMileage_reimbursement_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
