// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMilligrams_to_grams_calculator,
  type Milligrams_to_grams_calculatorInput,
} from "@generated/milligrams-to-grams-calculator";

describe("milligrams-to-grams-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "inputMilligrams": 0,
    "conversionFactor": 0.001,
    "decimalPlaces": 3,
    "batchMultiplier": 1,
    "roundingMode": 0
  } as unknown as Milligrams_to_grams_calculatorInput;
    const result = calculateMilligrams_to_grams_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
