// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePipe_flow_calculator,
  type Pipe_flow_calculatorInput,
} from "@generated/pipe-flow-calculator";

describe("pipe-flow-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pipe_diameter": 0.1,
    "pipe_length": 100,
    "flow_rate": 0.01,
    "fluid_density": 1000,
    "fluid_viscosity": 0.001,
    "roughness": 0.000045,
    "elevation_change": 0,
    "minor_loss_coefficient": 0.5
  } as unknown as Pipe_flow_calculatorInput;
    const result = calculatePipe_flow_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
