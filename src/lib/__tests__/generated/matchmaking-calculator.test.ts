// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMatchmaking_calculator,
  type Matchmaking_calculatorInput,
} from "@generated/matchmaking-calculator";

describe("matchmaking-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "criterion1_score_a": 50,
    "criterion1_score_b": 50,
    "criterion2_score_a": 50,
    "criterion2_score_b": 50,
    "criterion3_score_a": 50,
    "criterion3_score_b": 50,
    "criterion4_score_a": 50,
    "criterion4_score_b": 50
  } as unknown as Matchmaking_calculatorInput;
    const result = calculateMatchmaking_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
