// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePound_to_calorie_calculator,
  type Pound_to_calorie_calculatorInput,
} from "@generated/pound-to-calorie-calculator";

describe("pound-to-calorie-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weightPounds": 1,
    "caloriesPerPound": 3500,
    "includeKilojoules": 0,
    "roundingPrecision": 0
  } as unknown as Pound_to_calorie_calculatorInput;
    const result = calculatePound_to_calorie_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
