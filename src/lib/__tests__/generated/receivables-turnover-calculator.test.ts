// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateReceivables_turnover_calculator,
  type Receivables_turnover_calculatorInput,
} from "@generated/receivables-turnover-calculator";

describe("receivables-turnover-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netCreditSales": 100000,
    "beginningAR": 20000,
    "endingAR": 25000,
    "daysInPeriod": 365
  } as unknown as Receivables_turnover_calculatorInput;
    const result = calculateReceivables_turnover_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
