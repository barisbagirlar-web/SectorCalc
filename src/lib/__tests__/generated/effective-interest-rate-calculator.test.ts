// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEffective_interest_rate_calculator,
  type Effective_interest_rate_calculatorInput,
} from "@generated/effective-interest-rate-calculator";

describe("effective-interest-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nominalRate": 5,
    "compoundingPeriodsPerYear": 12,
    "timePeriodYears": 1,
    "decimalPlaces": 2
  } as unknown as Effective_interest_rate_calculatorInput;
    const result = calculateEffective_interest_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
