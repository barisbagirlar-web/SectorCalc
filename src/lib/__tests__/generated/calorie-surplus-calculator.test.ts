// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalorie_surplus_calculator,
  type Calorie_surplus_calculatorInput,
} from "@generated/calorie-surplus-calculator";

describe("calorie-surplus-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalGenerated": 10000,
    "totalConsumed": 8000,
    "lossPercent": 5,
    "storageEfficiency": 90,
    "auxConsumption": 200
  } as unknown as Calorie_surplus_calculatorInput;
    const result = calculateCalorie_surplus_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
