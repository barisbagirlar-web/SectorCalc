// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateReynolds_number_calculator,
  type Reynolds_number_calculatorInput,
} from "@generated/reynolds-number-calculator";

describe("reynolds-number-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fluid_type": "water",
    "density": 998.2,
    "dynamic_viscosity": 0.001002,
    "velocity": 1.5,
    "pipe_diameter": 0.05,
    "pipe_roughness": 0.000045,
    "temperature": 20,
    "flow_regime_threshold": "standard"
  } as Reynolds_number_calculatorInput;
    const result = calculateReynolds_number_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
