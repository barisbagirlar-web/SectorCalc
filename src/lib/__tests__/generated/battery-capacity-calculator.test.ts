// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBattery_capacity_calculator,
  type Battery_capacity_calculatorInput,
} from "@generated/battery-capacity-calculator";

describe("battery-capacity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loadPower": 100,
    "backupTime": 1,
    "voltage": 12,
    "efficiency": 0.9,
    "depthOfDischarge": 0.8
  } as unknown as Battery_capacity_calculatorInput;
    const result = calculateBattery_capacity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
