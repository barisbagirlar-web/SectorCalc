// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFont_size_converter_calculator,
  type Font_size_converter_calculatorInput,
} from "@generated/font-size-converter-calculator";

describe("font-size-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fontSizePt": 12,
    "ppi": 96,
    "baseFontSizePx": 16,
    "rootFontSizePx": 16,
    "customScale": 1
  } as Font_size_converter_calculatorInput;
    const result = calculateFont_size_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
