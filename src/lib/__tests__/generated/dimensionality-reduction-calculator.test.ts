// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDimensionality_reduction_calculator,
  type Dimensionality_reduction_calculatorInput,
} from "@generated/dimensionality-reduction-calculator";

describe("dimensionality-reduction-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "n": 100,
    "r": 0.9,
    "a": 100,
    "threshold": 0.95
  } as unknown as Dimensionality_reduction_calculatorInput;
    const result = calculateDimensionality_reduction_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
