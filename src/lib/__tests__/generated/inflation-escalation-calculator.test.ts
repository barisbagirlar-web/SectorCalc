// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInflation_escalation_calculator,
  type Inflation_escalation_calculatorInput,
} from "@generated/inflation-escalation-calculator";

describe("inflation-escalation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "base_cost": 100000,
    "inflation_rate": 3,
    "period_years": 5,
    "escalation_method": "compound",
    "include_energy": true,
    "energy_escalation_rate": 5,
    "labor_productivity_gain": 1,
    "material_volatility_index": 1
  } as Inflation_escalation_calculatorInput;
    const result = calculateInflation_escalation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
