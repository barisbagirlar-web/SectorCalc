// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSlope_stability_calculator,
  type Slope_stability_calculatorInput,
} from "@generated/slope-stability-calculator";

describe("slope-stability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "slopeAngle": 30,
    "frictionAngle": 25,
    "cohesion": 10,
    "unitWeight": 18,
    "depth": 5,
    "porePressureRatio": 0
  } as unknown as Slope_stability_calculatorInput;
    const result = calculateSlope_stability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
