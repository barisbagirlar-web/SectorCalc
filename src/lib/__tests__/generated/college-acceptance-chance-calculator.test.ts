// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCollege_acceptance_chance_calculator,
  type College_acceptance_chance_calculatorInput,
} from "@generated/college-acceptance-chance-calculator";

describe("college-acceptance-chance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "gpa": 3,
    "sat": 1200,
    "extracurriculars": 5,
    "essay": 3,
    "interview": 3
  } as unknown as College_acceptance_chance_calculatorInput;
    const result = calculateCollege_acceptance_chance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
