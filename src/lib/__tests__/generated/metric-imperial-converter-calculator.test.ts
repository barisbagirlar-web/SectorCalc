// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMetric_imperial_converter_calculator,
  type Metric_imperial_converter_calculatorInput,
} from "@generated/metric-imperial-converter-calculator";

describe("metric-imperial-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "meters": 0,
    "kilograms": 0,
    "liters": 0,
    "celsius": 0
  } as unknown as Metric_imperial_converter_calculatorInput;
    const result = calculateMetric_imperial_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
