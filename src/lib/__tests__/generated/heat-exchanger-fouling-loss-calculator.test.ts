// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHeat_exchanger_fouling_loss_calculator,
  type Heat_exchanger_fouling_loss_calculatorInput,
} from "@generated/heat-exchanger-fouling-loss-calculator";

describe("heat-exchanger-fouling-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "design_ua": 500,
    "actual_ua": 350,
    "hot_inlet_temp": 120,
    "cold_inlet_temp": 30,
    "hot_flow_rate": 50,
    "hot_specific_heat": 2.5,
    "energy_cost": 0.08,
    "operating_hours_per_year": 8000
  } as unknown as Heat_exchanger_fouling_loss_calculatorInput;
    const result = calculateHeat_exchanger_fouling_loss_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
