// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTroy_pounds_to_kg_calculator,
  type Troy_pounds_to_kg_calculatorInput,
} from "@generated/troy-pounds-to-kg-calculator";

describe("troy-pounds-to-kg-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "troyPounds": 1,
    "precision": 4,
    "batchSize": 1,
    "scaleCalibrationFactor": 1,
    "measurementUncertainty": 0.1
  } as unknown as Troy_pounds_to_kg_calculatorInput;
    const result = calculateTroy_pounds_to_kg_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
