// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLong_term_care_calculator,
  type Long_term_care_calculatorInput,
} from "@generated/long-term-care-calculator";

describe("long-term-care-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentAge": 45,
    "retirementAge": 65,
    "expectedLongTermCareYears": 3,
    "dailyCareCost": 150,
    "inflationRate": 2.5,
    "savingsForCare": 50000,
    "otherIncome": 20000
  } as Long_term_care_calculatorInput;
    const result = calculateLong_term_care_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
