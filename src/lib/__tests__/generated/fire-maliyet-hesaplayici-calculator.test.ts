// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFire_maliyet_hesaplayici_calculator,
  type Fire_maliyet_hesaplayici_calculatorInput,
} from "@generated/fire-maliyet-hesaplayici-calculator";

describe("fire-maliyet-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalMaterial": 100,
    "goodProduct": 85,
    "reworkMaterial": 10,
    "scrapPrice": 2.5,
    "materialCost": 12,
    "laborOverhead": 5
  } as Fire_maliyet_hesaplayici_calculatorInput;
    const result = calculateFire_maliyet_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
