// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCar_payment_calculator,
  type Car_payment_calculatorInput,
} from "@generated/car-payment-calculator";

describe("car-payment-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "carPrice": 20000,
    "downPayment": 2000,
    "annualInterestRate": 4.5,
    "loanTermMonths": 60,
    "salesTaxRate": 6,
    "additionalFees": 500
  } as unknown as Car_payment_calculatorInput;
    const result = calculateCar_payment_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
