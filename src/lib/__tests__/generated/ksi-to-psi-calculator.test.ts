// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKsi_to_psi_calculator,
  type Ksi_to_psi_calculatorInput,
} from "@generated/ksi-to-psi-calculator";

describe("ksi-to-psi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nominal_pressure_ksi": 0,
    "safety_factor": 1.5,
    "temperature_derating": 1,
    "ambient_pressure_psi": 14.7,
    "allowable_stress_ksi": 30
  } as unknown as Ksi_to_psi_calculatorInput;
    const result = calculateKsi_to_psi_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
