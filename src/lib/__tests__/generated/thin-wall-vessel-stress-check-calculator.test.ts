// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateThin_wall_vessel_stress_check_calculator,
  type Thin_wall_vessel_stress_check_calculatorInput,
} from "@generated/thin-wall-vessel-stress-check-calculator";

describe("thin-wall-vessel-stress-check-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "P": 1,
    "D": 1000,
    "t": 10,
    "S": 150,
    "E": 1
  } as Thin_wall_vessel_stress_check_calculatorInput;
    const result = calculateThin_wall_vessel_stress_check_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
