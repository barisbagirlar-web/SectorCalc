// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSubcontractor_margin_leak_detector_calculator,
  type Subcontractor_margin_leak_detector_calculatorInput,
} from "@generated/subcontractor-margin-leak-detector-calculator";

describe("subcontractor-margin-leak-detector-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "contract_value": 1000000,
    "actual_labor_cost": 350000,
    "actual_material_cost": 250000,
    "actual_equipment_cost": 100000,
    "overhead_percentage": 15,
    "quality_rework_cost": 50000,
    "schedule_delay_penalty": 20000,
    "waste_factor": 8
  } as Subcontractor_margin_leak_detector_calculatorInput;
    const result = calculateSubcontractor_margin_leak_detector_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
