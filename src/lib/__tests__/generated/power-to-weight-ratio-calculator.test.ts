// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePower_to_weight_ratio_calculator,
  type Power_to_weight_ratio_calculatorInput,
} from "@generated/power-to-weight-ratio-calculator";

describe("power-to-weight-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "enginePowerkW": 100,
    "vehicleMassKg": 1200,
    "drivetrainEfficiencyPercent": 85,
    "payloadKg": 200
  } as Power_to_weight_ratio_calculatorInput;
    const result = calculatePower_to_weight_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
