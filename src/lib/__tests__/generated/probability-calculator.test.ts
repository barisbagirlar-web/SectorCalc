// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProbability_calculator,
  type Probability_calculatorInput,
} from "@generated/probability-calculator";

describe("probability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "defect_count": 10,
    "sample_size": 1000,
    "sigma_shift": 1.5,
    "distribution_type": "binomial",
    "confidence_level": "95",
    "use_historical_bias": false
  } as unknown as Probability_calculatorInput;
    const result = calculateProbability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
