// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePs_ratio_calculator,
  type Ps_ratio_calculatorInput,
} from "@generated/ps-ratio-calculator";

describe("ps-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "marketCap": 1,
    "totalSales": 1,
    "pricePerShare": 1,
    "salesPerShare": 1
  } as unknown as Ps_ratio_calculatorInput;
    const result = calculatePs_ratio_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
