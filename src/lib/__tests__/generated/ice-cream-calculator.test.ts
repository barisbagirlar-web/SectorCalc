// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIce_cream_calculator,
  type Ice_cream_calculatorInput,
} from "@generated/ice-cream-calculator";

describe("ice-cream-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "batchMixVolume": 100,
    "overrunPercent": 50,
    "mixCostPerLiter": 2.5,
    "packagingCostPerLiter": 0.5,
    "otherCostsPerBatch": 50,
    "desiredProfitMarginPercent": 30
  } as unknown as Ice_cream_calculatorInput;
    const result = calculateIce_cream_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
