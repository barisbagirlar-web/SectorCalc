// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWelding_calculator,
  type Welding_calculatorInput,
} from "@generated/welding-calculator";

describe("welding-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weldLength": 100,
    "weldThroat": 5,
    "electrodeDiameter": 3.2,
    "depositionEfficiency": 65,
    "electrodeDensity": 7.85,
    "laborCostPerHour": 45,
    "electrodeCostPerKg": 3.5,
    "weldingSpeed": 300
  } as unknown as Welding_calculatorInput;
    const result = calculateWelding_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
