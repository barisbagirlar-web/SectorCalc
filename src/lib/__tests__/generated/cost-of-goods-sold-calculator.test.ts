// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCost_of_goods_sold_calculator,
  type Cost_of_goods_sold_calculatorInput,
} from "@generated/cost-of-goods-sold-calculator";

describe("cost-of-goods-sold-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "beginningInventory": 0,
    "materialPurchases": 0,
    "directLabor": 0,
    "overhead": 0,
    "endingInventory": 0
  } as unknown as Cost_of_goods_sold_calculatorInput;
    const result = calculateCost_of_goods_sold_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
