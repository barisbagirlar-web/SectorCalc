// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFortnights_to_days_calculator,
  type Fortnights_to_days_calculatorInput,
} from "@generated/fortnights-to-days-calculator";

describe("fortnights-to-days-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fortnights": 1,
    "daysPerFortnight": 14,
    "decimalPlaces": 2,
    "batchSize": 1
  } as unknown as Fortnights_to_days_calculatorInput;
    const result = calculateFortnights_to_days_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
