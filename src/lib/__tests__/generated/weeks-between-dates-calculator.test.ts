// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWeeks_between_dates_calculator,
  type Weeks_between_dates_calculatorInput,
} from "@generated/weeks-between-dates-calculator";

describe("weeks-between-dates-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "startDay": 1,
    "startMonth": 1,
    "startYear": 2025,
    "endDay": 15,
    "endMonth": 1,
    "endYear": 2025
  } as Weeks_between_dates_calculatorInput;
    const result = calculateWeeks_between_dates_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
