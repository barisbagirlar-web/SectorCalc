// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFeed_cost_estimator_calculator,
  type Feed_cost_estimator_calculatorInput,
} from "@generated/feed-cost-estimator-calculator";

describe("feed-cost-estimator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "feed_intake_kg": 10,
    "raw_material_cost_per_kg": 0.35,
    "processing_loss_percent": 3,
    "moisture_adjustment_factor": 1,
    "quality_grade": "standard",
    "include_transport_cost": false
  } as unknown as Feed_cost_estimator_calculatorInput;
    const result = calculateFeed_cost_estimator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
