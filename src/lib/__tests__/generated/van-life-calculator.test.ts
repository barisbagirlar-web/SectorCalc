// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVan_life_calculator,
  type Van_life_calculatorInput,
} from "@generated/van-life-calculator";

describe("van-life-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dailyEnergyWh": 1000,
    "sunPeakHours": 5,
    "panelWattage": 300,
    "tiltAngleDeviation": 0,
    "systemEfficiency": 80,
    "daysAutonomy": 2,
    "batteryVoltage": 12,
    "depthOfDischarge": 50
  } as unknown as Van_life_calculatorInput;
    const result = calculateVan_life_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
