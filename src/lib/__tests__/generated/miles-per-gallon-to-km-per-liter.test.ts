// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMiles_per_gallon_to_km_per_liter,
  type Miles_per_gallon_to_km_per_literInput,
} from "@generated/miles-per-gallon-to-km-per-liter";

describe("miles-per-gallon-to-km-per-liter", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mpg": 25,
    "fuelType": 1,
    "temperatureCorrection": 1,
    "altitudeCorrection": 1
  } as unknown as Miles_per_gallon_to_km_per_literInput;
    const result = calculateMiles_per_gallon_to_km_per_liter(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
