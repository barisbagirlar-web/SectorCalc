// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateImplicit_differentiation_calculator,
  type Implicit_differentiation_calculatorInput,
} from "@generated/implicit-differentiation-calculator";

describe("implicit-differentiation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "coeff_x2": 1,
    "coeff_y2": 1,
    "coeff_xy": 0,
    "coeff_x": 0,
    "coeff_y": 0,
    "const": -1,
    "x": 0.5,
    "y": 0.8660254037844386
  } as unknown as Implicit_differentiation_calculatorInput;
    const result = calculateImplicit_differentiation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
