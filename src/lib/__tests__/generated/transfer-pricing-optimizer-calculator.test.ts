// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTransfer_pricing_optimizer_calculator,
  type Transfer_pricing_optimizer_calculatorInput,
} from "@generated/transfer-pricing-optimizer-calculator";

describe("transfer-pricing-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "production_cost_per_unit": 100,
    "market_price_per_unit": 150,
    "volume_units": 10000,
    "tax_rate_producer": 21,
    "tax_rate_distributor": 25,
    "transfer_price_current": 120,
    "compliance_risk_tolerance": "Medium",
    "include_lean_waste_factor": true
  } as Transfer_pricing_optimizer_calculatorInput;
    const result = calculateTransfer_pricing_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
