// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHome_maintenance_calculator,
  type Home_maintenance_calculatorInput,
} from "@generated/home-maintenance-calculator";

describe("home-maintenance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "area": 1500,
    "age": 10,
    "baseCostPerSqFt": 1.5,
    "ageMultiplier": 0.02,
    "inflation": 0.03,
    "yearsToProject": 5
  } as Home_maintenance_calculatorInput;
    const result = calculateHome_maintenance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
