// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateReptile_temperature_calculator,
  type Reptile_temperature_calculatorInput,
} from "@generated/reptile-temperature-calculator";

describe("reptile-temperature-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "enclosureLength": 60,
    "enclosureWidth": 45,
    "enclosureHeight": 45,
    "ambientTemp": 20,
    "desiredTemp": 35,
    "insulationFactor": 5
  } as unknown as Reptile_temperature_calculatorInput;
    const result = calculateReptile_temperature_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
