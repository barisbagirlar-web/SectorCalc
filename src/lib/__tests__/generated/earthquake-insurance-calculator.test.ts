// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEarthquake_insurance_calculator,
  type Earthquake_insurance_calculatorInput,
} from "@generated/earthquake-insurance-calculator";

describe("earthquake-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "buildingValue": 500000,
    "seismicZone": 1,
    "constructionType": 1,
    "buildingAge": 10,
    "deductible": 2
  } as Earthquake_insurance_calculatorInput;
    const result = calculateEarthquake_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
