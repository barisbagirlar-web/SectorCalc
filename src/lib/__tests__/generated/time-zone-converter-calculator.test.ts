// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTime_zone_converter_calculator,
  type Time_zone_converter_calculatorInput,
} from "@generated/time-zone-converter-calculator";

describe("time-zone-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "inputHours": 12,
    "inputMinutes": 0,
    "sourceOffsetHours": 0,
    "sourceOffsetMinutes": 0,
    "targetOffsetHours": 3,
    "targetOffsetMinutes": 0
  } as unknown as Time_zone_converter_calculatorInput;
    const result = calculateTime_zone_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
