// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFood_conversion_calculator,
  type Food_conversion_calculatorInput,
} from "@generated/food-conversion-calculator";

describe("food-conversion-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "volumeMl": 250,
    "densityGml": 1,
    "numberOfServings": 1,
    "caloriesPerGram": 0.04,
    "cupFactor": 236.588
  } as unknown as Food_conversion_calculatorInput;
    const result = calculateFood_conversion_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
