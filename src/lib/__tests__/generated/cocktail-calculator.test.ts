// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCocktail_calculator,
  type Cocktail_calculatorInput,
} from "@generated/cocktail-calculator";

describe("cocktail-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numberOfCocktails": 10,
    "drinkVolume": 200,
    "ingredient1Part": 2,
    "ingredient2Part": 1,
    "ingredient3Part": 1.5,
    "ingredient4Part": 0,
    "wastagePercentage": 5
  } as unknown as Cocktail_calculatorInput;
    const result = calculateCocktail_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
