// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInstrumentation_amplifier_calculator,
  type Instrumentation_amplifier_calculatorInput,
} from "@generated/instrumentation-amplifier-calculator";

describe("instrumentation-amplifier-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "r1": 1000,
    "r2": 10000,
    "vinPlus": 0.01,
    "vinMinus": 0,
    "vref": 0
  } as Instrumentation_amplifier_calculatorInput;
    const result = calculateInstrumentation_amplifier_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
