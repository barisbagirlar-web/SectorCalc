// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAirbnb_profit_calculator,
  type Airbnb_profit_calculatorInput,
} from "@generated/airbnb-profit-calculator";

describe("airbnb-profit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nightlyRate": 150,
    "occupancyRate": 60,
    "nightsAvailable": 30,
    "avgStay": 3,
    "cleaningFee": 50,
    "airbnbFeePercent": 3,
    "monthlyExpenses": 2000
  } as Airbnb_profit_calculatorInput;
    const result = calculateAirbnb_profit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
