// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateActive_earth_pressure_calculator,
  type Active_earth_pressure_calculatorInput,
} from "@generated/active-earth-pressure-calculator";

describe("active-earth-pressure-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "height": 5,
    "unitWeight": 18,
    "frictionAngle": 30,
    "cohesion": 10,
    "surcharge": 0
  } as Active_earth_pressure_calculatorInput;
    const result = calculateActive_earth_pressure_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
