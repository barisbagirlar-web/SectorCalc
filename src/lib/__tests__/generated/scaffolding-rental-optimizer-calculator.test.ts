// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateScaffolding_rental_optimizer_calculator,
  type Scaffolding_rental_optimizer_calculatorInput,
} from "@generated/scaffolding-rental-optimizer-calculator";

describe("scaffolding-rental-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "project_duration_days": 30,
    "total_scaffold_area_sqm": 500,
    "rental_rate_per_sqm_per_day": 2.5,
    "transport_cost_per_trip": 200,
    "number_of_trips": 4,
    "labor_cost_per_hour": 35,
    "erection_hours_per_sqm": 0.3,
    "dismantle_hours_per_sqm": 0.2
  } as Scaffolding_rental_optimizer_calculatorInput;
    const result = calculateScaffolding_rental_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
