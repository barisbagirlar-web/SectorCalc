// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMonte_carlo_profit_estimator_calculator,
  type Monte_carlo_profit_estimator_calculatorInput,
} from "@generated/monte-carlo-profit-estimator-calculator";

describe("monte-carlo-profit-estimator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "revenueMean": 100000,
    "revenueStd": 10000,
    "costMean": 70000,
    "costStd": 5000,
    "correlation": 0,
    "zScore": 1.96
  } as unknown as Monte_carlo_profit_estimator_calculatorInput;
    const result = calculateMonte_carlo_profit_estimator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
