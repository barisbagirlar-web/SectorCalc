// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTakt_time_flexibility_cost_calculator,
  type Takt_time_flexibility_cost_calculatorInput,
} from "@generated/takt-time-flexibility-cost-calculator";

describe("takt-time-flexibility-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "available_work_time_seconds": 28800,
    "customer_demand_per_shift": 1000,
    "cycle_time_seconds": 30,
    "changeover_time_minutes": 15,
    "batch_size": 100,
    "labor_cost_per_hour": 25,
    "overhead_rate_percent": 150,
    "demand_variability_coefficient": 0.3
  } as unknown as Takt_time_flexibility_cost_calculatorInput;
    const result = calculateTakt_time_flexibility_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
