// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIndicated_horsepower_calculator,
  type Indicated_horsepower_calculatorInput,
} from "@generated/indicated-horsepower-calculator";

describe("indicated-horsepower-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pressure": 100,
    "bore": 4,
    "stroke": 4,
    "speed": 3000,
    "cylinders": 4,
    "cycles": 4
  } as unknown as Indicated_horsepower_calculatorInput;
    const result = calculateIndicated_horsepower_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
