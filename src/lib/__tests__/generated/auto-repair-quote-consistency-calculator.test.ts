// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAuto_repair_quote_consistency_calculator,
  type Auto_repair_quote_consistency_calculatorInput,
} from "@generated/auto-repair-quote-consistency-calculator";

describe("auto-repair-quote-consistency-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "labor_rate": 100,
    "labor_hours": 4,
    "parts_cost": 500,
    "shop_supplies": 50,
    "tax_rate": 8,
    "shop_type": "independent",
    "repair_category": "mechanical",
    "use_original_parts": true
  } as Auto_repair_quote_consistency_calculatorInput;
    const result = calculateAuto_repair_quote_consistency_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
