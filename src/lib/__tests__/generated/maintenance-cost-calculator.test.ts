// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMaintenance_cost_calculator,
  type Maintenance_cost_calculatorInput,
} from "@generated/maintenance-cost-calculator";

describe("maintenance-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "laborRate": 50,
    "technicians": 2,
    "hoursPerMaintenance": 4,
    "maintenanceFrequency": 12,
    "partsCostPerMaintenance": 200,
    "overheadCostPerMaintenance": 100
  } as unknown as Maintenance_cost_calculatorInput;
    const result = calculateMaintenance_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
