// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBenefit_cost_ratio_calculator,
  type Benefit_cost_ratio_calculatorInput,
} from "@generated/benefit-cost-ratio-calculator";

describe("benefit-cost-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialCost": 100000,
    "annualBenefits": 30000,
    "annualCosts": 5000,
    "projectLife": 10,
    "discountRate": 8
  } as unknown as Benefit_cost_ratio_calculatorInput;
    const result = calculateBenefit_cost_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
