// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePallet_rack_optimizer_calculator,
  type Pallet_rack_optimizer_calculatorInput,
} from "@generated/pallet-rack-optimizer-calculator";

describe("pallet-rack-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rack_height_mm": 12000,
    "pallet_depth_mm": 1200,
    "pallet_width_mm": 1000,
    "pallet_height_mm": 1500,
    "beam_thickness_mm": 100,
    "clearance_vertical_mm": 100,
    "aisle_width_mm": 3000,
    "rack_length_mm": 2700
  } as Pallet_rack_optimizer_calculatorInput;
    const result = calculatePallet_rack_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
