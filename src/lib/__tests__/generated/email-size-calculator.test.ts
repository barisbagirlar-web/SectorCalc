// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEmail_size_calculator,
  type Email_size_calculatorInput,
} from "@generated/email-size-calculator";

describe("email-size-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "textCharacters": 1000,
    "attachmentCount": 2,
    "avgAttachmentSize": 500,
    "embeddedImageCount": 3,
    "avgImageSize": 200,
    "metadataSize": 1024
  } as unknown as Email_size_calculatorInput;
    const result = calculateEmail_size_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
