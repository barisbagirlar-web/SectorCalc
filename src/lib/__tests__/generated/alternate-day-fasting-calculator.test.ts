// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAlternate_day_fasting_calculator,
  type Alternate_day_fasting_calculatorInput,
} from "@generated/alternate-day-fasting-calculator";

describe("alternate-day-fasting-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "normalCal": 2000,
    "fastingCal": 500,
    "totalDays": 30,
    "startDay": 0
  } as unknown as Alternate_day_fasting_calculatorInput;
    const result = calculateAlternate_day_fasting_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
