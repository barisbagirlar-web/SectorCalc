// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLally_column_calculator,
  type Lally_column_calculatorInput,
} from "@generated/lally-column-calculator";

describe("lally-column-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "column_height": 2.5,
    "outer_diameter": 89,
    "wall_thickness": 3,
    "steel_yield_strength": 250,
    "effective_length_factor": 1,
    "safety_factor": 1.5,
    "youngs_modulus": 200000
  } as Lally_column_calculatorInput;
    const result = calculateLally_column_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
