// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateColumn_buckling_calculator,
  type Column_buckling_calculatorInput,
} from "@generated/column-buckling-calculator";

describe("column-buckling-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "elasticModulus": 200000,
    "momentInertia": 1000000,
    "area": 5000,
    "length": 3000,
    "effectiveLengthFactor": 1,
    "appliedLoad": 50000
  } as Column_buckling_calculatorInput;
    const result = calculateColumn_buckling_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
