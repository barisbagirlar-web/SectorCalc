// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGestational_diabetes_risk_calculator,
  type Gestational_diabetes_risk_calculatorInput,
} from "@generated/gestational-diabetes-risk-calculator";

describe("gestational-diabetes-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 30,
    "bmi": 25,
    "familyHistory": 1,
    "previousGdm": 1,
    "ethnicity": 1,
    "fastingGlucose": 90,
    "hba1c": 5.2,
    "triglycerides": 130
  } as unknown as Gestational_diabetes_risk_calculatorInput;
    const result = calculateGestational_diabetes_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
