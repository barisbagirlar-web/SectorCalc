// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStandard_deviation_calculator,
  type Standard_deviation_calculatorInput,
} from "@generated/standard-deviation-calculator";

describe("standard-deviation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "data_points": "",
    "sample_size": 10,
    "data_type": "continuous",
    "population_flag": "sample",
    "unit_of_measure": "mm"
  } as Standard_deviation_calculatorInput;
    const result = calculateStandard_deviation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
