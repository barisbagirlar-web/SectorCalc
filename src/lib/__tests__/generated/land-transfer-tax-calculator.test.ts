// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLand_transfer_tax_calculator,
  type Land_transfer_tax_calculatorInput,
} from "@generated/land-transfer-tax-calculator";

describe("land-transfer-tax-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "propertyValue": 500000,
    "taxRate": 2.5,
    "exemptionAmount": 4000,
    "isFirstTimeBuyer": 0,
    "fixedFee": 250
  } as Land_transfer_tax_calculatorInput;
    const result = calculateLand_transfer_tax_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
