// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFiber_cement_siding_calculator,
  type Fiber_cement_siding_calculatorInput,
} from "@generated/fiber-cement-siding-calculator";

describe("fiber-cement-siding-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wall_length": 10,
    "wall_height": 3,
    "siding_width": 0.2,
    "siding_overlap": 0.025,
    "waste_factor": 10,
    "labor_rate": 15,
    "material_cost": 25
  } as unknown as Fiber_cement_siding_calculatorInput;
    const result = calculateFiber_cement_siding_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
