// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTennis_serve_speed_calculator,
  type Tennis_serve_speed_calculatorInput,
} from "@generated/tennis-serve-speed-calculator";

describe("tennis-serve-speed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 18.29,
    "timeOfFlight": 0.5,
    "ballMass": 0.0577,
    "launchHeight": 2.6,
    "landingHeight": 0
  } as unknown as Tennis_serve_speed_calculatorInput;
    const result = calculateTennis_serve_speed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
