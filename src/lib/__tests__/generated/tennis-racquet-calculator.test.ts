// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTennis_racquet_calculator,
  type Tennis_racquet_calculatorInput,
} from "@generated/tennis-racquet-calculator";

describe("tennis-racquet-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "racquetMass": 300,
    "racquetBalance": 32,
    "racquetSwingWeight": 320,
    "addedMass": 5,
    "addedPosition": 68.5
  } as unknown as Tennis_racquet_calculatorInput;
    const result = calculateTennis_racquet_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
