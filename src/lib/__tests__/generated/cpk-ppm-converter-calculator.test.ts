// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCpk_ppm_converter_calculator,
  type Cpk_ppm_converter_calculatorInput,
} from "@generated/cpk-ppm-converter-calculator";

describe("cpk-ppm-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cpk": 1.33,
    "sigmaShift": 1.5,
    "distributionType": "normal",
    "sampleSize": 100,
    "confidenceLevel": 0.95,
    "useConfidenceInterval": false
  } as Cpk_ppm_converter_calculatorInput;
    const result = calculateCpk_ppm_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
