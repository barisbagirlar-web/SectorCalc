// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePermutation_generator_calculator,
  type Permutation_generator_calculatorInput,
} from "@generated/permutation-generator-calculator";

describe("permutation-generator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "n": 5,
    "r": 3,
    "orderMatters": 1,
    "repetitionAllowed": 0
  } as unknown as Permutation_generator_calculatorInput;
    const result = calculatePermutation_generator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
