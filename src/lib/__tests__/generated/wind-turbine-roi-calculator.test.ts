// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWind_turbine_roi_calculator,
  type Wind_turbine_roi_calculatorInput,
} from "@generated/wind-turbine-roi-calculator";

describe("wind-turbine-roi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "turbine_capacity": 2,
    "capacity_factor": 35,
    "capital_cost_per_mw": 1300000,
    "opex_per_mwh": 12,
    "electricity_price": 50,
    "discount_rate": 8,
    "project_lifetime": 20,
    "degradation_rate": 0.5
  } as unknown as Wind_turbine_roi_calculatorInput;
    const result = calculateWind_turbine_roi_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
