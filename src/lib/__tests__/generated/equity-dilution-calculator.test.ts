// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEquity_dilution_calculator,
  type Equity_dilution_calculatorInput,
} from "@generated/equity-dilution-calculator";

describe("equity-dilution-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalOutstandingShares": 1000000,
    "newSharesIssued": 200000,
    "investorCurrentShares": 100000,
    "investorNewShares": 0
  } as Equity_dilution_calculatorInput;
    const result = calculateEquity_dilution_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
