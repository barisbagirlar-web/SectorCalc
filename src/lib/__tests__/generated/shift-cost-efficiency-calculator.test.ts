// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateShift_cost_efficiency_calculator,
  type Shift_cost_efficiency_calculatorInput,
} from "@generated/shift-cost-efficiency-calculator";

describe("shift-cost-efficiency-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "shift_duration_hours": 8,
    "total_units_produced": 1000,
    "defective_units": 50,
    "rework_units": 30,
    "labor_cost_per_hour": 25,
    "number_of_operators": 10,
    "material_cost_per_unit": 2.5,
    "energy_cost_per_shift": 500
  } as Shift_cost_efficiency_calculatorInput;
    const result = calculateShift_cost_efficiency_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
