// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAirbnb_occupancy_calculator,
  type Airbnb_occupancy_calculatorInput,
} from "@generated/airbnb-occupancy-calculator";

describe("airbnb-occupancy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bookedNights": 15,
    "totalAvailableNights": 30,
    "averageDailyRate": 120,
    "cleaningFeePerBooking": 50,
    "numberOfBookings": 3,
    "fixedMonthlyCosts": 800
  } as Airbnb_occupancy_calculatorInput;
    const result = calculateAirbnb_occupancy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
