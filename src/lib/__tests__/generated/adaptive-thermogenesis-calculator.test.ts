// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAdaptive_thermogenesis_calculator,
  type Adaptive_thermogenesis_calculatorInput,
} from "@generated/adaptive-thermogenesis-calculator";

describe("adaptive-thermogenesis-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bee": 2000,
    "energyIntake": 1500,
    "duration": 30,
    "weight": 70,
    "tau": 14,
    "fatFreeMass": 50
  } as unknown as Adaptive_thermogenesis_calculatorInput;
    const result = calculateAdaptive_thermogenesis_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
