// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCubic_feet_to_cubic_meters_calculator,
  type Cubic_feet_to_cubic_meters_calculatorInput,
} from "@generated/cubic-feet-to-cubic-meters-calculator";

describe("cubic-feet-to-cubic-meters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lengthFeet": 1,
    "widthFeet": 1,
    "heightFeet": 1,
    "decimalPlaces": 2
  } as Cubic_feet_to_cubic_meters_calculatorInput;
    const result = calculateCubic_feet_to_cubic_meters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
