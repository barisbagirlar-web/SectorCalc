// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDollar_cost_averaging_calculator,
  type Dollar_cost_averaging_calculatorInput,
} from "@generated/dollar-cost-averaging-calculator";

describe("dollar-cost-averaging-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 0,
    "periodicInvestment": 100,
    "years": 10,
    "periodsPerYear": 12,
    "annualReturnRate": 7
  } as unknown as Dollar_cost_averaging_calculatorInput;
    const result = calculateDollar_cost_averaging_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
