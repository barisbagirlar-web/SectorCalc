// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMountaineering_calculator,
  type Mountaineering_calculatorInput,
} from "@generated/mountaineering-calculator";

describe("mountaineering-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 10,
    "elevationGain": 800,
    "flatSpeed": 5,
    "ascentPace": 600,
    "difficultyMultiplier": 1,
    "safetyMarginPercent": 10
  } as unknown as Mountaineering_calculatorInput;
    const result = calculateMountaineering_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
