// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateYoungs_double_slit_calculator,
  type Youngs_double_slit_calculatorInput,
} from "@generated/youngs-double-slit-calculator";

describe("youngs-double-slit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wavelength": 5e-7,
    "slitSeparation": 0.001,
    "screenDistance": 1,
    "fringeOrder": 1
  } as unknown as Youngs_double_slit_calculatorInput;
    const result = calculateYoungs_double_slit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
