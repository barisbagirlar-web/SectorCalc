// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMachine_economic_life_calculator,
  type Machine_economic_life_calculatorInput,
} from "@generated/machine-economic-life-calculator";

describe("machine-economic-life-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchase_price": 100000,
    "salvage_value": 10000,
    "useful_life_years": 10,
    "annual_operating_cost": 15000,
    "operating_cost_escalation_rate": 3,
    "discount_rate": 8,
    "annual_production_units": 50000,
    "revenue_per_unit": 5
  } as unknown as Machine_economic_life_calculatorInput;
    const result = calculateMachine_economic_life_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
