// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGhg_protocol_calculator,
  type Ghg_protocol_calculatorInput,
} from "@generated/ghg-protocol-calculator";

describe("ghg-protocol-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "electricityKwh": 1000,
    "electricityFactor": 0.5,
    "naturalGasM3": 500,
    "naturalGasFactor": 2,
    "fuelLiters": 2000,
    "fuelFactor": 2.3
  } as unknown as Ghg_protocol_calculatorInput;
    const result = calculateGhg_protocol_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
