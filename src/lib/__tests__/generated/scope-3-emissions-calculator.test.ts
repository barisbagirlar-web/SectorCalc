// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateScope_3_emissions_calculator,
  type Scope_3_emissions_calculatorInput,
} from "@generated/scope-3-emissions-calculator";

describe("scope-3-emissions-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualSpendOnPurchasedGoods": 1000000,
    "emissionFactorPurchasedGoods": 0.5,
    "businessTravelDistance": 50000,
    "emissionFactorBusinessTravel": 0.2,
    "employeeCommutingDistance": 200000,
    "emissionFactorCommuting": 0.15,
    "wasteGenerated": 100,
    "emissionFactorWaste": 500
  } as unknown as Scope_3_emissions_calculatorInput;
    const result = calculateScope_3_emissions_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
