// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHoliday_budget_calculator,
  type Holiday_budget_calculatorInput,
} from "@generated/holiday-budget-calculator";

describe("holiday-budget-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "travelers": 2,
    "days": 7,
    "flightCostPerPerson": 500,
    "hotelCostPerNight": 150,
    "dailyFoodBudgetPerPerson": 50,
    "dailyActivityBudgetPerPerson": 30
  } as Holiday_budget_calculatorInput;
    const result = calculateHoliday_budget_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
