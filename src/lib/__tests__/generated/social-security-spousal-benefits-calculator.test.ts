// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSocial_security_spousal_benefits_calculator,
  type Social_security_spousal_benefits_calculatorInput,
} from "@generated/social-security-spousal-benefits-calculator";

describe("social-security-spousal-benefits-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "primaryPIA": 2000,
    "spousePIA": 0,
    "spouseFilingAge": 62,
    "spouseFRA": 67
  } as Social_security_spousal_benefits_calculatorInput;
    const result = calculateSocial_security_spousal_benefits_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
