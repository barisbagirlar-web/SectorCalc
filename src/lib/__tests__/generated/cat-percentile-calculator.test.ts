// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCat_percentile_calculator,
  type Cat_percentile_calculatorInput,
} from "@generated/cat-percentile-calculator";

describe("cat-percentile-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "overall_rank": 1,
    "total_candidates": 200000,
    "qa_rank": 1,
    "qa_total": 200000,
    "varc_rank": 1,
    "varc_total": 200000,
    "lrdi_rank": 1,
    "lrdi_total": 200000
  } as Cat_percentile_calculatorInput;
    const result = calculateCat_percentile_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
