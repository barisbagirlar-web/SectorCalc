// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSpearman_correlation_calculator,
  type Spearman_correlation_calculatorInput,
} from "@generated/spearman-correlation-calculator";

describe("spearman-correlation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sampleSize": 5,
    "sumSquaredDifferences": 2,
    "tieCorrectionX": 0,
    "tieCorrectionY": 0
  } as unknown as Spearman_correlation_calculatorInput;
    const result = calculateSpearman_correlation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
