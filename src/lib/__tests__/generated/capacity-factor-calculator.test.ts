// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCapacity_factor_calculator,
  type Capacity_factor_calculatorInput,
} from "@generated/capacity-factor-calculator";

describe("capacity-factor-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "actualEnergy": 0,
    "installedCapacity": 0,
    "hoursPeriod": 8760,
    "availabilityFactor": 1,
    "targetCapacityFactor": 50
  } as Capacity_factor_calculatorInput;
    const result = calculateCapacity_factor_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
