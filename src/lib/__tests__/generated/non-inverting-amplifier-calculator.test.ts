// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNon_inverting_amplifier_calculator,
  type Non_inverting_amplifier_calculatorInput,
} from "@generated/non-inverting-amplifier-calculator";

describe("non-inverting-amplifier-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "vin": 1,
    "r1": 10000,
    "r2": 10000,
    "vcc": 15,
    "vee": -15,
    "headroom": 1.5
  } as Non_inverting_amplifier_calculatorInput;
    const result = calculateNon_inverting_amplifier_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
