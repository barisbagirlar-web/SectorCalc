// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRepeated_measures_anova_calculator,
  type Repeated_measures_anova_calculatorInput,
} from "@generated/repeated-measures-anova-calculator";

describe("repeated-measures-anova-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numberOfConditions": 3,
    "numberOfSubjects": 10,
    "ssCondition": 100,
    "ssError": 50
  } as Repeated_measures_anova_calculatorInput;
    const result = calculateRepeated_measures_anova_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
