// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePercent_decrease_calculator,
  type Percent_decrease_calculatorInput,
} from "@generated/percent-decrease-calculator";

describe("percent-decrease-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialValue": 1000,
    "finalValue": 800,
    "precision": 2,
    "adjustmentFactor": 1,
    "targetDecreasePercent": 10
  } as Percent_decrease_calculatorInput;
    const result = calculatePercent_decrease_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
