// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEnergy_savings_calculator,
  type Energy_savings_calculatorInput,
} from "@generated/energy-savings-calculator";

describe("energy-savings-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentEnergyUsage": 1000,
    "energyCostPerUnit": 0.12,
    "efficiencyGain": 15,
    "operatingHours": 720,
    "emissionFactor": 0.5
  } as unknown as Energy_savings_calculatorInput;
    const result = calculateEnergy_savings_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
