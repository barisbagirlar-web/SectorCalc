// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCredit_card_processing_fee_calculator,
  type Credit_card_processing_fee_calculatorInput,
} from "@generated/credit-card-processing-fee-calculator";

describe("credit-card-processing-fee-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "transactionAmount": 100,
    "processingRate": 2.9,
    "perTransactionFee": 0.3,
    "monthlyStatementFee": 10,
    "numberOfTransactions": 100
  } as Credit_card_processing_fee_calculatorInput;
    const result = calculateCredit_card_processing_fee_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
