// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBakers_percentage_calculator,
  type Bakers_percentage_calculatorInput,
} from "@generated/bakers-percentage-calculator";

describe("bakers-percentage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "flourWeight": 1000,
    "waterPercentage": 60,
    "yeastPercentage": 1,
    "saltPercentage": 2,
    "sugarPercentage": 0,
    "fatPercentage": 0
  } as Bakers_percentage_calculatorInput;
    const result = calculateBakers_percentage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
