// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDeferred_annuity_calculator,
  type Deferred_annuity_calculatorInput,
} from "@generated/deferred-annuity-calculator";

describe("deferred-annuity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "presentValue": 100000,
    "annualInterestRate": 5,
    "deferralYears": 10,
    "payoutYears": 20
  } as unknown as Deferred_annuity_calculatorInput;
    const result = calculateDeferred_annuity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
