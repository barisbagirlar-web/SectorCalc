// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInterest_coverage_ratio_calculator,
  type Interest_coverage_ratio_calculatorInput,
} from "@generated/interest-coverage-ratio-calculator";

describe("interest-coverage-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ebit": 100000,
    "interestExpense": 20000,
    "ebitda": 120000,
    "depreciation": 20000
  } as Interest_coverage_ratio_calculatorInput;
    const result = calculateInterest_coverage_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
