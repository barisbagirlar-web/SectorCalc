// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDecimal_to_fraction_calculator,
  type Decimal_to_fraction_calculatorInput,
} from "@generated/decimal-to-fraction-calculator";

describe("decimal-to-fraction-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "decimalValue": 0.5,
    "maxDenom": 10000,
    "simplify": 1,
    "format": 0,
    "rounding": 0,
    "tolerance": 1e-9
  } as unknown as Decimal_to_fraction_calculatorInput;
    const result = calculateDecimal_to_fraction_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
