// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRoth_vs_traditional_ira_calculator,
  type Roth_vs_traditional_ira_calculatorInput,
} from "@generated/roth-vs-traditional-ira-calculator";

describe("roth-vs-traditional-ira-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentAge": 30,
    "annualContribution": 6000,
    "currentTaxRate": 22,
    "expectedRetirementTaxRate": 15,
    "expectedAnnualReturn": 7,
    "yearsUntilRetirement": 30
  } as unknown as Roth_vs_traditional_ira_calculatorInput;
    const result = calculateRoth_vs_traditional_ira_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
