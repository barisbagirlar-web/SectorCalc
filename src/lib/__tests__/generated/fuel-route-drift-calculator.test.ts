// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFuel_route_drift_calculator,
  type Fuel_route_drift_calculatorInput,
} from "@generated/fuel-route-drift-calculator";

describe("fuel-route-drift-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "planned_distance_km": 100,
    "actual_distance_km": 110,
    "fuel_consumption_rate": 35,
    "fuel_price_per_liter": 1.5,
    "load_weight_tonnes": 20,
    "terrain_factor": "flat",
    "traffic_condition": "low",
    "driver_behavior_score": 80
  } as unknown as Fuel_route_drift_calculatorInput;
    const result = calculateFuel_route_drift_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
