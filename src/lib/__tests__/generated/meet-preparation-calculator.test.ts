// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMeet_preparation_calculator,
  type Meet_preparation_calculatorInput,
} from "@generated/meet-preparation-calculator";

describe("meet-preparation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "openerWeight": 100,
    "secondAttemptFactor": 105,
    "thirdAttemptFactor": 110,
    "warmUpStartPercent": 50,
    "warmUpEndPercent": 90,
    "warmUpStepPercent": 10
  } as unknown as Meet_preparation_calculatorInput;
    const result = calculateMeet_preparation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
