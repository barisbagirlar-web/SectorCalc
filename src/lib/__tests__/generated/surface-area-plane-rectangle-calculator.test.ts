// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSurface_area_plane_rectangle_calculator,
  type Surface_area_plane_rectangle_calculatorInput,
} from "@generated/surface-area-plane-rectangle-calculator";

describe("surface-area-plane-rectangle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "slopeX": 0,
    "slopeY": 0,
    "xMin": 0,
    "xMax": 1,
    "yMin": 0,
    "yMax": 1
  } as Surface_area_plane_rectangle_calculatorInput;
    const result = calculateSurface_area_plane_rectangle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
