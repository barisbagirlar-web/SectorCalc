// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDnd_damage_calculator,
  type Dnd_damage_calculatorInput,
} from "@generated/dnd-damage-calculator";

describe("dnd-damage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weaponDamage": 4.5,
    "abilityModifier": 3,
    "proficiencyBonus": 2,
    "magicBonus": 0,
    "criticalHitChance": 5,
    "extraDamageDice": 0,
    "damageMultiplier": 1
  } as unknown as Dnd_damage_calculatorInput;
    const result = calculateDnd_damage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
