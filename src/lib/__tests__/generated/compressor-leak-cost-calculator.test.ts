// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressor_leak_cost_calculator,
  type Compressor_leak_cost_calculatorInput,
} from "@generated/compressor-leak-cost-calculator";

describe("compressor-leak-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "system_pressure": 100,
    "leak_diameter": 0.125,
    "number_of_leaks": 10,
    "operating_hours": 8760,
    "electricity_cost": 0.1,
    "compressor_efficiency": 75,
    "leak_type": "Round orifice",
    "include_maintenance_cost": true
  } as Compressor_leak_cost_calculatorInput;
    const result = calculateCompressor_leak_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
