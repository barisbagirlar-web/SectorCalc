// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWedding_cake_serving_calculator,
  type Wedding_cake_serving_calculatorInput,
} from "@generated/wedding-cake-serving-calculator";

describe("wedding-cake-serving-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "shape": 0,
    "tier1Diameter": 6,
    "tier2Diameter": 0,
    "tier3Diameter": 0,
    "servingSize": 2,
    "guestCount": 100
  } as Wedding_cake_serving_calculatorInput;
    const result = calculateWedding_cake_serving_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
