// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCash_flow_gap_calculator,
  type Cash_flow_gap_calculatorInput,
} from "@generated/cash-flow-gap-calculator";

describe("cash-flow-gap-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avg_days_receivable": 45,
    "avg_days_payable": 30,
    "inventory_days": 60,
    "operating_expenses_daily": 10000,
    "revenue_daily": 15000,
    "payment_terms_suppliers": "net30",
    "customer_payment_behavior": "average",
    "lean_inventory_flag": false
  } as unknown as Cash_flow_gap_calculatorInput;
    const result = calculateCash_flow_gap_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
