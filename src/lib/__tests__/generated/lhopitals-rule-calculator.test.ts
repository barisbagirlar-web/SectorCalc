// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLhopitals_rule_calculator,
  type Lhopitals_rule_calculatorInput,
} from "@generated/lhopitals-rule-calculator";

describe("lhopitals-rule-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numerator_coeff": 1,
    "numerator_power": 2,
    "denominator_coeff": 1,
    "denominator_power": 1,
    "limit_point": 0,
    "iterations": 1
  } as Lhopitals_rule_calculatorInput;
    const result = calculateLhopitals_rule_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
