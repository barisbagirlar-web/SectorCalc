// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDebt_to_income_ratio_calculator,
  type Debt_to_income_ratio_calculatorInput,
} from "@generated/debt-to-income-ratio-calculator";

describe("debt-to-income-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mortgageMonth": 1,
    "creditCardMonth": 1,
    "autoLoanMonth": 1,
    "studentLoanMonth": 1,
    "otherDebtMonth": 1,
    "grossMonthlyIncome": 1
  } as unknown as Debt_to_income_ratio_calculatorInput;
    const result = calculateDebt_to_income_ratio_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
