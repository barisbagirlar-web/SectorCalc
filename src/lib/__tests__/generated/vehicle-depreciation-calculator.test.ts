// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVehicle_depreciation_calculator,
  type Vehicle_depreciation_calculatorInput,
} from "@generated/vehicle-depreciation-calculator";

describe("vehicle-depreciation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchase_price": 30000,
    "residual_value": 12000,
    "holding_period_years": 5,
    "annual_mileage": 12000,
    "depreciation_method": "straight_line",
    "maintenance_cost_per_year": 800,
    "fuel_efficiency_mpg": 25,
    "fuel_price_per_gallon": 3.5
  } as unknown as Vehicle_depreciation_calculatorInput;
    const result = calculateVehicle_depreciation_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
