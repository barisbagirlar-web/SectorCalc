// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHockey_skating_speed_calculator,
  type Hockey_skating_speed_calculatorInput,
} from "@generated/hockey-skating-speed-calculator";

describe("hockey-skating-speed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 100,
    "time": 10,
    "strideLength": 1.5,
    "strideFrequency": 2
  } as unknown as Hockey_skating_speed_calculatorInput;
    const result = calculateHockey_skating_speed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
