// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDegree_of_combined_leverage_calculator,
  type Degree_of_combined_leverage_calculatorInput,
} from "@generated/degree-of-combined-leverage-calculator";

describe("degree-of-combined-leverage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "salesRevenue": 100000,
    "variableCosts": 40000,
    "fixedCosts": 30000,
    "interestExpense": 10000
  } as unknown as Degree_of_combined_leverage_calculatorInput;
    const result = calculateDegree_of_combined_leverage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
