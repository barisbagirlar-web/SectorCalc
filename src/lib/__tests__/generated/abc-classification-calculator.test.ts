// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAbc_classification_calculator,
  type Abc_classification_calculatorInput,
} from "@generated/abc-classification-calculator";

describe("abc-classification-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualDemand": 1,
    "unitCost": 1,
    "totalAnnualValue": 1,
    "cumulativeBefore": 1
  } as unknown as Abc_classification_calculatorInput;
    const result = calculateAbc_classification_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
