// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFertilizer_dosage_calculator,
  type Fertilizer_dosage_calculatorInput,
} from "@generated/fertilizer-dosage-calculator";

describe("fertilizer-dosage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "crop_type": "corn",
    "target_yield": 8000,
    "soil_nitrogen": 15,
    "soil_phosphorus": 10,
    "soil_potassium": 120,
    "application_efficiency": 70,
    "fertilizer_type_n": "urea",
    "fertilizer_type_p": "dap"
  } as unknown as Fertilizer_dosage_calculatorInput;
    const result = calculateFertilizer_dosage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
