// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAtrial_fibrillation_risk_calculator,
  type Atrial_fibrillation_risk_calculatorInput,
} from "@generated/atrial-fibrillation-risk-calculator";

describe("atrial-fibrillation-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "congestiveHeartFailure": 1,
    "hypertension": 1,
    "age": 65,
    "diabetes": 1,
    "priorStrokeOrTIA": 1,
    "vascularDisease": 1,
    "sex": 1
  } as unknown as Atrial_fibrillation_risk_calculatorInput;
    const result = calculateAtrial_fibrillation_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
