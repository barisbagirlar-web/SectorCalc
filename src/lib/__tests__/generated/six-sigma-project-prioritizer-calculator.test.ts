// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSix_sigma_project_prioritizer_calculator,
  type Six_sigma_project_prioritizer_calculatorInput,
} from "@generated/six-sigma-project-prioritizer-calculator";

describe("six-sigma-project-prioritizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "defect_rate": 50000,
    "process_sigma": 3,
    "annual_volume": 100000,
    "cost_per_defect": 50,
    "implementation_cost": 50000,
    "project_risk": "medium",
    "strategic_alignment": 7,
    "customer_impact": 8
  } as Six_sigma_project_prioritizer_calculatorInput;
    const result = calculateSix_sigma_project_prioritizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
