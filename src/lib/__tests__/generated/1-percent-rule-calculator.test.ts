// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculate_1_percent_rule_calculator,
  type _1_percent_rule_calculatorInput,
} from "@generated/1-percent-rule-calculator";

describe("1-percent-rule-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 200000,
    "monthlyRent": 2000,
    "closingCosts": 5000,
    "repairCosts": 10000,
    "monthlyExpenses": 500
  } as unknown as _1_percent_rule_calculatorInput;
    const result = calculate_1_percent_rule_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
