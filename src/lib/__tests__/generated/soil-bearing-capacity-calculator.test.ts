// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSoil_bearing_capacity_calculator,
  type Soil_bearing_capacity_calculatorInput,
} from "@generated/soil-bearing-capacity-calculator";

describe("soil-bearing-capacity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cohesion": 1,
    "frictionAngle": 30,
    "unitWeight": 18,
    "foundationDepth": 1,
    "foundationWidth": 1.5,
    "safetyFactor": 3
  } as unknown as Soil_bearing_capacity_calculatorInput;
    const result = calculateSoil_bearing_capacity_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
