// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSeconds_to_milliseconds_calculator,
  type Seconds_to_milliseconds_calculatorInput,
} from "@generated/seconds-to-milliseconds-calculator";

describe("seconds-to-milliseconds-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "seconds": 1,
    "milliseconds": 1000,
    "minutes": 0.0166666667,
    "hours": 0.00027777778,
    "days": 0.000011574074
  } as unknown as Seconds_to_milliseconds_calculatorInput;
    const result = calculateSeconds_to_milliseconds_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
