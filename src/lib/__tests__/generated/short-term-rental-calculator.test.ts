// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateShort_term_rental_calculator,
  type Short_term_rental_calculatorInput,
} from "@generated/short-term-rental-calculator";

describe("short-term-rental-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nightlyRate": 0,
    "numberOfNights": 1,
    "cleaningFee": 0,
    "serviceFeePercent": 0,
    "occupancyTaxPercent": 0,
    "discountPercent": 0
  } as unknown as Short_term_rental_calculatorInput;
    const result = calculateShort_term_rental_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
