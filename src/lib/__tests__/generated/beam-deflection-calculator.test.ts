// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBeam_deflection_calculator,
  type Beam_deflection_calculatorInput,
} from "@generated/beam-deflection-calculator";

describe("beam-deflection-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "beam_length": 3,
    "load_type": "point_center",
    "point_load": 10,
    "udl_load": 5,
    "load_position": 1.5,
    "young_modulus": 200,
    "moment_of_inertia": 500,
    "yield_strength": 250
  } as Beam_deflection_calculatorInput;
    const result = calculateBeam_deflection_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
