// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLightweight_cost_savings_calculator,
  type Lightweight_cost_savings_calculatorInput,
} from "@generated/lightweight-cost-savings-calculator";

describe("lightweight-cost-savings-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "current_weight_kg": 10,
    "new_weight_kg": 8,
    "annual_volume_units": 100000,
    "material_cost_per_kg": 2.5,
    "shipping_cost_per_kg": 0.3,
    "waste_rate_percent": 5,
    "labor_hours_per_unit": 0.5,
    "labor_rate_per_hour": 20
  } as unknown as Lightweight_cost_savings_calculatorInput;
    const result = calculateLightweight_cost_savings_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
