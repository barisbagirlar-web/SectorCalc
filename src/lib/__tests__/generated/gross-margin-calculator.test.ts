// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGross_margin_calculator,
  type Gross_margin_calculatorInput,
} from "@generated/gross-margin-calculator";

describe("gross-margin-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sellingPricePerUnit": 100,
    "costPerUnit": 70,
    "unitsSold": 1000,
    "additionalVariableCostPerUnit": 5,
    "discountPercentage": 10
  } as unknown as Gross_margin_calculatorInput;
    const result = calculateGross_margin_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
