// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMoq_inventory_balance_calculator,
  type Moq_inventory_balance_calculatorInput,
} from "@generated/moq-inventory-balance-calculator";

describe("moq-inventory-balance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_demand": 10000,
    "ordering_cost": 50,
    "holding_cost_per_unit": 2.5,
    "unit_cost": 15,
    "moq": 500,
    "lead_time_days": 30,
    "demand_std_dev": 10,
    "service_level": 95
  } as unknown as Moq_inventory_balance_calculatorInput;
    const result = calculateMoq_inventory_balance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
