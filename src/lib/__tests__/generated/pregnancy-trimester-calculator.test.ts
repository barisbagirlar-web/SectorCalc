// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePregnancy_trimester_calculator,
  type Pregnancy_trimester_calculatorInput,
} from "@generated/pregnancy-trimester-calculator";

describe("pregnancy-trimester-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lmpDay": 1,
    "lmpMonth": 1,
    "lmpYear": 2024,
    "currentDay": 1,
    "currentMonth": 1,
    "currentYear": 2024
  } as unknown as Pregnancy_trimester_calculatorInput;
    const result = calculatePregnancy_trimester_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
