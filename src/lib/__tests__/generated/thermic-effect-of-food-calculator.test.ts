// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateThermic_effect_of_food_calculator,
  type Thermic_effect_of_food_calculatorInput,
} from "@generated/thermic-effect-of-food-calculator";

describe("thermic-effect-of-food-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "protein": 0,
    "carbohydrates": 0,
    "fat": 0,
    "alcohol": 0
  } as unknown as Thermic_effect_of_food_calculatorInput;
    const result = calculateThermic_effect_of_food_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
