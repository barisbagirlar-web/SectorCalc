// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMuda_waste_cost_calculator,
  type Muda_waste_cost_calculatorInput,
} from "@generated/muda-waste-cost-calculator";

describe("muda-waste-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_cost_per_unit": 0.5,
    "labor_cost_per_hour": 25,
    "overhead_rate": 150,
    "defect_rate": 5,
    "rework_time_per_unit": 0.25,
    "scrap_rate": 2,
    "waiting_time_per_unit": 0.1,
    "excess_motion_cost_per_unit": 0.05
  } as unknown as Muda_waste_cost_calculatorInput;
    const result = calculateMuda_waste_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
