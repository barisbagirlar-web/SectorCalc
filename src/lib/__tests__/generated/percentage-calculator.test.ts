// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePercentage_calculator,
  type Percentage_calculatorInput,
} from "@generated/percentage-calculator";

describe("percentage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_units": 1000,
    "defective_units": 50,
    "rework_units": 20,
    "scrap_units": 10,
    "target_yield": 99,
    "process_type": "manufacturing",
    "include_scrap_in_defect": true
  } as unknown as Percentage_calculatorInput;
    const result = calculatePercentage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
