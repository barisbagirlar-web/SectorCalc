// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBaccarat_calculator,
  type Baccarat_calculatorInput,
} from "@generated/baccarat-calculator";

describe("baccarat-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rawMaterialCost": 10,
    "laborCostPerHour": 50,
    "hoursPerUnit": 0.5,
    "energyCostPerUnit": 2,
    "additionalCost": 5,
    "overheadRate": 20,
    "profitMargin": 30
  } as unknown as Baccarat_calculatorInput;
    const result = calculateBaccarat_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
