// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFlexible_manufacturing_roi_calculator,
  type Flexible_manufacturing_roi_calculatorInput,
} from "@generated/flexible-manufacturing-roi-calculator";

describe("flexible-manufacturing-roi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_production_volume": 100000,
    "avg_batch_size": 500,
    "current_setup_time": 120,
    "target_setup_time": 15,
    "hourly_operating_cost": 85,
    "changeover_cost_per_hour": 150,
    "inventory_holding_cost_rate": 25,
    "avg_inventory_value": 2000000
  } as unknown as Flexible_manufacturing_roi_calculatorInput;
    const result = calculateFlexible_manufacturing_roi_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
