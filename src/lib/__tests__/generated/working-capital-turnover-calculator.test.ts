// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWorking_capital_turnover_calculator,
  type Working_capital_turnover_calculatorInput,
} from "@generated/working-capital-turnover-calculator";

describe("working-capital-turnover-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netSales": 0,
    "begCurrentAssets": 0,
    "endCurrentAssets": 0,
    "begCurrentLiabilities": 0,
    "endCurrentLiabilities": 0
  } as unknown as Working_capital_turnover_calculatorInput;
    const result = calculateWorking_capital_turnover_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
