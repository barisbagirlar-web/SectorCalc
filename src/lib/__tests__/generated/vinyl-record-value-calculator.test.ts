// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVinyl_record_value_calculator,
  type Vinyl_record_value_calculatorInput,
} from "@generated/vinyl-record-value-calculator";

describe("vinyl-record-value-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "basePrice": 20,
    "conditionScore": 7,
    "rarityScore": 5,
    "popularityScore": 5,
    "ageYears": 10
  } as unknown as Vinyl_record_value_calculatorInput;
    const result = calculateVinyl_record_value_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
