// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePipe_insulation_calculator,
  type Pipe_insulation_calculatorInput,
} from "@generated/pipe-insulation-calculator";

describe("pipe-insulation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pipe_outer_diameter": 100,
    "insulation_thickness": 50,
    "pipe_length": 10,
    "thermal_conductivity": 0.035,
    "inner_temperature": 150,
    "ambient_temperature": 25,
    "convection_coefficient": 10
  } as Pipe_insulation_calculatorInput;
    const result = calculatePipe_insulation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
