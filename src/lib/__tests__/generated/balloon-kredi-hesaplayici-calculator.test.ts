// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBalloon_kredi_hesaplayici_calculator,
  type Balloon_kredi_hesaplayici_calculatorInput,
} from "@generated/balloon-kredi-hesaplayici-calculator";

describe("balloon-kredi-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "principal": 100000,
    "annualInterestRate": 10,
    "termMonths": 60,
    "balloonPercent": 30
  } as Balloon_kredi_hesaplayici_calculatorInput;
    const result = calculateBalloon_kredi_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
