// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTriangular_prism_volume_calculator,
  type Triangular_prism_volume_calculatorInput,
} from "@generated/triangular-prism-volume-calculator";

describe("triangular-prism-volume-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sideA": 3,
    "sideB": 4,
    "sideC": 5,
    "prismHeight": 10
  } as unknown as Triangular_prism_volume_calculatorInput;
    const result = calculateTriangular_prism_volume_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
