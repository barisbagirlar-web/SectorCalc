// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateClosing_costs_calculator,
  type Closing_costs_calculatorInput,
} from "@generated/closing-costs-calculator";

describe("closing-costs-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "propertyPrice": 300000,
    "downPaymentRate": 20,
    "originationFeeRate": 1,
    "appraisalFee": 500,
    "titleInsurance": 1000,
    "recordingFees": 200
  } as unknown as Closing_costs_calculatorInput;
    const result = calculateClosing_costs_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
