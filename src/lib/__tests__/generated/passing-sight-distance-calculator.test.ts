// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePassing_sight_distance_calculator,
  type Passing_sight_distance_calculatorInput,
} from "@generated/passing-sight-distance-calculator";

describe("passing-sight-distance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialSpeed": 80,
    "targetSpeed": 100,
    "acceleration": 1.5,
    "timeConstant": 3,
    "perceptionReactionTime": 1.5,
    "clearanceDistance": 30,
    "opposingSpeed": 80
  } as unknown as Passing_sight_distance_calculatorInput;
    const result = calculatePassing_sight_distance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
