// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRv_loan_calculator,
  type Rv_loan_calculatorInput,
} from "@generated/rv-loan-calculator";

describe("rv-loan-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rvPrice": 50000,
    "downPayment": 10000,
    "tradeInValue": 1,
    "annualInterestRate": 5.5,
    "loanTerm": 60,
    "salesTaxRate": 8
  } as unknown as Rv_loan_calculatorInput;
    const result = calculateRv_loan_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
