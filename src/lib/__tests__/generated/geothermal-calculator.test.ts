// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGeothermal_calculator,
  type Geothermal_calculatorInput,
} from "@generated/geothermal-calculator";

describe("geothermal-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "reservoir_temp": 150,
    "ambient_temp": 20,
    "flow_rate": 50,
    "specific_heat": 4.2,
    "efficiency": 15,
    "capacity_factor": 90,
    "operating_hours": 8000
  } as unknown as Geothermal_calculatorInput;
    const result = calculateGeothermal_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
