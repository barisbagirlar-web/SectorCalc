// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWrestling_weight_class_calculator,
  type Wrestling_weight_class_calculatorInput,
} from "@generated/wrestling-weight-class-calculator";

describe("wrestling-weight-class-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "gender": 1,
    "weight": 70,
    "height": 170,
    "age": 25,
    "bodyFat": 15
  } as unknown as Wrestling_weight_class_calculatorInput;
    const result = calculateWrestling_weight_class_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
