// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFood_waste_margin_loss_calculator,
  type Food_waste_margin_loss_calculatorInput,
} from "@generated/food-waste-margin-loss-calculator";

describe("food-waste-margin-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_production_kg": 10000,
    "waste_kg": 500,
    "selling_price_per_kg": 5,
    "cost_per_kg": 3.5,
    "waste_disposal_cost_per_kg": 0.15,
    "rework_percentage": 2,
    "labor_hourly_rate": 25,
    "rework_hours_per_kg": 0.05
  } as unknown as Food_waste_margin_loss_calculatorInput;
    const result = calculateFood_waste_margin_loss_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
