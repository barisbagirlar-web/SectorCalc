// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSitting_height_ratio_calculator,
  type Sitting_height_ratio_calculatorInput,
} from "@generated/sitting-height-ratio-calculator";

describe("sitting-height-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sittingHeightCm": 90,
    "sittingHeightMm": 0,
    "standingHeightCm": 170,
    "standingHeightMm": 0
  } as unknown as Sitting_height_ratio_calculatorInput;
    const result = calculateSitting_height_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
