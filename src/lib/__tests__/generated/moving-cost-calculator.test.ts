// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMoving_cost_calculator,
  type Moving_cost_calculatorInput,
} from "@generated/moving-cost-calculator";

describe("moving-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 100,
    "weight": 1000,
    "laborHours": 8,
    "packingItems": 20,
    "ratePerKm": 0.5,
    "ratePerKg": 0.2,
    "hourlyRate": 25,
    "packingCostPerItem": 10
  } as unknown as Moving_cost_calculatorInput;
    const result = calculateMoving_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
