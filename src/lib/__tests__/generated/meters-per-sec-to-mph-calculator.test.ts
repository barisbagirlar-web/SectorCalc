// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMeters_per_sec_to_mph_calculator,
  type Meters_per_sec_to_mph_calculatorInput,
} from "@generated/meters-per-sec-to-mph-calculator";

describe("meters-per-sec-to-mph-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "speed_ms": 1,
    "factor_m_to_mi": 0.000621371,
    "factor_s_to_h": 3600,
    "decimal_places": 2
  } as unknown as Meters_per_sec_to_mph_calculatorInput;
    const result = calculateMeters_per_sec_to_mph_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
