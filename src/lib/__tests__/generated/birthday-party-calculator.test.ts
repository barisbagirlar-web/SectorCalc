// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBirthday_party_calculator,
  type Birthday_party_calculatorInput,
} from "@generated/birthday-party-calculator";

describe("birthday-party-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "guestCount": 20,
    "foodCostPerGuest": 15,
    "drinkCostPerGuest": 8,
    "decorationCost": 100,
    "entertainmentCost": 200,
    "venueRental": 300,
    "cakeCost": 50,
    "miscCost": 50
  } as unknown as Birthday_party_calculatorInput;
    const result = calculateBirthday_party_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
