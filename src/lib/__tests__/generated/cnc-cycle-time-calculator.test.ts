// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCnc_cycle_time_calculator,
  type Cnc_cycle_time_calculatorInput,
} from "@generated/cnc-cycle-time-calculator";

describe("cnc-cycle-time-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cutting_length": 100,
    "feed_rate": 500,
    "spindle_speed": 3000,
    "number_of_passes": 1,
    "rapid_traverse_distance": 50,
    "rapid_traverse_rate": 15000,
    "tool_change_time": 5,
    "number_of_tool_changes": 2
  } as Cnc_cycle_time_calculatorInput;
    const result = calculateCnc_cycle_time_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
