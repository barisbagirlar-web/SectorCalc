// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDemand_forecast_inventory_cost_calculator,
  type Demand_forecast_inventory_cost_calculatorInput,
} from "@generated/demand-forecast-inventory-cost-calculator";

describe("demand-forecast-inventory-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "historical_demand_mean": 1000,
    "demand_std_dev": 200,
    "lead_time_days": 14,
    "service_level": 0.95,
    "unit_cost": 50,
    "holding_cost_rate": 25,
    "ordering_cost": 150,
    "stockout_cost_per_unit": 200
  } as unknown as Demand_forecast_inventory_cost_calculatorInput;
    const result = calculateDemand_forecast_inventory_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
