// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePercent_error_calculator,
  type Percent_error_calculatorInput,
} from "@generated/percent-error-calculator";

describe("percent-error-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "measuredValue": 0,
    "trueValue": 100,
    "tolerancePercent": 5,
    "measurementUncertainty": 0.1,
    "coverageFactor": 2
  } as unknown as Percent_error_calculatorInput;
    const result = calculatePercent_error_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
