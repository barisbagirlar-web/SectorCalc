// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAnxiety_calculator,
  type Anxiety_calculatorInput,
} from "@generated/anxiety-calculator";

describe("anxiety-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "measurement_variance": 0.1,
    "operator_error_rate": 2,
    "machine_downtime": 0.05,
    "defect_rate": 1.5,
    "process_temperature_variance": 2.1,
    "noise_level": 75
  } as unknown as Anxiety_calculatorInput;
    const result = calculateAnxiety_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
