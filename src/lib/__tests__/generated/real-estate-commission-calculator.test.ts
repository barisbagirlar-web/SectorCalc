// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateReal_estate_commission_calculator,
  type Real_estate_commission_calculatorInput,
} from "@generated/real-estate-commission-calculator";

describe("real-estate-commission-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "salePrice": 300000,
    "totalCommissionRate": 5,
    "buyerAgentSplit": 50,
    "additionalFlatFee": 0,
    "vatRate": 20,
    "isVatApplicable": 1
  } as Real_estate_commission_calculatorInput;
    const result = calculateReal_estate_commission_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
