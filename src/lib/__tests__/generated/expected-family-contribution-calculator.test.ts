// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateExpected_family_contribution_calculator,
  type Expected_family_contribution_calculatorInput,
} from "@generated/expected-family-contribution-calculator";

describe("expected-family-contribution-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "parent_agi": 1,
    "parent_assets": 1,
    "student_income": 1,
    "student_assets": 1,
    "household_size": 4,
    "number_in_college": 1,
    "state_tax_rate": 5,
    "age_older_parent": 45
  } as unknown as Expected_family_contribution_calculatorInput;
    const result = calculateExpected_family_contribution_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
