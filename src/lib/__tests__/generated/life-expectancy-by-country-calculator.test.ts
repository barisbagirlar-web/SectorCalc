// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLife_expectancy_by_country_calculator,
  type Life_expectancy_by_country_calculatorInput,
} from "@generated/life-expectancy-by-country-calculator";

describe("life-expectancy-by-country-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentAge": 30,
    "gender": 0,
    "countryLifeExpectancy": 75,
    "healthFactor": 1
  } as Life_expectancy_by_country_calculatorInput;
    const result = calculateLife_expectancy_by_country_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
