// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWps_preheat_temperature_calculator,
  type Wps_preheat_temperature_calculatorInput,
} from "@generated/wps-preheat-temperature-calculator";

describe("wps-preheat-temperature-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_thickness": 25,
    "carbon_equivalent": 0.45,
    "hydrogen_level": "medium",
    "heat_input": 1.5,
    "joint_restraint": "moderate",
    "preheat_method": "electric"
  } as unknown as Wps_preheat_temperature_calculatorInput;
    const result = calculateWps_preheat_temperature_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
