// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateForex_pip_value_calculator,
  type Forex_pip_value_calculatorInput,
} from "@generated/forex-pip-value-calculator";

describe("forex-pip-value-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "trade_size_lots": 1,
    "contract_size": 100000,
    "pip_definition": 0.0001,
    "quote_to_account_rate": 1.2
  } as unknown as Forex_pip_value_calculatorInput;
    const result = calculateForex_pip_value_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
