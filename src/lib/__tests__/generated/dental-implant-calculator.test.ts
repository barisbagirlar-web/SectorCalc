// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDental_implant_calculator,
  type Dental_implant_calculatorInput,
} from "@generated/dental-implant-calculator";

describe("dental-implant-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "materialCost": 25,
    "machiningTime": 0.5,
    "machineRate": 120,
    "laborCost": 40,
    "overheadPercent": 25,
    "batchSize": 10,
    "setupTime": 1
  } as Dental_implant_calculatorInput;
    const result = calculateDental_implant_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
