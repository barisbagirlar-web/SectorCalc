// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAuto_shop_margin_leak_calculator,
  type Auto_shop_margin_leak_calculatorInput,
} from "@generated/auto-shop-margin-leak-calculator";

describe("auto-shop-margin-leak-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "labor_rate_charged": 120,
    "labor_rate_effective": 95,
    "total_labor_hours_sold": 1600,
    "parts_markup_percent": 40,
    "parts_markup_realized": 28,
    "total_parts_cost": 240000,
    "shop_supply_charge_per_ro": 15,
    "shop_supply_cost_per_ro": 22
  } as unknown as Auto_shop_margin_leak_calculatorInput;
    const result = calculateAuto_shop_margin_leak_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
