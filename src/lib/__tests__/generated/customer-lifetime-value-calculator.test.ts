// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCustomer_lifetime_value_calculator,
  type Customer_lifetime_value_calculatorInput,
} from "@generated/customer-lifetime-value-calculator";

describe("customer-lifetime-value-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avgPurchaseValue": 100,
    "purchaseFrequency": 12,
    "lifespanYears": 5,
    "grossMarginPercent": 100
  } as unknown as Customer_lifetime_value_calculatorInput;
    const result = calculateCustomer_lifetime_value_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
