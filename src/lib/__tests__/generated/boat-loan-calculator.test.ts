// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBoat_loan_calculator,
  type Boat_loan_calculatorInput,
} from "@generated/boat-loan-calculator";

describe("boat-loan-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "boatPrice": 50000,
    "downPayment": 10000,
    "tradeInValue": 1,
    "salesTaxRate": 6,
    "loanTerm": 5,
    "annualInterestRate": 5
  } as unknown as Boat_loan_calculatorInput;
    const result = calculateBoat_loan_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
