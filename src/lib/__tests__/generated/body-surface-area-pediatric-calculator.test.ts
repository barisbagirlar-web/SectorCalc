// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBody_surface_area_pediatric_calculator,
  type Body_surface_area_pediatric_calculatorInput,
} from "@generated/body-surface-area-pediatric-calculator";

describe("body-surface-area-pediatric-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weight": 10,
    "height": 100,
    "formula": 1,
    "adjustment": 1
  } as unknown as Body_surface_area_pediatric_calculatorInput;
    const result = calculateBody_surface_area_pediatric_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
