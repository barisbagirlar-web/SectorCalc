// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKibibytes_to_kilobytes_calculator,
  type Kibibytes_to_kilobytes_calculatorInput,
} from "@generated/kibibytes-to-kilobytes-calculator";

describe("kibibytes-to-kilobytes-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "kibibytes": 1,
    "decimalPlaces": 2,
    "redundancyFactor": 1,
    "overheadPercent": 0
  } as Kibibytes_to_kilobytes_calculatorInput;
    const result = calculateKibibytes_to_kilobytes_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
