// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKarvonen_heart_rate_calculator,
  type Karvonen_heart_rate_calculatorInput,
} from "@generated/karvonen-heart-rate-calculator";

describe("karvonen-heart-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 30,
    "restingHR": 60,
    "maxHR": 190,
    "intensityLow": 60,
    "intensityHigh": 70
  } as unknown as Karvonen_heart_rate_calculatorInput;
    const result = calculateKarvonen_heart_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
