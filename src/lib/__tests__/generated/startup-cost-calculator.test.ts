// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStartup_cost_calculator,
  type Startup_cost_calculatorInput,
} from "@generated/startup-cost-calculator";

describe("startup-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "equipmentCost": 0,
    "legalAndPermitCost": 0,
    "depositCost": 0,
    "initialInventoryCost": 0,
    "marketingLaunchCost": 0,
    "workingCapital": 0
  } as unknown as Startup_cost_calculatorInput;
    const result = calculateStartup_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
