// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInsan_hayati_degeri_hesaplayici,
  type Insan_hayati_degeri_hesaplayiciInput,
} from "@generated/insan-hayati-degeri-hesaplayici";

describe("insan-hayati-degeri-hesaplayici", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualIncome": 100000,
    "personalExpenseRate": 30,
    "currentAge": 35,
    "retirementAge": 65,
    "discountRate": 5,
    "incomeGrowthRate": 2
  } as Insan_hayati_degeri_hesaplayiciInput;
    const result = calculateInsan_hayati_degeri_hesaplayici(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
