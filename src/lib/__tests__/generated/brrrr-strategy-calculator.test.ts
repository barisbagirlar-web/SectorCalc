// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBrrrr_strategy_calculator,
  type Brrrr_strategy_calculatorInput,
} from "@generated/brrrr-strategy-calculator";

describe("brrrr-strategy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 100000,
    "rehabCost": 20000,
    "arv": 150000,
    "monthlyRent": 1200,
    "operatingExpensePercentage": 50,
    "refinanceLTV": 75,
    "refinanceRate": 4.5,
    "loanTerm": 30
  } as Brrrr_strategy_calculatorInput;
    const result = calculateBrrrr_strategy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
