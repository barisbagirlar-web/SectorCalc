// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCns_fatigue_calculator,
  type Cns_fatigue_calculatorInput,
} from "@generated/cns-fatigue-calculator";

describe("cns-fatigue-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bodyWeight": 70,
    "weightLifted": 100,
    "reps": 10,
    "sets": 5,
    "restBetweenSets": 2,
    "daysSinceLastSession": 1,
    "trainingExperience": 2
  } as unknown as Cns_fatigue_calculatorInput;
    const result = calculateCns_fatigue_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
