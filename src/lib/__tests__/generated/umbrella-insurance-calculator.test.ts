// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateUmbrella_insurance_calculator,
  type Umbrella_insurance_calculatorInput,
} from "@generated/umbrella-insurance-calculator";

describe("umbrella-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalAssets": 500000,
    "totalLiabilities": 200000,
    "underlyingCoverage": 300000,
    "riskFactor": 100
  } as Umbrella_insurance_calculatorInput;
    const result = calculateUmbrella_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
