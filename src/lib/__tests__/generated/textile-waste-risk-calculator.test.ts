// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTextile_waste_risk_calculator,
  type Textile_waste_risk_calculatorInput,
} from "@generated/textile-waste-risk-calculator";

describe("textile-waste-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "production_volume_meters": 100000,
    "fabric_type": "cotton",
    "waste_percentage": 8.5,
    "rework_rate": 3,
    "defect_density": 12,
    "energy_cost_per_kwh": 0.12,
    "labor_cost_per_hour": 15,
    "recycling_capability": "low"
  } as Textile_waste_risk_calculatorInput;
    const result = calculateTextile_waste_risk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
