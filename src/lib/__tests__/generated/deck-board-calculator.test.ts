// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDeck_board_calculator,
  type Deck_board_calculatorInput,
} from "@generated/deck-board-calculator";

describe("deck-board-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "deckLength": 12,
    "deckWidth": 10,
    "boardLength": 8,
    "boardWidth": 5.5,
    "gap": 0.125,
    "wasteFactor": 10
  } as unknown as Deck_board_calculatorInput;
    const result = calculateDeck_board_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
