// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePickling_calculator,
  type Pickling_calculatorInput,
} from "@generated/pickling-calculator";

describe("pickling-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "surfaceArea": 10,
    "scaleThickness": 50,
    "scaleDensity": 5000,
    "acidConcentration": 18,
    "acidDensity": 1.09,
    "stoichiometricRatio": 1.5,
    "dragOutRate": 1.5,
    "rinseWaterRatio": 10
  } as Pickling_calculatorInput;
    const result = calculatePickling_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
