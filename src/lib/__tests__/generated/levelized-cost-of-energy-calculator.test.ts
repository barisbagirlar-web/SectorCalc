// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLevelized_cost_of_energy_calculator,
  type Levelized_cost_of_energy_calculatorInput,
} from "@generated/levelized-cost-of-energy-calculator";

describe("levelized-cost-of-energy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "capitalCost": 1000000,
    "annualOMCost": 20000,
    "annualEnergyProduction": 5000000,
    "discountRate": 5,
    "projectLifetime": 20,
    "decommissioningCost": 0
  } as Levelized_cost_of_energy_calculatorInput;
    const result = calculateLevelized_cost_of_energy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
