// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMortgage_points_calculator,
  type Mortgage_points_calculatorInput,
} from "@generated/mortgage-points-calculator";

describe("mortgage-points-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loanAmount": 300000,
    "baseInterestRate": 6.5,
    "pointsPurchased": 2,
    "rateReductionPerPoint": 0.25,
    "loanTermYears": 30
  } as Mortgage_points_calculatorInput;
    const result = calculateMortgage_points_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
