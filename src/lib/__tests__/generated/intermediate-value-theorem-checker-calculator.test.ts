// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIntermediate_value_theorem_checker_calculator,
  type Intermediate_value_theorem_checker_calculatorInput,
} from "@generated/intermediate-value-theorem-checker-calculator";

describe("intermediate-value-theorem-checker-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "a": 0,
    "b": 1,
    "f_a": -1,
    "f_b": 1,
    "k": 0
  } as unknown as Intermediate_value_theorem_checker_calculatorInput;
    const result = calculateIntermediate_value_theorem_checker_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
