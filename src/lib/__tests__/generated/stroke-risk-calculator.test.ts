// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStroke_risk_calculator,
  type Stroke_risk_calculatorInput,
} from "@generated/stroke-risk-calculator";

describe("stroke-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 60,
    "systolicBP": 130,
    "diabetes": 0,
    "currentSmoker": 0,
    "atrialFibrillation": 0,
    "leftVentricularHypertrophy": 0,
    "antihypertensiveMed": 0
  } as unknown as Stroke_risk_calculatorInput;
    const result = calculateStroke_risk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
