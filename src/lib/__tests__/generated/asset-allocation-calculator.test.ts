// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAsset_allocation_calculator,
  type Asset_allocation_calculatorInput,
} from "@generated/asset-allocation-calculator";

describe("asset-allocation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalInvestment": 100000,
    "stockAllocation": 60,
    "bondAllocation": 30,
    "cashAllocation": 10,
    "stockReturn": 7,
    "bondReturn": 4,
    "cashReturn": 2
  } as Asset_allocation_calculatorInput;
    const result = calculateAsset_allocation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
