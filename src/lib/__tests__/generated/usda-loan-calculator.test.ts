// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateUsda_loan_calculator,
  type Usda_loan_calculatorInput,
} from "@generated/usda-loan-calculator";

describe("usda-loan-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "baseLoanAmount": 200000,
    "interestRate": 3.5,
    "loanTermYears": 30,
    "upfrontFeePercent": 1,
    "annualFeePercent": 0.35,
    "annualPropertyTax": 2400,
    "annualHomeInsurance": 1200
  } as unknown as Usda_loan_calculatorInput;
    const result = calculateUsda_loan_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
