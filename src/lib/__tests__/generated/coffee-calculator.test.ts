// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCoffee_calculator,
  type Coffee_calculatorInput,
} from "@generated/coffee-calculator";

describe("coffee-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "coffeeBeans": 15,
    "waterAmount": 250,
    "beanCostPerKg": 20,
    "electricityCostPerKwh": 0.15,
    "machinePowerWatts": 1000,
    "brewTimeMinutes": 3,
    "laborCostPerHour": 12,
    "overheadFactor": 15
  } as unknown as Coffee_calculatorInput;
    const result = calculateCoffee_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
