// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRecipe_cost_calculator,
  type Recipe_cost_calculatorInput,
} from "@generated/recipe-cost-calculator";

describe("recipe-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ingredientCostPerUnit": 5,
    "ingredientQuantity": 0.5,
    "numberOfPortions": 4,
    "laborCostPerHour": 15,
    "laborHours": 1,
    "overheadPercent": 10
  } as Recipe_cost_calculatorInput;
    const result = calculateRecipe_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
