// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDays_payable_outstanding_calculator,
  type Days_payable_outstanding_calculatorInput,
} from "@generated/days-payable-outstanding-calculator";

describe("days-payable-outstanding-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "apBegin": 80000,
    "apEnd": 100000,
    "cogs": 500000,
    "days": 365
  } as unknown as Days_payable_outstanding_calculatorInput;
    const result = calculateDays_payable_outstanding_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
