// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStokes_law_calculator,
  type Stokes_law_calculatorInput,
} from "@generated/stokes-law-calculator";

describe("stokes-law-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "particleDensity": 2500,
    "fluidDensity": 1000,
    "dynamicViscosity": 0.001,
    "particleDiameter": 0.001,
    "gravity": 9.81
  } as unknown as Stokes_law_calculatorInput;
    const result = calculateStokes_law_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
