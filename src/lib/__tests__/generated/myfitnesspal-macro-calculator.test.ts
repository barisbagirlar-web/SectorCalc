// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMyfitnesspal_macro_calculator,
  type Myfitnesspal_macro_calculatorInput,
} from "@generated/myfitnesspal-macro-calculator";

describe("myfitnesspal-macro-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weight": 70,
    "height": 170,
    "age": 30,
    "gender": 0,
    "activityLevel": 1.55,
    "goalFactor": 0.8
  } as unknown as Myfitnesspal_macro_calculatorInput;
    const result = calculateMyfitnesspal_macro_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
