// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKwh_cost_calculator,
  type Kwh_cost_calculatorInput,
} from "@generated/kwh-cost-calculator";

describe("kwh-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "energy_consumption_kwh": 10000,
    "peak_demand_kw": 500,
    "energy_rate_per_kwh": 0.12,
    "demand_rate_per_kw": 15,
    "power_factor": 0.85,
    "pf_penalty_threshold": 0.9,
    "pf_penalty_rate": 0.5,
    "system_efficiency": 95
  } as Kwh_cost_calculatorInput;
    const result = calculateKwh_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
