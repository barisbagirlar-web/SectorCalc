// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateChatter_surface_quality_loss_calculator,
  type Chatter_surface_quality_loss_calculatorInput,
} from "@generated/chatter-surface-quality-loss-calculator";

describe("chatter-surface-quality-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cutting_speed": 150,
    "feed_rate": 0.15,
    "depth_of_cut": 2,
    "tool_overhang": 80,
    "tool_diameter": 20,
    "workpiece_material_hardness": "200",
    "machine_spindle_power": 15,
    "machine_damping_ratio": 0.05
  } as Chatter_surface_quality_loss_calculatorInput;
    const result = calculateChatter_surface_quality_loss_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
