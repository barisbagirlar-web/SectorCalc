// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTop_speed_calculator,
  type Top_speed_calculatorInput,
} from "@generated/top-speed-calculator";

describe("top-speed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "enginePower": 100,
    "dragCoefficient": 0.3,
    "frontalArea": 2.2,
    "vehicleMass": 1500,
    "rollingResistanceCoefficient": 0.015,
    "airDensity": 1.225,
    "drivetrainEfficiency": 85
  } as unknown as Top_speed_calculatorInput;
    const result = calculateTop_speed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
