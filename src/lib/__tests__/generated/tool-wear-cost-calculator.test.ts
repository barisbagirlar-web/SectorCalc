// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTool_wear_cost_calculator,
  type Tool_wear_cost_calculatorInput,
} from "@generated/tool-wear-cost-calculator";

describe("tool-wear-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "tool_cost": 150,
    "tool_life_parts": 500,
    "replacement_time_min": 10,
    "machine_hourly_rate": 85,
    "rework_rate": 2,
    "rework_cost_per_part": 5,
    "scrap_rate": 0.5,
    "scrap_material_cost": 12
  } as unknown as Tool_wear_cost_calculatorInput;
    const result = calculateTool_wear_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
