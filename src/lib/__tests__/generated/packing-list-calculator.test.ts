// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePacking_list_calculator,
  type Packing_list_calculatorInput,
} from "@generated/packing-list-calculator";

describe("packing-list-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "quantityOrdered": 1000,
    "unitsPerCarton": 50,
    "cartonLength": 40,
    "cartonWidth": 30,
    "cartonHeight": 20,
    "cartonWeight": 12,
    "maxCartonsPerPallet": 24,
    "maxWeightPerPallet": 500
  } as unknown as Packing_list_calculatorInput;
    const result = calculatePacking_list_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
