// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDye_recipe_cost_optimizer_calculator,
  type Dye_recipe_cost_optimizer_calculatorInput,
} from "@generated/dye-recipe-cost-optimizer-calculator";

describe("dye-recipe-cost-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dye_volume_l": 1000,
    "dye_concentration_gpl": 5,
    "dye_price_per_kg": 25,
    "chemicals_per_liter": 0.15,
    "energy_cost_per_batch": 50,
    "labor_cost_per_hour": 25,
    "batch_time_hours": 4,
    "waste_percentage": 3
  } as Dye_recipe_cost_optimizer_calculatorInput;
    const result = calculateDye_recipe_cost_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
