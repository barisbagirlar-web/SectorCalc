// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateQuality_cost_paf_calculator,
  type Quality_cost_paf_calculatorInput,
} from "@generated/quality-cost-paf-calculator";

describe("quality-cost-paf-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_sales": 1000000,
    "prevention_training_cost": 15000,
    "prevention_design_cost": 25000,
    "appraisal_inspection_cost": 40000,
    "appraisal_audit_cost": 10000,
    "internal_failure_scrap_cost": 60000,
    "internal_failure_downtime_cost": 20000,
    "external_failure_warranty_cost": 80000
  } as Quality_cost_paf_calculatorInput;
    const result = calculateQuality_cost_paf_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
