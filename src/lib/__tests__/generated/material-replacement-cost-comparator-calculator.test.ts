// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMaterial_replacement_cost_comparator_calculator,
  type Material_replacement_cost_comparator_calculatorInput,
} from "@generated/material-replacement-cost-comparator-calculator";

describe("material-replacement-cost-comparator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "current_material_cost": 100,
    "alternative_material_cost": 85,
    "annual_volume": 10000,
    "scrap_rate_current": 5,
    "scrap_rate_alternative": 3,
    "logistics_cost_current": 10,
    "logistics_cost_alternative": 12,
    "changeover_cost": 5000
  } as Material_replacement_cost_comparator_calculatorInput;
    const result = calculateMaterial_replacement_cost_comparator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
