// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEnvironmental_waste_cost_calculator,
  type Environmental_waste_cost_calculatorInput,
} from "@generated/environmental-waste-cost-calculator";

describe("environmental-waste-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "waste_type": "mixed_solid",
    "waste_volume_kg": 1000,
    "disposal_cost_per_kg": 0.15,
    "transport_distance_km": 50,
    "transport_cost_per_km": 2.5,
    "labor_hours_per_ton": 2,
    "labor_rate_per_hour": 25,
    "recycling_revenue_per_kg": 0.05
  } as unknown as Environmental_waste_cost_calculatorInput;
    const result = calculateEnvironmental_waste_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
