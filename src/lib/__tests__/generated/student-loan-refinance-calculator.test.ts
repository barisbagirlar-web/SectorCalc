// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStudent_loan_refinance_calculator,
  type Student_loan_refinance_calculatorInput,
} from "@generated/student-loan-refinance-calculator";

describe("student-loan-refinance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentBalance": 30000,
    "currentRate": 6.8,
    "currentTermRemaining": 120,
    "newRate": 4.5,
    "newTerm": 120,
    "originationFee": 1
  } as unknown as Student_loan_refinance_calculatorInput;
    const result = calculateStudent_loan_refinance_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
