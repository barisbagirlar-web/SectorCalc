// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSquare_inches_to_sqcm_calculator,
  type Square_inches_to_sqcm_calculatorInput,
} from "@generated/square-inches-to-sqcm-calculator";

describe("square-inches-to-sqcm-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "squareInches": 1,
    "conversionFactor": 6.4516,
    "decimalPlaces": 4,
    "applyRounding": 1
  } as unknown as Square_inches_to_sqcm_calculatorInput;
    const result = calculateSquare_inches_to_sqcm_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
