// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSentence_count_calculator,
  type Sentence_count_calculatorInput,
} from "@generated/sentence-count-calculator";

describe("sentence-count-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalWords": 0,
    "avgWordsPerSentence": 15,
    "totalCharacters": 0,
    "avgCharsPerSentence": 100,
    "totalParagraphs": 0,
    "avgSentencesPerParagraph": 3,
    "lineCount": 0,
    "wordsPerLine": 10
  } as Sentence_count_calculatorInput;
    const result = calculateSentence_count_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
