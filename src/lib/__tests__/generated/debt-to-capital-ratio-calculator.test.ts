// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDebt_to_capital_ratio_calculator,
  type Debt_to_capital_ratio_calculatorInput,
} from "@generated/debt-to-capital-ratio-calculator";

describe("debt-to-capital-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalDebt": 0,
    "commonStock": 0,
    "preferredStock": 0,
    "retainedEarnings": 0,
    "otherEquity": 0
  } as unknown as Debt_to_capital_ratio_calculatorInput;
    const result = calculateDebt_to_capital_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
