// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCm_to_inch_converter_calculator,
  type Cm_to_inch_converter_calculatorInput,
} from "@generated/cm-to-inch-converter-calculator";

describe("cm-to-inch-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length_cm": 0,
    "measurement_uncertainty": 0.05,
    "conversion_precision": "standard",
    "tolerance_class": "general",
    "unit_cost_per_inch": 0.5,
    "batch_quantity": 1
  } as Cm_to_inch_converter_calculatorInput;
    const result = calculateCm_to_inch_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
