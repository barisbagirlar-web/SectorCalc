// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConsolidation_calculator,
  type Consolidation_calculatorInput,
} from "@generated/consolidation-calculator";

describe("consolidation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentTotalDebt": 100000,
    "currentMonthlyPayment": 2000,
    "currentRemainingMonths": 60,
    "newInterestRate": 15,
    "newTermMonths": 48,
    "consolidationFee": 500
  } as unknown as Consolidation_calculatorInput;
    const result = calculateConsolidation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
