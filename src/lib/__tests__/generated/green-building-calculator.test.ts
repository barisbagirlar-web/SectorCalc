// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGreen_building_calculator,
  type Green_building_calculatorInput,
} from "@generated/green-building-calculator";

describe("green-building-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "floorArea": 1000,
    "electricity": 50000,
    "gridFactor": 0.475,
    "gas": 10000,
    "gasFactor": 0.184,
    "renewable": 10000
  } as unknown as Green_building_calculatorInput;
    const result = calculateGreen_building_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
