// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNautical_miles_to_km_calculator,
  type Nautical_miles_to_km_calculatorInput,
} from "@generated/nautical-miles-to-km-calculator";

describe("nautical-miles-to-km-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nauticalMiles": 0,
    "conversionFactor": 1.852,
    "numberOfTrips": 1,
    "precision": 2
  } as Nautical_miles_to_km_calculatorInput;
    const result = calculateNautical_miles_to_km_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
