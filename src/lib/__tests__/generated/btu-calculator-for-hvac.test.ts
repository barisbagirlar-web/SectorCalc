// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBtu_calculator_for_hvac,
  type Btu_calculator_for_hvacInput,
} from "@generated/btu-calculator-for-hvac";

describe("btu-calculator-for-hvac", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "room_length": 10,
    "room_width": 10,
    "ceiling_height": 8,
    "insulation_factor": 1,
    "sun_exposure": 0,
    "num_occupants": 2,
    "num_windows": 1
  } as Btu_calculator_for_hvacInput;
    const result = calculateBtu_calculator_for_hvac(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
