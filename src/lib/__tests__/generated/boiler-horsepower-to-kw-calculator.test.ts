// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBoiler_horsepower_to_kw_calculator,
  type Boiler_horsepower_to_kw_calculatorInput,
} from "@generated/boiler-horsepower-to-kw-calculator";

describe("boiler-horsepower-to-kw-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "boilerHorsepower": 1,
    "numberOfBoilers": 1,
    "loadFactor": 100,
    "boilerEfficiency": 80,
    "operatingHours": 8000,
    "conversionFactor": 9.8095
  } as unknown as Boiler_horsepower_to_kw_calculatorInput;
    const result = calculateBoiler_horsepower_to_kw_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
