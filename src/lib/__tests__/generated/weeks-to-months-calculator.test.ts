// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWeeks_to_months_calculator,
  type Weeks_to_months_calculatorInput,
} from "@generated/weeks-to-months-calculator";

describe("weeks-to-months-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weeks": 4,
    "daysPerWeek": 7,
    "daysPerMonth": 30.44,
    "workDaysPerWeek": 5,
    "workDaysPerMonth": 22,
    "precision": 2
  } as unknown as Weeks_to_months_calculatorInput;
    const result = calculateWeeks_to_months_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
