// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateThermal_conductivity_calculator,
  type Thermal_conductivity_calculatorInput,
} from "@generated/thermal-conductivity-calculator";

describe("thermal-conductivity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_type": "generic_solid",
    "temperature_k": 300,
    "thickness_m": 0.05,
    "cross_section_area_m2": 1,
    "heat_flow_w": 100,
    "temperature_delta_k": 50,
    "moisture_content_pct": 0,
    "aging_factor": 1
  } as Thermal_conductivity_calculatorInput;
    const result = calculateThermal_conductivity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
