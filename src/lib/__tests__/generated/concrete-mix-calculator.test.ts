// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConcrete_mix_calculator,
  type Concrete_mix_calculatorInput,
} from "@generated/concrete-mix-calculator";

describe("concrete-mix-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "concreteVolume": 1,
    "cementRatio": 1,
    "sandRatio": 1.5,
    "aggregateRatio": 3,
    "waterCementRatio": 0.5,
    "dryFactor": 1.54,
    "wastagePercent": 5
  } as unknown as Concrete_mix_calculatorInput;
    const result = calculateConcrete_mix_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
