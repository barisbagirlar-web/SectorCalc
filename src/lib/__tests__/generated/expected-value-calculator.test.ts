// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateExpected_value_calculator,
  type Expected_value_calculatorInput,
} from "@generated/expected-value-calculator";

describe("expected-value-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "outcome1_probability": 0.25,
    "outcome1_value": 100,
    "outcome2_probability": 0.25,
    "outcome2_value": 200,
    "outcome3_probability": 0.25,
    "outcome3_value": 300,
    "outcome4_probability": 0.25,
    "outcome4_value": 400
  } as unknown as Expected_value_calculatorInput;
    const result = calculateExpected_value_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
