// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProject_deadline_calculator,
  type Project_deadline_calculatorInput,
} from "@generated/project-deadline-calculator";

describe("project-deadline-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalTasks": 100,
    "tasksPerDay": 5,
    "teamSize": 3,
    "complexityFactor": 1.2,
    "contingencyPercent": 15
  } as unknown as Project_deadline_calculatorInput;
    const result = calculateProject_deadline_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
