// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMegabytes_to_gigabytes_calculator,
  type Megabytes_to_gigabytes_calculatorInput,
} from "@generated/megabytes-to-gigabytes-calculator";

describe("megabytes-to-gigabytes-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "megabytes": 0,
    "conversionFactor": 1024,
    "decimalPlaces": 2,
    "batchSize": 1
  } as unknown as Megabytes_to_gigabytes_calculatorInput;
    const result = calculateMegabytes_to_gigabytes_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
