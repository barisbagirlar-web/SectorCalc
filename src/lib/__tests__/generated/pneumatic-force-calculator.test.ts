// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePneumatic_force_calculator,
  type Pneumatic_force_calculatorInput,
} from "@generated/pneumatic-force-calculator";

describe("pneumatic-force-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "systemPressure": 6,
    "boreDiameter": 50,
    "rodDiameter": 25,
    "cylinderCount": 1,
    "safetyFactor": 1.5,
    "efficiency": 0.9
  } as unknown as Pneumatic_force_calculatorInput;
    const result = calculatePneumatic_force_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
