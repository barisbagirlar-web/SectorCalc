// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFixed_charge_coverage_ratio_calculator,
  type Fixed_charge_coverage_ratio_calculatorInput,
} from "@generated/fixed-charge-coverage-ratio-calculator";

describe("fixed-charge-coverage-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ebitda": 0,
    "interestExpense": 0,
    "currentMaturities": 0,
    "leasePayments": 0
  } as Fixed_charge_coverage_ratio_calculatorInput;
    const result = calculateFixed_charge_coverage_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
