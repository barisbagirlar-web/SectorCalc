// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTravel_insurance_calculator,
  type Travel_insurance_calculatorInput,
} from "@generated/travel-insurance-calculator";

describe("travel-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "tripDurationDays": 7,
    "travelerAge": 35,
    "coverageAmount": 50000,
    "destinationRisk": 1,
    "deductible": 0,
    "numberOfTravelers": 1
  } as Travel_insurance_calculatorInput;
    const result = calculateTravel_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
