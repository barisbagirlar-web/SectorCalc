// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInterior_paint_calculator,
  type Interior_paint_calculatorInput,
} from "@generated/interior-paint-calculator";

describe("interior-paint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length": 4,
    "width": 3,
    "height": 2.5,
    "numberDoors": 1,
    "numberWindows": 1,
    "paintCoverage": 10,
    "coats": 2,
    "ceilingIncluded": 1
  } as unknown as Interior_paint_calculatorInput;
    const result = calculateInterior_paint_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
