// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKeplers_laws_calculator,
  type Keplers_laws_calculatorInput,
} from "@generated/keplers-laws-calculator";

describe("keplers-laws-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "semiMajorAxis": 1,
    "orbitalPeriod": 1,
    "eccentricity": 0,
    "massCentralBody": 1,
    "distanceAtPerihelion": 0.5,
    "distanceAtAphelion": 1.5
  } as Keplers_laws_calculatorInput;
    const result = calculateKeplers_laws_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
