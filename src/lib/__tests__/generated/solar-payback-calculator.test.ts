// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSolar_payback_calculator,
  type Solar_payback_calculatorInput,
} from "@generated/solar-payback-calculator";

describe("solar-payback-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "system_cost": 10000,
    "annual_production_kwh": 5000,
    "electricity_price_per_kwh": 0.12,
    "annual_maintenance_cost": 200,
    "tax_credit_percent": 26,
    "degradation_rate": 0.005,
    "escalation_rate": 0.03
  } as Solar_payback_calculatorInput;
    const result = calculateSolar_payback_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
