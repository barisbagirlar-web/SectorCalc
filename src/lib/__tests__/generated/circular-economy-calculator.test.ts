// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCircular_economy_calculator,
  type Circular_economy_calculatorInput,
} from "@generated/circular-economy-calculator";

describe("circular-economy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_material_input": 1000,
    "recycled_input": 200,
    "reused_input": 100,
    "total_waste_generated": 300,
    "recycled_waste": 150,
    "reused_waste": 50
  } as unknown as Circular_economy_calculatorInput;
    const result = calculateCircular_economy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
