// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLtt_calculator_wales,
  type Ltt_calculator_walesInput,
} from "@generated/ltt-calculator-wales";

describe("ltt-calculator-wales", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance_km": 100,
    "fuel_consumption_l_per_100km": 30,
    "fuel_price_gbp_per_l": 1.5,
    "driver_wage_gbp_per_hour": 15,
    "average_speed_kmh": 60,
    "load_tonnes": 20,
    "co2_emission_factor_kg_per_l": 2.68
  } as Ltt_calculator_walesInput;
    const result = calculateLtt_calculator_wales(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
