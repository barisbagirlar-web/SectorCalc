// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMorning_sickness_calculator,
  type Morning_sickness_calculatorInput,
} from "@generated/morning-sickness-calculator";

describe("morning-sickness-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "machine_age_years": 5,
    "downtime_hours": 12,
    "ambient_temperature_celsius": 20,
    "cold_starts_per_day": 1,
    "severity_factor": 1
  } as unknown as Morning_sickness_calculatorInput;
    const result = calculateMorning_sickness_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
