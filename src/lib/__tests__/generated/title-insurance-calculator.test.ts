// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTitle_insurance_calculator,
  type Title_insurance_calculatorInput,
} from "@generated/title-insurance-calculator";

describe("title-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "propertyValue": 300000,
    "coverageAmount": 300000,
    "ratePerThousand": 5,
    "policyFee": 150,
    "stateTaxRate": 0.5
  } as Title_insurance_calculatorInput;
    const result = calculateTitle_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
