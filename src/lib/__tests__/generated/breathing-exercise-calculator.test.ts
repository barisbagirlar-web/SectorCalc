// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBreathing_exercise_calculator,
  type Breathing_exercise_calculatorInput,
} from "@generated/breathing-exercise-calculator";

describe("breathing-exercise-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "breathingRate": 12,
    "tidalVolume": 0.5,
    "cylinderVolume": 6,
    "cylinderPressure": 200,
    "safetyFactor": 0.8,
    "ambientPressure": 1
  } as unknown as Breathing_exercise_calculatorInput;
    const result = calculateBreathing_exercise_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
