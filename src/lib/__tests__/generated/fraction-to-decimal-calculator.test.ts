// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFraction_to_decimal_calculator,
  type Fraction_to_decimal_calculatorInput,
} from "@generated/fraction-to-decimal-calculator";

describe("fraction-to-decimal-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numerator": 1,
    "denominator": 4,
    "wholeNumber": 0,
    "decimalPlaces": 6,
    "roundingMethod": "half_up",
    "uncertaintyInput": 0
  } as unknown as Fraction_to_decimal_calculatorInput;
    const result = calculateFraction_to_decimal_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
