// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBaby_growth_percentile_calculator,
  type Baby_growth_percentile_calculatorInput,
} from "@generated/baby-growth-percentile-calculator";

describe("baby-growth-percentile-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "measurement": 5,
    "L": 1,
    "M": 7,
    "S": 0.1,
    "measurementType": 1
  } as unknown as Baby_growth_percentile_calculatorInput;
    const result = calculateBaby_growth_percentile_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
