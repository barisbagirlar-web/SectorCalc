// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBusiness_valuation_calculator,
  type Business_valuation_calculatorInput,
} from "@generated/business-valuation-calculator";

describe("business-valuation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "revenue": 1000000,
    "expenses": 800000,
    "growthRate": 3,
    "discountRate": 10,
    "assets": 500000,
    "liabilities": 200000
  } as Business_valuation_calculatorInput;
    const result = calculateBusiness_valuation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
