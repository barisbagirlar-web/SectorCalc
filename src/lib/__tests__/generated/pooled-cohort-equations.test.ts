// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePooled_cohort_equations,
  type Pooled_cohort_equationsInput,
} from "@generated/pooled-cohort-equations";

describe("pooled-cohort-equations", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 55,
    "sex": 0,
    "totalCholesterol": 200,
    "hdlCholesterol": 50,
    "systolicBP": 120,
    "treatedHypertension": 0,
    "diabetes": 0,
    "smoker": 0
  } as unknown as Pooled_cohort_equationsInput;
    const result = calculatePooled_cohort_equations(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
