// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFreelance_rate_calculator,
  type Freelance_rate_calculatorInput,
} from "@generated/freelance-rate-calculator";

describe("freelance-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "desiredAnnualIncome": 100000,
    "overheadCosts": 20000,
    "billableHoursPerYear": 1500,
    "profitMarginPercent": 20,
    "taxRatePercent": 30
  } as unknown as Freelance_rate_calculatorInput;
    const result = calculateFreelance_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
