// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMass_flow_rate_orifice_calculator,
  type Mass_flow_rate_orifice_calculatorInput,
} from "@generated/mass-flow-rate-orifice-calculator";

describe("mass-flow-rate-orifice-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dischargeCoefficient": 0.62,
    "orificeArea": 0.01,
    "fluidDensity": 1000,
    "pressureDifference": 10000
  } as Mass_flow_rate_orifice_calculatorInput;
    const result = calculateMass_flow_rate_orifice_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
