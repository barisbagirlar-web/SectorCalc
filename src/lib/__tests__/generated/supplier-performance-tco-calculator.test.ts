// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSupplier_performance_tco_calculator,
  type Supplier_performance_tco_calculatorInput,
} from "@generated/supplier-performance-tco-calculator";

describe("supplier-performance-tco-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchase_price": 100,
    "annual_quantity": 10000,
    "defect_rate": 500,
    "inspection_cost_per_unit": 2,
    "freight_cost_per_unit": 5,
    "lead_time_days": 30,
    "carrying_cost_rate": 25,
    "warranty_claim_rate": 2
  } as unknown as Supplier_performance_tco_calculatorInput;
    const result = calculateSupplier_performance_tco_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
