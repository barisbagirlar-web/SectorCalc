// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConcrete_footing_calculator,
  type Concrete_footing_calculatorInput,
} from "@generated/concrete-footing-calculator";

describe("concrete-footing-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "FootingLength": 2,
    "FootingWidth": 2,
    "FootingDepth": 0.5,
    "NumberOfFootings": 1,
    "WasteFactor": 5
  } as unknown as Concrete_footing_calculatorInput;
    const result = calculateConcrete_footing_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
