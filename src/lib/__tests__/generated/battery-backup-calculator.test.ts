// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBattery_backup_calculator,
  type Battery_backup_calculatorInput,
} from "@generated/battery-backup-calculator";

describe("battery-backup-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loadPower": 100,
    "systemVoltage": 12,
    "batteryCapacityAh": 100,
    "depthOfDischarge": 50,
    "inverterEfficiency": 90,
    "safetyFactor": 1,
    "temperatureDerating": 1
  } as unknown as Battery_backup_calculatorInput;
    const result = calculateBattery_backup_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
