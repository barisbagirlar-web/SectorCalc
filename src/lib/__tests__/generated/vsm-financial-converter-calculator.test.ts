// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVsm_financial_converter_calculator,
  type Vsm_financial_converter_calculatorInput,
} from "@generated/vsm-financial-converter-calculator";

describe("vsm-financial-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualDemand": 100000,
    "sellingPrice": 50,
    "materialCostPerUnit": 20,
    "laborCostPerHour": 25,
    "totalCycleTime": 45,
    "totalValueAddedTime": 15,
    "defectRate": 2,
    "reworkCostPerUnit": 10
  } as unknown as Vsm_financial_converter_calculatorInput;
    const result = calculateVsm_financial_converter_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
