// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBody_fat_set_point_calculator,
  type Body_fat_set_point_calculatorInput,
} from "@generated/body-fat-set-point-calculator";

describe("body-fat-set-point-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentBodyFat": 20,
    "desiredBodyFat": 15,
    "currentWeight": 70,
    "height": 170,
    "age": 30,
    "activityLevel": 1.55
  } as unknown as Body_fat_set_point_calculatorInput;
    const result = calculateBody_fat_set_point_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
