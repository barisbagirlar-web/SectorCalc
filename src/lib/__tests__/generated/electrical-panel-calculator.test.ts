// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateElectrical_panel_calculator,
  type Electrical_panel_calculatorInput,
} from "@generated/electrical-panel-calculator";

describe("electrical-panel-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "voltage": 400,
    "phases": 3,
    "numberOfCircuits": 10,
    "loadPerCircuit": 20,
    "diversityFactor": 0.8,
    "powerFactor": 0.85
  } as unknown as Electrical_panel_calculatorInput;
    const result = calculateElectrical_panel_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
