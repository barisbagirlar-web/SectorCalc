// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSales_commission_calculator,
  type Sales_commission_calculatorInput,
} from "@generated/sales-commission-calculator";

describe("sales-commission-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalSales": 5000,
    "commissionRate": 5,
    "targetSales": 10000,
    "bonusRate": 2,
    "baseSalary": 2000
  } as unknown as Sales_commission_calculatorInput;
    const result = calculateSales_commission_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
