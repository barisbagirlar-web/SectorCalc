// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAdrenal_fatigue_calculator,
  type Adrenal_fatigue_calculatorInput,
} from "@generated/adrenal-fatigue-calculator";

describe("adrenal-fatigue-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "shift_length": 8,
    "consecutive_shifts": 5,
    "rest_between_shifts": 16,
    "noise_level": 70,
    "temperature": 22,
    "load_factor": 1
  } as Adrenal_fatigue_calculatorInput;
    const result = calculateAdrenal_fatigue_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
