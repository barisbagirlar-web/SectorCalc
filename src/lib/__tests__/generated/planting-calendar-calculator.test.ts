// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePlanting_calendar_calculator,
  type Planting_calendar_calculatorInput,
} from "@generated/planting-calendar-calculator";

describe("planting-calendar-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lastFrostDay": 120,
    "plantingDateOffset": 7,
    "baseTemperature": 10,
    "targetGDD": 1500,
    "averageDailyTemp": 20,
    "soilTemp": 14,
    "minSoilTemp": 10,
    "safetyMargin": 10
  } as unknown as Planting_calendar_calculatorInput;
    const result = calculatePlanting_calendar_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
