// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDelivery_cost_calculator,
  type Delivery_cost_calculatorInput,
} from "@generated/delivery-cost-calculator";

describe("delivery-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance_km": 100,
    "fuel_cost_per_liter": 1.5,
    "fuel_efficiency_kmpl": 8,
    "driver_wage_per_hour": 25,
    "average_speed_kmph": 60,
    "load_weight_kg": 1000,
    "handling_cost_per_kg": 0.1,
    "vehicle_maintenance_per_km": 0.05
  } as unknown as Delivery_cost_calculatorInput;
    const result = calculateDelivery_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
