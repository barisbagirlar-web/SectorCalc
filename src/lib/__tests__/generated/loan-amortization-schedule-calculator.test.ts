// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLoan_amortization_schedule_calculator,
  type Loan_amortization_schedule_calculatorInput,
} from "@generated/loan-amortization-schedule-calculator";

describe("loan-amortization-schedule-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loanAmount": 100000,
    "annualInterestRate": 5,
    "loanTermYears": 30,
    "paymentsPerYear": 12
  } as Loan_amortization_schedule_calculatorInput;
    const result = calculateLoan_amortization_schedule_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
