// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePrice_elasticity_simulator_calculator,
  type Price_elasticity_simulator_calculatorInput,
} from "@generated/price-elasticity-simulator-calculator";

describe("price-elasticity-simulator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "current_price": 100,
    "current_quantity": 1000,
    "price_change_percent": -10,
    "elasticity_coefficient": -1.5,
    "variable_cost_per_unit": 60,
    "fixed_cost_monthly": 20000,
    "demand_shift_factor": 1,
    "confidence_level": "medium"
  } as unknown as Price_elasticity_simulator_calculatorInput;
    const result = calculatePrice_elasticity_simulator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
