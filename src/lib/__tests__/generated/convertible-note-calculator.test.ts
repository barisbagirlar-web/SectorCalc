// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConvertible_note_calculator,
  type Convertible_note_calculatorInput,
} from "@generated/convertible-note-calculator";

describe("convertible-note-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "investmentAmount": 100000,
    "discountRate": 20,
    "valuationCap": 5000000,
    "nextRoundPricePerShare": 1,
    "interestRate": 5,
    "maturity": 2,
    "preMoneyFullyDilutedShares": 10000000
  } as Convertible_note_calculatorInput;
    const result = calculateConvertible_note_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
