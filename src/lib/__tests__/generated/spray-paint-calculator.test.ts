// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSpray_paint_calculator,
  type Spray_paint_calculatorInput,
} from "@generated/spray-paint-calculator";

describe("spray-paint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "area": 10,
    "coverage": 10,
    "coats": 1,
    "wasteFactor": 10,
    "canVolume": 0.4
  } as unknown as Spray_paint_calculatorInput;
    const result = calculateSpray_paint_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
