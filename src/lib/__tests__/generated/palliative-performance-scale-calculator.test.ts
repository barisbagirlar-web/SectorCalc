// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePalliative_performance_scale_calculator,
  type Palliative_performance_scale_calculatorInput,
} from "@generated/palliative-performance-scale-calculator";

describe("palliative-performance-scale-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ambulationScore": 100,
    "activityScore": 100,
    "selfCareScore": 100,
    "intakeScore": 100,
    "consciousnessScore": 100
  } as unknown as Palliative_performance_scale_calculatorInput;
    const result = calculatePalliative_performance_scale_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
