// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDrip_irrigation_calculator,
  type Drip_irrigation_calculatorInput,
} from "@generated/drip-irrigation-calculator";

describe("drip-irrigation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "area": 1000,
    "emitterFlowRate": 2,
    "emitterSpacing": 0.3,
    "lateralSpacing": 1,
    "irrigationDepth": 10,
    "systemEfficiency": 90
  } as unknown as Drip_irrigation_calculatorInput;
    const result = calculateDrip_irrigation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
