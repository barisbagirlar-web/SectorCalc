// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMsa_gage_rr_cost_calculator,
  type Msa_gage_rr_cost_calculatorInput,
} from "@generated/msa-gage-rr-cost-calculator";

describe("msa-gage-rr-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_parts": 10,
    "num_appraisers": 3,
    "num_trials": 3,
    "total_variation": 0.5,
    "repeatability_variation": 0.15,
    "reproducibility_variation": 0.1,
    "cost_per_defect": 50,
    "cost_per_escaped_defect": 500
  } as Msa_gage_rr_cost_calculatorInput;
    const result = calculateMsa_gage_rr_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
