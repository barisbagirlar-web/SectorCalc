// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProfit_margin_calculator,
  type Profit_margin_calculatorInput,
} from "@generated/profit-margin-calculator";

describe("profit-margin-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalRevenue": 100000,
    "costOfGoodsSold": 60000,
    "operatingExpenses": 20000,
    "otherExpenses": 5000,
    "taxExpense": 5000
  } as unknown as Profit_margin_calculatorInput;
    const result = calculateProfit_margin_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
