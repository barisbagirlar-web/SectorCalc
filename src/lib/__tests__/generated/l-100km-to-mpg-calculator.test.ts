// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateL_100km_to_mpg_calculator,
  type L_100km_to_mpg_calculatorInput,
} from "@generated/l-100km-to-mpg-calculator";

describe("l-100km-to-mpg-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "l_per_100km": 8,
    "gallon_type": 1,
    "decimal_places": 2,
    "rounding_method": 0,
    "measurement_standard": 1,
    "fuel_type": 1,
    "vehicle_class": 1,
    "route_type": 3
  } as L_100km_to_mpg_calculatorInput;
    const result = calculateL_100km_to_mpg_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
