// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePercentage_to_letter_grade_calculator,
  type Percentage_to_letter_grade_calculatorInput,
} from "@generated/percentage-to-letter-grade-calculator";

describe("percentage-to-letter-grade-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "percentage": 85,
    "aMin": 90,
    "bMin": 80,
    "cMin": 70,
    "dMin": 60
  } as Percentage_to_letter_grade_calculatorInput;
    const result = calculatePercentage_to_letter_grade_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
