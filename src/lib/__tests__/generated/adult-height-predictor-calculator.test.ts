// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAdult_height_predictor_calculator,
  type Adult_height_predictor_calculatorInput,
} from "@generated/adult-height-predictor-calculator";

describe("adult-height-predictor-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "childGender": 0,
    "fatherHeightCm": 175,
    "motherHeightCm": 165,
    "childHeightCm": 120,
    "childAgeYrs": 8
  } as Adult_height_predictor_calculatorInput;
    const result = calculateAdult_height_predictor_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
