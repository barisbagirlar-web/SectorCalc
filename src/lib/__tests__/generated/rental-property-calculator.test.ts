// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRental_property_calculator,
  type Rental_property_calculatorInput,
} from "@generated/rental-property-calculator";

describe("rental-property-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 200000,
    "downPaymentPercent": 20,
    "interestRate": 4,
    "loanTermYears": 25,
    "monthlyRent": 1500,
    "annualExpensePercent": 30,
    "vacancyRate": 5
  } as Rental_property_calculatorInput;
    const result = calculateRental_property_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
