// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStartup_valuation_calculator,
  type Startup_valuation_calculatorInput,
} from "@generated/startup-valuation-calculator";

describe("startup-valuation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "projectedRevenue": 10000000,
    "netProfitMargin": 15,
    "peRatio": 20,
    "requiredROI": 3,
    "investmentAmount": 1000000
  } as unknown as Startup_valuation_calculatorInput;
    const result = calculateStartup_valuation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
