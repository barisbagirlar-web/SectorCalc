// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBlood_pressure_category_calculator,
  type Blood_pressure_category_calculatorInput,
} from "@generated/blood-pressure-category-calculator";

describe("blood-pressure-category-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "systolic": 120,
    "diastolic": 80,
    "heart_rate": 70,
    "rest_duration": 5
  } as unknown as Blood_pressure_category_calculatorInput;
    const result = calculateBlood_pressure_category_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
