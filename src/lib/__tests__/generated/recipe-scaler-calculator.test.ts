// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRecipe_scaler_calculator,
  type Recipe_scaler_calculatorInput,
} from "@generated/recipe-scaler-calculator";

describe("recipe-scaler-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "originalServings": 4,
    "desiredServings": 8,
    "ingredient1Amount": 200,
    "ingredient2Amount": 150,
    "ingredient3Amount": 100,
    "ingredient4Amount": 50,
    "ingredient5Amount": 25,
    "ingredient6Amount": 10
  } as unknown as Recipe_scaler_calculatorInput;
    const result = calculateRecipe_scaler_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
