// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConfidence_interval_calculator,
  type Confidence_interval_calculatorInput,
} from "@generated/confidence-interval-calculator";

describe("confidence-interval-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sample_mean": 0,
    "standard_deviation": 1,
    "sample_size": 30,
    "critical_value": 1.96
  } as unknown as Confidence_interval_calculatorInput;
    const result = calculateConfidence_interval_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
