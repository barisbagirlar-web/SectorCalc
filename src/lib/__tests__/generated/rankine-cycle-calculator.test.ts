// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRankine_cycle_calculator,
  type Rankine_cycle_calculatorInput,
} from "@generated/rankine-cycle-calculator";

describe("rankine-cycle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "turbineInletEnthalpy": 2800,
    "turbineExitIsentropicEnthalpy": 2000,
    "condenserOutletEnthalpy": 200,
    "pumpExitIsentropicEnthalpy": 210,
    "turbineEfficiency": 0.9,
    "pumpEfficiency": 0.85
  } as unknown as Rankine_cycle_calculatorInput;
    const result = calculateRankine_cycle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
