// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConditional_probability_calculator,
  type Conditional_probability_calculatorInput,
} from "@generated/conditional-probability-calculator";

describe("conditional-probability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mode": 0,
    "pIntersection": 0.3,
    "pB": 0.5,
    "countBoth": 30,
    "countB": 50
  } as unknown as Conditional_probability_calculatorInput;
    const result = calculateConditional_probability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
