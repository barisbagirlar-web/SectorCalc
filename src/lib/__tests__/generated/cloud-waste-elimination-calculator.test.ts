// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCloud_waste_elimination_calculator,
  type Cloud_waste_elimination_calculatorInput,
} from "@generated/cloud-waste-elimination-calculator";

describe("cloud-waste-elimination-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_cloud_spend": 100000,
    "compute_utilization_rate": 45,
    "storage_waste_percentage": 20,
    "idle_resource_count": 15,
    "data_transfer_out_gb": 5000,
    "reserved_instance_coverage": 30,
    "rightsizing_opportunity_score": 60,
    "cloud_provider": "AWS"
  } as Cloud_waste_elimination_calculatorInput;
    const result = calculateCloud_waste_elimination_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
