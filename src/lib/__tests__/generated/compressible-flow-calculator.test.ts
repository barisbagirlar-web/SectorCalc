// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressible_flow_calculator,
  type Compressible_flow_calculatorInput,
} from "@generated/compressible-flow-calculator";

describe("compressible-flow-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "upstreamPressure": 5,
    "downstreamPressure": 1,
    "temperature": 293.15,
    "gasConstant": 287,
    "specificHeatRatio": 1.4,
    "orificeDiameter": 10,
    "dischargeCoefficient": 0.85
  } as unknown as Compressible_flow_calculatorInput;
    const result = calculateCompressible_flow_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
