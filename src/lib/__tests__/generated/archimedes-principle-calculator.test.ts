// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateArchimedes_principle_calculator,
  type Archimedes_principle_calculatorInput,
} from "@generated/archimedes-principle-calculator";

describe("archimedes-principle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fluidDensity": 1000,
    "objectVolume": 0.5,
    "objectMass": 500,
    "gravity": 9.81
  } as unknown as Archimedes_principle_calculatorInput;
    const result = calculateArchimedes_principle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
