// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWeight_watchers_points_calculator,
  type Weight_watchers_points_calculatorInput,
} from "@generated/weight-watchers-points-calculator";

describe("weight-watchers-points-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "calories": 0,
    "saturatedFat": 0,
    "sugar": 0,
    "protein": 0
  } as unknown as Weight_watchers_points_calculatorInput;
    const result = calculateWeight_watchers_points_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
