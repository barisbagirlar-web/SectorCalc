// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePint_to_liter_converter_calculator,
  type Pint_to_liter_converter_calculatorInput,
} from "@generated/pint-to-liter-converter-calculator";

describe("pint-to-liter-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pints": 0,
    "conversionType": 1,
    "batchId": 0,
    "temperature": 20
  } as Pint_to_liter_converter_calculatorInput;
    const result = calculatePint_to_liter_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
