// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHamilton_norwood_calculator,
  type Hamilton_norwood_calculatorInput,
} from "@generated/hamilton-norwood-calculator";

describe("hamilton-norwood-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "temporalRecession": 0,
    "vertexDiameter": 0,
    "frontalThinningArea": 0,
    "crownThinningArea": 0
  } as unknown as Hamilton_norwood_calculatorInput;
    const result = calculateHamilton_norwood_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
