// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRoute_optimization_analyzer_calculator,
  type Route_optimization_analyzer_calculatorInput,
} from "@generated/route-optimization-analyzer-calculator";

describe("route-optimization-analyzer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_stops": 10,
    "total_distance": 150,
    "avg_speed": 40,
    "dwell_time_per_stop": 10,
    "fuel_cost_per_km": 1.2,
    "driver_hourly_rate": 25,
    "vehicle_type": "van",
    "traffic_condition": "moderate"
  } as unknown as Route_optimization_analyzer_calculatorInput;
    const result = calculateRoute_optimization_analyzer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
