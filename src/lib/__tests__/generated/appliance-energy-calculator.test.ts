// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAppliance_energy_calculator,
  type Appliance_energy_calculatorInput,
} from "@generated/appliance-energy-calculator";

describe("appliance-energy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "powerRating": 1000,
    "usageHoursPerDay": 5,
    "daysPerMonth": 30,
    "electricityCost": 0.12,
    "applianceCount": 1,
    "standbyPower": 0
  } as unknown as Appliance_energy_calculatorInput;
    const result = calculateAppliance_energy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
