// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLean_bulk_calculator,
  type Lean_bulk_calculatorInput,
} from "@generated/lean-bulk-calculator";

describe("lean-bulk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dailyProduction": 1000,
    "materialPerUnit": 0.5,
    "containerCapacity": 25,
    "safetyStockDays": 2,
    "leadTimeDays": 5,
    "reorderPointMultiplier": 1.2
  } as unknown as Lean_bulk_calculatorInput;
    const result = calculateLean_bulk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
