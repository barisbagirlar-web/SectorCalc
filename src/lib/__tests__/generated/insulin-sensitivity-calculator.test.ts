// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInsulin_sensitivity_calculator,
  type Insulin_sensitivity_calculatorInput,
} from "@generated/insulin-sensitivity-calculator";

describe("insulin-sensitivity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rawMaterialCost": 10,
    "laborCost": 5,
    "energyCost": 2,
    "overheadCost": 3,
    "sensitivityRaw": 0.3,
    "sensitivityLabor": 0.2,
    "sensitivityEnergy": 0.1,
    "sensitivityOverhead": 0.15
  } as unknown as Insulin_sensitivity_calculatorInput;
    const result = calculateInsulin_sensitivity_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
