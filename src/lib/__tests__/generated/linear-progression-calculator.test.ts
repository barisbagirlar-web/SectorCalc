// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLinear_progression_calculator,
  type Linear_progression_calculatorInput,
} from "@generated/linear-progression-calculator";

describe("linear-progression-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "firstTerm": 0,
    "commonDifference": 0,
    "numberOfTerms": 1,
    "termPosition": 1,
    "decimals": 2
  } as Linear_progression_calculatorInput;
    const result = calculateLinear_progression_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
