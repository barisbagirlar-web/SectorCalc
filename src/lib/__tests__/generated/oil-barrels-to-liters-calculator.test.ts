// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOil_barrels_to_liters_calculator,
  type Oil_barrels_to_liters_calculatorInput,
} from "@generated/oil-barrels-to-liters-calculator";

describe("oil-barrels-to-liters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "barrels": 1,
    "observedTemperature": 20,
    "referenceTemperature": 15,
    "alpha": 0.0007,
    "precision": 2
  } as Oil_barrels_to_liters_calculatorInput;
    const result = calculateOil_barrels_to_liters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
