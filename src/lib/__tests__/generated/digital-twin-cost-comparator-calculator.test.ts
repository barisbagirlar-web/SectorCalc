// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDigital_twin_cost_comparator_calculator,
  type Digital_twin_cost_comparator_calculatorInput,
} from "@generated/digital-twin-cost-comparator-calculator";

describe("digital-twin-cost-comparator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "twin_scope": "asset",
    "asset_count": 100,
    "sensor_density": 5,
    "data_frequency": "1",
    "integration_complexity": "medium",
    "data_quality_index": 85,
    "labor_rate": 75,
    "expected_lifespan": 10
  } as unknown as Digital_twin_cost_comparator_calculatorInput;
    const result = calculateDigital_twin_cost_comparator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
