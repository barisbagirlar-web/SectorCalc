// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateContainer_load_calculator,
  type Container_load_calculatorInput,
} from "@generated/container-load-calculator";

describe("container-load-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "container_type": "20ft_standard",
    "cargo_length_cm": 120,
    "cargo_width_cm": 100,
    "cargo_height_cm": 150,
    "cargo_weight_kg": 500,
    "stacking_factor": 1,
    "load_secure_method": "blocking",
    "use_pallet": true
  } as unknown as Container_load_calculatorInput;
    const result = calculateContainer_load_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
