// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSocial_security_benefits_calculator,
  type Social_security_benefits_calculatorInput,
} from "@generated/social-security-benefits-calculator";

describe("social-security-benefits-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "monthlySalary": 5000,
    "employeeRate": 14,
    "employerRate": 20.5,
    "ceiling": 20000
  } as unknown as Social_security_benefits_calculatorInput;
    const result = calculateSocial_security_benefits_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
