// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInventory_turnover_risk_calculator,
  type Inventory_turnover_risk_calculatorInput,
} from "@generated/inventory-turnover-risk-calculator";

describe("inventory-turnover-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avg_inventory_value": 1000000,
    "cogs_annual": 5000000,
    "demand_variability_coefficient": 0.3,
    "obsolescence_rate": 5,
    "carrying_cost_rate": 25,
    "lead_time_days": 30,
    "service_level_target": 95,
    "inventory_accuracy_pct": 98
  } as Inventory_turnover_risk_calculatorInput;
    const result = calculateInventory_turnover_risk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
