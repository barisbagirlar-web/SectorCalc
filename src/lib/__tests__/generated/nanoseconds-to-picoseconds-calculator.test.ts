// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNanoseconds_to_picoseconds_calculator,
  type Nanoseconds_to_picoseconds_calculatorInput,
} from "@generated/nanoseconds-to-picoseconds-calculator";

describe("nanoseconds-to-picoseconds-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nanoseconds": 0,
    "conversionFactor": 1000,
    "decimalPlaces": 3,
    "scale": 1,
    "offset": 0,
    "scientificNotation": 0
  } as unknown as Nanoseconds_to_picoseconds_calculatorInput;
    const result = calculateNanoseconds_to_picoseconds_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
