// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateParsecs_to_light_years_calculator,
  type Parsecs_to_light_years_calculatorInput,
} from "@generated/parsecs-to-light-years-calculator";

describe("parsecs-to-light-years-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "parsecs": 0,
    "conversionFactor": 3.26156,
    "decimalPlaces": 2,
    "scientificNotation": 0
  } as unknown as Parsecs_to_light_years_calculatorInput;
    const result = calculateParsecs_to_light_years_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
