// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInterest_rate_risk_calculator,
  type Interest_rate_risk_calculatorInput,
} from "@generated/interest-rate-risk-calculator";

describe("interest-rate-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "current_rate": 5,
    "rate_change": 50,
    "portfolio_value": 10000000,
    "duration": 4.5,
    "convexity": 25,
    "hedge_ratio": 60,
    "confidence_level": "95%",
    "volatility_regime": "normal"
  } as unknown as Interest_rate_risk_calculatorInput;
    const result = calculateInterest_rate_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
