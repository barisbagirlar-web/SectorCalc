// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWarehouse_calculator,
  type Warehouse_calculatorInput,
} from "@generated/warehouse-calculator";

describe("warehouse-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalArea": 1000,
    "storageArea": 800,
    "maxPalletPositions": 500,
    "currentPallets": 300,
    "dailyThroughput": 100,
    "operatingDays": 250,
    "annualFixedCost": 100000,
    "variableCostPerPallet": 10
  } as unknown as Warehouse_calculatorInput;
    const result = calculateWarehouse_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
