// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePoka_yoke_roi_calculator,
  type Poka_yoke_roi_calculatorInput,
} from "@generated/poka-yoke-roi-calculator";

describe("poka-yoke-roi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_units": 100000,
    "current_defect_rate": 5000,
    "defect_cost_per_unit": 25,
    "poka_yoke_effectiveness": "95",
    "implementation_cost": 15000,
    "annual_maintenance_cost": 2000,
    "labor_hours_saved_per_year": 500,
    "labor_rate": 35
  } as unknown as Poka_yoke_roi_calculatorInput;
    const result = calculatePoka_yoke_roi_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
