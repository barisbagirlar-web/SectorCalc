// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHalf_life_reaction_calculator,
  type Half_life_reaction_calculatorInput,
} from "@generated/half-life-reaction-calculator";

describe("half-life-reaction-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialQuantity": 100,
    "tolerancePercent": 5,
    "halfLife": 10,
    "elapsedTime": 20
  } as unknown as Half_life_reaction_calculatorInput;
    const result = calculateHalf_life_reaction_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
