// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateShop_hourly_rate_calculator,
  type Shop_hourly_rate_calculatorInput,
} from "@generated/shop-hourly-rate-calculator";

describe("shop-hourly-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "direct_labor_hourly_rate": 25,
    "num_direct_labor_employees": 10,
    "annual_operating_hours_per_employee": 2080,
    "total_annual_overhead_costs": 500000,
    "total_annual_machine_costs": 200000,
    "average_shop_efficiency_percent": 85,
    "quality_yield_percent": 95,
    "shift_premium_factor": 1
  } as unknown as Shop_hourly_rate_calculatorInput;
    const result = calculateShop_hourly_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
