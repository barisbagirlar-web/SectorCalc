// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePassword_entropy_calculator,
  type Password_entropy_calculatorInput,
} from "@generated/password-entropy-calculator";

describe("password-entropy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length": 12,
    "uppercasePool": 26,
    "lowercasePool": 26,
    "digitsPool": 10,
    "symbolsPool": 32,
    "customChars": 0
  } as unknown as Password_entropy_calculatorInput;
    const result = calculatePassword_entropy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
