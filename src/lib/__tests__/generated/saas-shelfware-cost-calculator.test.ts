// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSaas_shelfware_cost_calculator,
  type Saas_shelfware_cost_calculatorInput,
} from "@generated/saas-shelfware-cost-calculator";

describe("saas-shelfware-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_licenses": 100,
    "active_users": 60,
    "license_cost_per_month": 50,
    "implementation_cost": 5000,
    "monthly_support_cost": 2000,
    "contract_months_remaining": 12,
    "utilization_threshold": 80
  } as unknown as Saas_shelfware_cost_calculatorInput;
    const result = calculateSaas_shelfware_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
