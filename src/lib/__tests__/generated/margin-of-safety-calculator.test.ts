// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMargin_of_safety_calculator,
  type Margin_of_safety_calculatorInput,
} from "@generated/margin-of-safety-calculator";

describe("margin-of-safety-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "materialUltimateStrength": 400,
    "crossSectionArea": 100,
    "appliedForce": 10000,
    "safetyFactor": 1.5
  } as unknown as Margin_of_safety_calculatorInput;
    const result = calculateMargin_of_safety_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
