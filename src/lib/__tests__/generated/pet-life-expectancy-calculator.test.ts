// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePet_life_expectancy_calculator,
  type Pet_life_expectancy_calculatorInput,
} from "@generated/pet-life-expectancy-calculator";

describe("pet-life-expectancy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "baseLifespan": 12,
    "weight": 10,
    "idealWeight": 10,
    "activityLevel": 5,
    "dietQuality": 7,
    "vetVisits": 2,
    "sterilized": 1
  } as unknown as Pet_life_expectancy_calculatorInput;
    const result = calculatePet_life_expectancy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
