// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRestaurant_menu_margin_leak_calculator,
  type Restaurant_menu_margin_leak_calculatorInput,
} from "@generated/restaurant-menu-margin-leak-calculator";

describe("restaurant-menu-margin-leak-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avg_cover_price": 18.5,
    "food_cost_per_cover": 6.5,
    "labor_cost_per_cover": 4.2,
    "overhead_per_cover": 2.8,
    "waste_percentage": 5,
    "theft_shrinkage_percentage": 1.5,
    "discount_comp_percentage": 3,
    "covers_per_period": 3000
  } as unknown as Restaurant_menu_margin_leak_calculatorInput;
    const result = calculateRestaurant_menu_margin_leak_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
