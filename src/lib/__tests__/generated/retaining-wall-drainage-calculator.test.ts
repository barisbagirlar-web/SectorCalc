// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRetaining_wall_drainage_calculator,
  type Retaining_wall_drainage_calculatorInput,
} from "@generated/retaining-wall-drainage-calculator";

describe("retaining-wall-drainage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wallHeight": 3,
    "wallLength": 10,
    "soilPermeability": 0.00001,
    "pipeDiameter": 100,
    "pipeSlope": 1,
    "manningN": 0.013
  } as unknown as Retaining_wall_drainage_calculatorInput;
    const result = calculateRetaining_wall_drainage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
