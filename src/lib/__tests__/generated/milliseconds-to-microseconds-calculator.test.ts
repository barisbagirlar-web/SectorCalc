// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMilliseconds_to_microseconds_calculator,
  type Milliseconds_to_microseconds_calculatorInput,
} from "@generated/milliseconds-to-microseconds-calculator";

describe("milliseconds-to-microseconds-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "milliseconds": 0,
    "calibrationOffset": 0,
    "safetyFactor": 1,
    "decimalPlaces": 2
  } as Milliseconds_to_microseconds_calculatorInput;
    const result = calculateMilliseconds_to_microseconds_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
