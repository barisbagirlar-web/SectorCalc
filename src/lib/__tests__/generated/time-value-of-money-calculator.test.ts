// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTime_value_of_money_calculator,
  type Time_value_of_money_calculatorInput,
} from "@generated/time-value-of-money-calculator";

describe("time-value-of-money-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "presentValue": 10000,
    "annualInterestRate": 5,
    "numberOfYears": 10,
    "compoundingFrequency": 1
  } as unknown as Time_value_of_money_calculatorInput;
    const result = calculateTime_value_of_money_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
