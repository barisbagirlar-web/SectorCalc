// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAi_compute_token_cost_calculator,
  type Ai_compute_token_cost_calculatorInput,
} from "@generated/ai-compute-token-cost-calculator";

describe("ai-compute-token-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "model_type": "gpt-4",
    "input_tokens_per_month": 1000000,
    "output_tokens_per_month": 500000,
    "batch_size": 1,
    "compute_hours_per_month": 720,
    "gpu_hourly_cost": 2.5,
    "overhead_factor": 15,
    "enable_data_confidence": true
  } as Ai_compute_token_cost_calculatorInput;
    const result = calculateAi_compute_token_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
