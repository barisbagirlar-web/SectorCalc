// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDelayed_onset_muscle_soreness_calculator,
  type Delayed_onset_muscle_soreness_calculatorInput,
} from "@generated/delayed-onset-muscle-soreness-calculator";

describe("delayed-onset-muscle-soreness-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 25,
    "fitnessLevel": 5,
    "exerciseIntensity": 5,
    "exerciseDuration": 60,
    "recoveryHours": 24
  } as Delayed_onset_muscle_soreness_calculatorInput;
    const result = calculateDelayed_onset_muscle_soreness_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
