// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCoefficient_of_restitution_calculator,
  type Coefficient_of_restitution_calculatorInput,
} from "@generated/coefficient-of-restitution-calculator";

describe("coefficient-of-restitution-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "v1i": 0,
    "v2i": 0,
    "v1f": 0,
    "v2f": 0
  } as Coefficient_of_restitution_calculatorInput;
    const result = calculateCoefficient_of_restitution_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
