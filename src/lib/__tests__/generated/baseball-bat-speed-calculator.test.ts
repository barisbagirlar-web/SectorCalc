// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBaseball_bat_speed_calculator,
  type Baseball_bat_speed_calculatorInput,
} from "@generated/baseball-bat-speed-calculator";

describe("baseball-bat-speed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "angularVelocity": 1000,
    "batLength": 84,
    "pivotDistance": 10,
    "sweetSpotOffset": 15,
    "batMass": 900
  } as unknown as Baseball_bat_speed_calculatorInput;
    const result = calculateBaseball_bat_speed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
