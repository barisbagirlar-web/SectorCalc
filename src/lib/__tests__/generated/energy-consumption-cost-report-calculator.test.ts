// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEnergy_consumption_cost_report_calculator,
  type Energy_consumption_cost_report_calculatorInput,
} from "@generated/energy-consumption-cost-report-calculator";

describe("energy-consumption-cost-report-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_energy_kwh": 100000,
    "peak_demand_kw": 500,
    "tariff_rate_per_kwh": 0.12,
    "demand_charge_per_kw": 10,
    "production_units": 50000,
    "facility_type": "manufacturing",
    "include_renewable_offset": false,
    "renewable_kwh": 1
  } as unknown as Energy_consumption_cost_report_calculatorInput;
    const result = calculateEnergy_consumption_cost_report_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
