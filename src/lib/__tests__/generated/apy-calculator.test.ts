// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateApy_calculator,
  type Apy_calculatorInput,
} from "@generated/apy-calculator";

describe("apy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nominal_rate": 5,
    "compounding_frequency": "12",
    "time_period_years": 1,
    "initial_principal": 10000,
    "fee_percentage": 0.5,
    "inflation_rate": 2,
    "tax_rate": 15,
    "is_continuous_compounding": false
  } as unknown as Apy_calculatorInput;
    const result = calculateApy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
