// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePhantom_load_calculator,
  type Phantom_load_calculatorInput,
} from "@generated/phantom-load-calculator";

describe("phantom-load-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "standbyWatt": 5,
    "numberOfDevices": 1,
    "standbyHoursPerDay": 24,
    "electricityCost": 1.5,
    "carbonFactor": 0.5,
    "operatingDaysPerYear": 365
  } as unknown as Phantom_load_calculatorInput;
    const result = calculatePhantom_load_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
