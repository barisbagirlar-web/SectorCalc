// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHorse_riding_calculator,
  type Horse_riding_calculatorInput,
} from "@generated/horse-riding-calculator";

describe("horse-riding-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lessonDuration": 1,
    "hourlyRate": 50,
    "horseMaintenancePerLesson": 20,
    "equipmentRental": 10,
    "insurancePerLesson": 5,
    "travelCost": 5,
    "numberOfLessons": 4
  } as unknown as Horse_riding_calculatorInput;
    const result = calculateHorse_riding_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
