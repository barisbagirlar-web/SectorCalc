// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHvac_capacity_optimizer_calculator,
  type Hvac_capacity_optimizer_calculatorInput,
} from "@generated/hvac-capacity-optimizer-calculator";

describe("hvac-capacity-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cooling_load": 100,
    "heating_load": 80,
    "system_type": "VAV",
    "ambient_temperature": 35,
    "relative_humidity": 50,
    "efficiency_ratio": 3.5,
    "airflow_rate": 2.5,
    "duct_leakage_factor": 10
  } as Hvac_capacity_optimizer_calculatorInput;
    const result = calculateHvac_capacity_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
