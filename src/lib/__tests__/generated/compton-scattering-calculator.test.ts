// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompton_scattering_calculator,
  type Compton_scattering_calculatorInput,
} from "@generated/compton-scattering-calculator";

describe("compton-scattering-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "incidentEnergy": 100,
    "incidentWavelength": 0,
    "scatteringAngle": 90,
    "electronRestEnergy": 511
  } as unknown as Compton_scattering_calculatorInput;
    const result = calculateCompton_scattering_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
