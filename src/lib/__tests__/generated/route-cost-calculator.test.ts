// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRoute_cost_calculator,
  type Route_cost_calculatorInput,
} from "@generated/route-cost-calculator";

describe("route-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance_km": 100,
    "fuel_consumption_l_per_100km": 30,
    "fuel_price_per_l": 1.5,
    "driver_wage_per_hour": 25,
    "average_speed_kmh": 60,
    "maintenance_cost_per_km": 0.12,
    "toll_cost_total": 15,
    "load_weight_tonnes": 20
  } as unknown as Route_cost_calculatorInput;
    const result = calculateRoute_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
