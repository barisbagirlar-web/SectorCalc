// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTransport_mode_cost_risk_calculator,
  type Transport_mode_cost_risk_calculatorInput,
} from "@generated/transport-mode-cost-risk-calculator";

describe("transport-mode-cost-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_volume": 10000,
    "avg_shipment_weight": 500,
    "distance_km": 800,
    "mode": "truck",
    "fuel_price_per_liter": 1.2,
    "labor_rate_per_hour": 25,
    "transit_time_days": 3,
    "damage_rate_percent": 1.5
  } as unknown as Transport_mode_cost_risk_calculatorInput;
    const result = calculateTransport_mode_cost_risk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
