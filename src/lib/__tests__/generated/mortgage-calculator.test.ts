// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMortgage_calculator,
  type Mortgage_calculatorInput,
} from "@generated/mortgage-calculator";

describe("mortgage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loan_amount": 300000,
    "down_payment_percent": 20,
    "annual_interest_rate": 6.5,
    "loan_term_years": 30,
    "property_tax_rate": 1.2,
    "insurance_rate": 0.5,
    "monthly_hoa": 0,
    "annual_income": 120000
  } as unknown as Mortgage_calculatorInput;
    const result = calculateMortgage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
