// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLife_insurance_needs_calculator,
  type Life_insurance_needs_calculatorInput,
} from "@generated/life-insurance-needs-calculator";

describe("life-insurance-needs-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_income": 50000,
    "years_needed": 20,
    "current_savings": 10000,
    "outstanding_debts": 150000,
    "education_expenses": 50000,
    "final_expenses": 20000
  } as unknown as Life_insurance_needs_calculatorInput;
    const result = calculateLife_insurance_needs_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
