// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateExterior_paint_calculator,
  type Exterior_paint_calculatorInput,
} from "@generated/exterior-paint-calculator";

describe("exterior-paint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalWallArea": 100,
    "numberOfCoats": 2,
    "paintCoverage": 10,
    "wasteFactor": 10,
    "doorArea": 2,
    "windowArea": 5,
    "otherOpeningsArea": 0
  } as unknown as Exterior_paint_calculatorInput;
    const result = calculateExterior_paint_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
