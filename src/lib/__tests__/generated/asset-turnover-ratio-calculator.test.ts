// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAsset_turnover_ratio_calculator,
  type Asset_turnover_ratio_calculatorInput,
} from "@generated/asset-turnover-ratio-calculator";

describe("asset-turnover-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netSales": 1000000,
    "totalAssetsBeginning": 500000,
    "totalAssetsEnding": 600000,
    "industryAverage": 1.5
  } as unknown as Asset_turnover_ratio_calculatorInput;
    const result = calculateAsset_turnover_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
