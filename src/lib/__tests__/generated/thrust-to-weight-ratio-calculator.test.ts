// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateThrust_to_weight_ratio_calculator,
  type Thrust_to_weight_ratio_calculatorInput,
} from "@generated/thrust-to-weight-ratio-calculator";

describe("thrust-to-weight-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "emptyMass": 5000,
    "fuelMass": 3000,
    "payloadMass": 2000,
    "thrust": 120000,
    "gravity": 9.81
  } as unknown as Thrust_to_weight_ratio_calculatorInput;
    const result = calculateThrust_to_weight_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
