// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTime_study_analyzer_calculator,
  type Time_study_analyzer_calculatorInput,
} from "@generated/time-study-analyzer-calculator";

describe("time-study-analyzer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "observed_time": 60,
    "performance_rating": 100,
    "allowance_percentage": 15,
    "frequency_per_cycle": 1,
    "work_measurement_method": "Stopwatch",
    "include_learning_curve": false
  } as unknown as Time_study_analyzer_calculatorInput;
    const result = calculateTime_study_analyzer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
