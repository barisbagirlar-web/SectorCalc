// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStandby_power_calculator,
  type Standby_power_calculatorInput,
} from "@generated/standby-power-calculator";

describe("standby-power-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "deviceCount": 1,
    "standbyPower": 5,
    "hoursPerDay": 20,
    "daysPerMonth": 30,
    "electricityCost": 0.15,
    "co2Factor": 0.5
  } as unknown as Standby_power_calculatorInput;
    const result = calculateStandby_power_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
