// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOffice_supplies_cost_calculator,
  type Office_supplies_cost_calculatorInput,
} from "@generated/office-supplies-cost-calculator";

describe("office-supplies-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_usage_units": 1000,
    "unit_cost": 5,
    "order_cost": 25,
    "holding_cost_rate": 20,
    "waste_percentage": 5,
    "order_frequency": "monthly",
    "use_lean_inventory": false
  } as Office_supplies_cost_calculatorInput;
    const result = calculateOffice_supplies_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
