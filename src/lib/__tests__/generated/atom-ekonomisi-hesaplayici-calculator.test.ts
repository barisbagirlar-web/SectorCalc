// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAtom_ekonomisi_hesaplayici_calculator,
  type Atom_ekonomisi_hesaplayici_calculatorInput,
} from "@generated/atom-ekonomisi-hesaplayici-calculator";

describe("atom-ekonomisi-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "productMW": 100,
    "reactantMW1": 50,
    "reactantMW2": 50,
    "reactantMW3": 0,
    "reactantMW4": 0,
    "reactantMW5": 0
  } as unknown as Atom_ekonomisi_hesaplayici_calculatorInput;
    const result = calculateAtom_ekonomisi_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
