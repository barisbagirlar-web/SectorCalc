// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCarbon_footprint_calculator,
  type Carbon_footprint_calculatorInput,
} from "@generated/carbon-footprint-calculator";

describe("carbon-footprint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "electricity_kwh": 1000,
    "natural_gas_m3": 500,
    "fuel_liters": 2000,
    "waste_kg": 100,
    "water_m3": 50
  } as unknown as Carbon_footprint_calculatorInput;
    const result = calculateCarbon_footprint_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
