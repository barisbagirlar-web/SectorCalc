// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHyperfocal_distance_calculator,
  type Hyperfocal_distance_calculatorInput,
} from "@generated/hyperfocal-distance-calculator";

describe("hyperfocal-distance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "focalLength": 50,
    "aperture": 8,
    "circleOfConfusion": 0.03,
    "outputUnitMultiplier": 1
  } as Hyperfocal_distance_calculatorInput;
    const result = calculateHyperfocal_distance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
