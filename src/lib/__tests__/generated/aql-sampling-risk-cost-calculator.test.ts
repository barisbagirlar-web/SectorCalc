// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAql_sampling_risk_cost_calculator,
  type Aql_sampling_risk_cost_calculatorInput,
} from "@generated/aql-sampling-risk-cost-calculator";

describe("aql-sampling-risk-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lot_size": 1000,
    "aql_percent": 1,
    "inspection_level": "II",
    "sampling_plan_type": "normal",
    "unit_cost": 10,
    "defect_cost_per_unit": 50,
    "inspection_cost_per_unit": 2,
    "defect_rate_actual": 2
  } as unknown as Aql_sampling_risk_cost_calculatorInput;
    const result = calculateAql_sampling_risk_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
