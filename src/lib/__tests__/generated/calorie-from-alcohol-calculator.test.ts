// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalorie_from_alcohol_calculator,
  type Calorie_from_alcohol_calculatorInput,
} from "@generated/calorie-from-alcohol-calculator";

describe("calorie-from-alcohol-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "volumePerDrink": 355,
    "abv": 5,
    "numberOfDrinks": 1,
    "density": 0.8,
    "kcalPerGram": 7
  } as unknown as Calorie_from_alcohol_calculatorInput;
    const result = calculateCalorie_from_alcohol_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
