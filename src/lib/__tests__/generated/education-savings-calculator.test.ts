// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEducation_savings_calculator,
  type Education_savings_calculatorInput,
} from "@generated/education-savings-calculator";

describe("education-savings-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "childAge": 5,
    "yearsUntilCollege": 13,
    "annualCost": 30000,
    "inflationRate": 5,
    "returnRate": 7,
    "currentSavings": 5000,
    "monthlyContribution": 200,
    "durationOfStudy": 4
  } as unknown as Education_savings_calculatorInput;
    const result = calculateEducation_savings_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
