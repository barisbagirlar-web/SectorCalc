// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEarth_overshoot_day_calculator,
  type Earth_overshoot_day_calculatorInput,
} from "@generated/earth-overshoot-day-calculator";

describe("earth-overshoot-day-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "globalPopulation": 7.9,
    "perCapitaBiocapacity": 1.6,
    "perCapitaFootprint": 2.7,
    "year": 2025
  } as Earth_overshoot_day_calculatorInput;
    const result = calculateEarth_overshoot_day_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
