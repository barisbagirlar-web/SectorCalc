// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRestaurant_markup_calculator,
  type Restaurant_markup_calculatorInput,
} from "@generated/restaurant-markup-calculator";

describe("restaurant-markup-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ingredientCost": 4.5,
    "laborCost": 2,
    "overheadCost": 1,
    "wastePercent": 3,
    "desiredMarkupPercent": 300
  } as unknown as Restaurant_markup_calculatorInput;
    const result = calculateRestaurant_markup_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
