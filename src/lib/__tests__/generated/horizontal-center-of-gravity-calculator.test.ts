// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHorizontal_center_of_gravity_calculator,
  type Horizontal_center_of_gravity_calculatorInput,
} from "@generated/horizontal-center-of-gravity-calculator";

describe("horizontal-center-of-gravity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weight1": 1000,
    "distance1": 0.5,
    "weight2": 800,
    "distance2": 1.2,
    "weight3": 600,
    "distance3": 2,
    "referencePoint": 0
  } as unknown as Horizontal_center_of_gravity_calculatorInput;
    const result = calculateHorizontal_center_of_gravity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
