// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalorie_deficit_calculator,
  type Calorie_deficit_calculatorInput,
} from "@generated/calorie-deficit-calculator";

describe("calorie-deficit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 30,
    "gender": "male",
    "weight": 70,
    "height": 170,
    "activityLevel": "moderate",
    "goalRate": "moderate",
    "bodyFatPercent": 20,
    "isPregnant": false
  } as Calorie_deficit_calculatorInput;
    const result = calculateCalorie_deficit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
