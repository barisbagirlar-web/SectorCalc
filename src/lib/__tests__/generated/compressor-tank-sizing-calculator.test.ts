// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressor_tank_sizing_calculator,
  type Compressor_tank_sizing_calculatorInput,
} from "@generated/compressor-tank-sizing-calculator";

describe("compressor-tank-sizing-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "compressorFlowRate": 100,
    "demandFlowRate": 80,
    "peakDemandFlowRate": 150,
    "peakDuration": 30,
    "systemPressure": 100,
    "pressureDifferential": 10,
    "compressorControlType": "Load/Unload",
    "ambientTemperature": 80
  } as unknown as Compressor_tank_sizing_calculatorInput;
    const result = calculateCompressor_tank_sizing_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
