// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBrick_wall_calculator,
  type Brick_wall_calculatorInput,
} from "@generated/brick-wall-calculator";

describe("brick-wall-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wallLength": 10,
    "wallHeight": 3,
    "brickLength": 20,
    "brickHeight": 10,
    "brickWidth": 10,
    "mortarJointThickness": 1,
    "brickPrice": 0.5,
    "mortarPrice": 100
  } as Brick_wall_calculatorInput;
    const result = calculateBrick_wall_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
