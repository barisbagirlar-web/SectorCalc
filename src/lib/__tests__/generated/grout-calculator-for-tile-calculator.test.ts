// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGrout_calculator_for_tile_calculator,
  type Grout_calculator_for_tile_calculatorInput,
} from "@generated/grout-calculator-for-tile-calculator";

describe("grout-calculator-for-tile-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "tileLength": 300,
    "tileWidth": 300,
    "jointWidth": 3,
    "tileThickness": 10,
    "area": 10,
    "wasteFactor": 1.2,
    "groutDensity": 1.8
  } as unknown as Grout_calculator_for_tile_calculatorInput;
    const result = calculateGrout_calculator_for_tile_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
