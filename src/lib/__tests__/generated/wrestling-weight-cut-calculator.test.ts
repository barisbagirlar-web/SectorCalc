// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWrestling_weight_cut_calculator,
  type Wrestling_weight_cut_calculatorInput,
} from "@generated/wrestling-weight-cut-calculator";

describe("wrestling-weight-cut-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentWeight": 80,
    "targetWeight": 75,
    "hoursUntilWeighIn": 24,
    "bodyFatPercentage": 15,
    "maxDehydrationPercentage": 5
  } as unknown as Wrestling_weight_cut_calculatorInput;
    const result = calculateWrestling_weight_cut_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
