// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateQuantum_tunneling_calculator,
  type Quantum_tunneling_calculatorInput,
} from "@generated/quantum-tunneling-calculator";

describe("quantum-tunneling-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "eff_mass_ratio": 1,
    "barrier_height": 1,
    "particle_energy": 0.5,
    "barrier_width": 0.5
  } as unknown as Quantum_tunneling_calculatorInput;
    const result = calculateQuantum_tunneling_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
