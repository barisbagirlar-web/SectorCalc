// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKitten_weight_predictor_calculator,
  type Kitten_weight_predictor_calculatorInput,
} from "@generated/kitten-weight-predictor-calculator";

describe("kitten-weight-predictor-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ageInWeeks": 12,
    "currentWeight": 1000,
    "birthWeight": 100,
    "breedSizeFactor": 1,
    "sexFactor": 1,
    "dailyCalories": 250
  } as unknown as Kitten_weight_predictor_calculatorInput;
    const result = calculateKitten_weight_predictor_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
