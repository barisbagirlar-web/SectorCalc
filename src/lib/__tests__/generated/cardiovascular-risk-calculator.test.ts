// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCardiovascular_risk_calculator,
  type Cardiovascular_risk_calculatorInput,
} from "@generated/cardiovascular-risk-calculator";

describe("cardiovascular-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 50,
    "gender": 1,
    "totalCholesterol": 200,
    "hdl": 50,
    "systolicBP": 120,
    "smoking": 0,
    "diabetes": 0
  } as Cardiovascular_risk_calculatorInput;
    const result = calculateCardiovascular_risk_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
