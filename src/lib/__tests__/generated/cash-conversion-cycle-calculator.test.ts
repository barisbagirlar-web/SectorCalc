// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCash_conversion_cycle_calculator,
  type Cash_conversion_cycle_calculatorInput,
} from "@generated/cash-conversion-cycle-calculator";

describe("cash-conversion-cycle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "averageInventory": 0,
    "cogs": 0,
    "averageReceivables": 0,
    "revenue": 0,
    "averagePayables": 0,
    "daysInPeriod": 365
  } as Cash_conversion_cycle_calculatorInput;
    const result = calculateCash_conversion_cycle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
