// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLoan_affordability_calculator,
  type Loan_affordability_calculatorInput,
} from "@generated/loan-affordability-calculator";

describe("loan-affordability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "monthlyIncome": 5000,
    "monthlyExpenses": 2000,
    "annualInterestRate": 5,
    "loanTermYears": 20,
    "loanAmount": 200000
  } as Loan_affordability_calculatorInput;
    const result = calculateLoan_affordability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
