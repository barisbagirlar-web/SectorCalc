// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWedding_cake_calculator,
  type Wedding_cake_calculatorInput,
} from "@generated/wedding-cake-calculator";

describe("wedding-cake-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "guestCount": 100,
    "portionGrams": 150,
    "ingredientCostPerKg": 5,
    "laborHours": 4,
    "laborRate": 20,
    "overheadPercent": 15,
    "profitMarginPercent": 30
  } as unknown as Wedding_cake_calculatorInput;
    const result = calculateWedding_cake_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
