// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConduit_fill_calculator,
  type Conduit_fill_calculatorInput,
} from "@generated/conduit-fill-calculator";

describe("conduit-fill-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "conduitInnerDiameter": 1,
    "cableOuterDiameter": 0.25,
    "numberOfConductors": 3,
    "conduitFillLimitPercent": 0,
    "safetyMarginPercent": 0
  } as Conduit_fill_calculatorInput;
    const result = calculateConduit_fill_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
