// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePhotoelectric_effect_calculator,
  type Photoelectric_effect_calculatorInput,
} from "@generated/photoelectric-effect-calculator";

describe("photoelectric-effect-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wavelength": 500,
    "workFunction": 2,
    "planckConstant": 4.135667662e-15,
    "speedOfLight": 299792458
  } as Photoelectric_effect_calculatorInput;
    const result = calculatePhotoelectric_effect_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
