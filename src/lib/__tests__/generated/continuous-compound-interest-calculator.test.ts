// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateContinuous_compound_interest_calculator,
  type Continuous_compound_interest_calculatorInput,
} from "@generated/continuous-compound-interest-calculator";

describe("continuous-compound-interest-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "principal": 1000,
    "annualRate": 5,
    "time": 1,
    "decimalPlaces": 2
  } as unknown as Continuous_compound_interest_calculatorInput;
    const result = calculateContinuous_compound_interest_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
