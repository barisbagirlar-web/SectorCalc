// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConvergent_divergent_nozzle_calculator,
  type Convergent_divergent_nozzle_calculatorInput,
} from "@generated/convergent-divergent-nozzle-calculator";

describe("convergent-divergent-nozzle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalPressure": 500000,
    "totalTemperature": 300,
    "throatArea": 0.001,
    "exitArea": 0.005,
    "gasConstant": 287,
    "specificHeatRatio": 1.4,
    "ambientPressure": 101325
  } as Convergent_divergent_nozzle_calculatorInput;
    const result = calculateConvergent_divergent_nozzle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
