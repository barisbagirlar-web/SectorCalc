// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRent_vs_buy_calculator,
  type Rent_vs_buy_calculatorInput,
} from "@generated/rent-vs-buy-calculator";

describe("rent-vs-buy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 200000,
    "downPaymentPercentage": 20,
    "loanInterestRate": 5,
    "loanTermYears": 20,
    "monthlyRent": 1000,
    "annualRentIncrease": 3
  } as unknown as Rent_vs_buy_calculatorInput;
    const result = calculateRent_vs_buy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
