// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCost_of_living_comparison_calculator,
  type Cost_of_living_comparison_calculatorInput,
} from "@generated/cost-of-living-comparison-calculator";

describe("cost-of-living-comparison-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "baseRent": 1000,
    "baseUtilities": 200,
    "baseGroceries": 400,
    "baseTransport": 150,
    "baseMisc": 250,
    "targetIndex": 120
  } as Cost_of_living_comparison_calculatorInput;
    const result = calculateCost_of_living_comparison_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
