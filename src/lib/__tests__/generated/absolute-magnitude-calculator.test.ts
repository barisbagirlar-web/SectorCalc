// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAbsolute_magnitude_calculator,
  type Absolute_magnitude_calculatorInput,
} from "@generated/absolute-magnitude-calculator";

describe("absolute-magnitude-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "apparentMagnitude": 0,
    "distanceParsecs": 10,
    "extinction": 0,
    "bolometricCorrection": 0
  } as unknown as Absolute_magnitude_calculatorInput;
    const result = calculateAbsolute_magnitude_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
