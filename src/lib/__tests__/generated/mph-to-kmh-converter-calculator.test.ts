// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMph_to_kmh_converter_calculator,
  type Mph_to_kmh_converter_calculatorInput,
} from "@generated/mph-to-kmh-converter-calculator";

describe("mph-to-kmh-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mphValue": 60,
    "precision": 2,
    "outputUnit": 0,
    "roundingMode": 0
  } as unknown as Mph_to_kmh_converter_calculatorInput;
    const result = calculateMph_to_kmh_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
