// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProfitability_index_calculator,
  type Profitability_index_calculatorInput,
} from "@generated/profitability-index-calculator";

describe("profitability-index-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 100000,
    "discountRate": 10,
    "cashFlowYear1": 30000,
    "cashFlowYear2": 30000,
    "cashFlowYear3": 30000,
    "cashFlowYear4": 30000,
    "cashFlowYear5": 30000
  } as unknown as Profitability_index_calculatorInput;
    const result = calculateProfitability_index_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
