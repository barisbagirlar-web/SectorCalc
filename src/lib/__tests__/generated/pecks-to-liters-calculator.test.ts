// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePecks_to_liters_calculator,
  type Pecks_to_liters_calculatorInput,
} from "@generated/pecks-to-liters-calculator";

describe("pecks-to-liters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "peckType": 0,
    "peckQuantity": 1,
    "decimalPlaces": 2,
    "uncertaintyMargin": 0,
    "roundingMode": 0,
    "temperature": 20
  } as Pecks_to_liters_calculatorInput;
    const result = calculatePecks_to_liters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
