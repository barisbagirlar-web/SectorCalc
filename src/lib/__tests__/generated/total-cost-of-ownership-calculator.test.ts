// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTotal_cost_of_ownership_calculator,
  type Total_cost_of_ownership_calculatorInput,
} from "@generated/total-cost-of-ownership-calculator";

describe("total-cost-of-ownership-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 10000,
    "annualMaintenance": 200,
    "annualEnergy": 500,
    "resaleValue": 3000,
    "ownershipYears": 5,
    "discountRate": 5
  } as Total_cost_of_ownership_calculatorInput;
    const result = calculateTotal_cost_of_ownership_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
