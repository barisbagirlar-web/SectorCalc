// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVoltage_drop_calculator,
  type Voltage_drop_calculatorInput,
} from "@generated/voltage-drop-calculator";

describe("voltage-drop-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "conductor_material": "copper",
    "conductor_size": "10",
    "circuit_length": 100,
    "load_current": 20,
    "system_voltage": 480,
    "phase_type": "three_phase",
    "power_factor": 0.85,
    "ambient_temperature": 30
  } as Voltage_drop_calculatorInput;
    const result = calculateVoltage_drop_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
