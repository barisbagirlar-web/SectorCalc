// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHeat_of_vaporization_calculator,
  type Heat_of_vaporization_calculatorInput,
} from "@generated/heat-of-vaporization-calculator";

describe("heat-of-vaporization-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mass": 1,
    "initialTemp": 25,
    "boilingPoint": 100,
    "specificHeat": 4.18,
    "latentHeat": 2260,
    "efficiency": 100,
    "safetyFactor": 1
  } as Heat_of_vaporization_calculatorInput;
    const result = calculateHeat_of_vaporization_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
