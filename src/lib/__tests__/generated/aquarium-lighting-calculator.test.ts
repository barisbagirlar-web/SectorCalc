// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAquarium_lighting_calculator,
  type Aquarium_lighting_calculatorInput,
} from "@generated/aquarium-lighting-calculator";

describe("aquarium-lighting-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "tankLength": 100,
    "tankWidth": 40,
    "tankHeight": 50,
    "desiredLux": 20,
    "lightEfficiency": 100,
    "photoperiod": 8,
    "electricityCost": 0.15
  } as unknown as Aquarium_lighting_calculatorInput;
    const result = calculateAquarium_lighting_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
