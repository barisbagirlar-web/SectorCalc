// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateImplied_probability_calculator,
  type Implied_probability_calculatorInput,
} from "@generated/implied-probability-calculator";

describe("implied-probability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "oddsType": 1,
    "decimalOdds": 2,
    "fractionalNumerator": 1,
    "fractionalDenominator": 2,
    "americanOdds": -110
  } as unknown as Implied_probability_calculatorInput;
    const result = calculateImplied_probability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
