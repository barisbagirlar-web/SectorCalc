// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePower_calculator_electrical_calculator,
  type Power_calculator_electrical_calculatorInput,
} from "@generated/power-calculator-electrical-calculator";

describe("power-calculator-electrical-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "voltage": 230,
    "current": 10,
    "powerFactor": 1,
    "phaseCount": 1,
    "efficiency": 1,
    "isAC": 1
  } as Power_calculator_electrical_calculatorInput;
    const result = calculatePower_calculator_electrical_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
