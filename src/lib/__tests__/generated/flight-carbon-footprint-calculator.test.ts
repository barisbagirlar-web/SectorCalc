// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFlight_carbon_footprint_calculator,
  type Flight_carbon_footprint_calculatorInput,
} from "@generated/flight-carbon-footprint-calculator";

describe("flight-carbon-footprint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance_km": 1000,
    "fuel_burn_per_100_pkm": 3.5,
    "emission_factor_kg_per_liter": 2.52,
    "class_multiplier": 1,
    "radiative_forcing_index": 1.9
  } as unknown as Flight_carbon_footprint_calculatorInput;
    const result = calculateFlight_carbon_footprint_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
