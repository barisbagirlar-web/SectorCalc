// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBackpacking_weight_calculator,
  type Backpacking_weight_calculatorInput,
} from "@generated/backpacking-weight-calculator";

describe("backpacking-weight-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "baseWeight": 5,
    "foodWeightPerDay": 0.6,
    "waterWeightPerDay": 2,
    "fuelWeight": 0.2,
    "clothingWeight": 1,
    "tripDays": 3
  } as Backpacking_weight_calculatorInput;
    const result = calculateBackpacking_weight_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
