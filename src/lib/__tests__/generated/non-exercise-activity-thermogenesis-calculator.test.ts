// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNon_exercise_activity_thermogenesis_calculator,
  type Non_exercise_activity_thermogenesis_calculatorInput,
} from "@generated/non-exercise-activity-thermogenesis-calculator";

describe("non-exercise-activity-thermogenesis-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "tdee": 2000,
    "bmr": 1500,
    "tef": 200,
    "exercise": 150
  } as unknown as Non_exercise_activity_thermogenesis_calculatorInput;
    const result = calculateNon_exercise_activity_thermogenesis_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
