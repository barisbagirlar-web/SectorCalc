// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDropshipping_profit_calculator,
  type Dropshipping_profit_calculatorInput,
} from "@generated/dropshipping-profit-calculator";

describe("dropshipping-profit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sellingPrice": 50,
    "costPrice": 30,
    "shippingCost": 5,
    "orderVolume": 100,
    "fixedCosts": 200,
    "feePercentage": 3
  } as Dropshipping_profit_calculatorInput;
    const result = calculateDropshipping_profit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
