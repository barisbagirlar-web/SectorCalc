// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePoisson_probability_comparison_calculator,
  type Poisson_probability_comparison_calculatorInput,
} from "@generated/poisson-probability-comparison-calculator";

describe("poisson-probability-comparison-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lambdaA": 2,
    "kA": 2,
    "lambdaB": 3,
    "kB": 2
  } as Poisson_probability_comparison_calculatorInput;
    const result = calculatePoisson_probability_comparison_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
