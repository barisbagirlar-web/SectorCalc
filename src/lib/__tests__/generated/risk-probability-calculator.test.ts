// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRisk_probability_calculator,
  type Risk_probability_calculatorInput,
} from "@generated/risk-probability-calculator";

describe("risk-probability-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "failureRate": 0.001,
    "missionTime": 24,
    "stressFactor": 1.5,
    "maintenanceFactor": 0.9,
    "detectionProbability": 0.8,
    "redundancyLevel": 1
  } as Risk_probability_calculatorInput;
    const result = calculateRisk_probability_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
