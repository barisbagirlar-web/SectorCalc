// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDividend_payout_ratio_calculator,
  type Dividend_payout_ratio_calculatorInput,
} from "@generated/dividend-payout-ratio-calculator";

describe("dividend-payout-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dividendsPaid": 1000000,
    "netIncome": 5000000,
    "dividendsPerShare": 2,
    "earningsPerShare": 5
  } as unknown as Dividend_payout_ratio_calculatorInput;
    const result = calculateDividend_payout_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
