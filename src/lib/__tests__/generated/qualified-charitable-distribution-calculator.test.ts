// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateQualified_charitable_distribution_calculator,
  type Qualified_charitable_distribution_calculatorInput,
} from "@generated/qualified-charitable-distribution-calculator";

describe("qualified-charitable-distribution-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 72,
    "distributionAmount": 10000,
    "marginalTaxRate": 24,
    "iraBalance": 500000
  } as unknown as Qualified_charitable_distribution_calculatorInput;
    const result = calculateQualified_charitable_distribution_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
