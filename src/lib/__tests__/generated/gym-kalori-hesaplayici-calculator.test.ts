// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGym_kalori_hesaplayici_calculator,
  type Gym_kalori_hesaplayici_calculatorInput,
} from "@generated/gym-kalori-hesaplayici-calculator";

describe("gym-kalori-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "duration": 30,
    "weight": 70,
    "met": 5,
    "intensity": 1
  } as unknown as Gym_kalori_hesaplayici_calculatorInput;
    const result = calculateGym_kalori_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
