// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSensitivity_specificity_calculator,
  type Sensitivity_specificity_calculatorInput,
} from "@generated/sensitivity-specificity-calculator";

describe("sensitivity-specificity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "truePositive": 80,
    "falsePositive": 20,
    "falseNegative": 10,
    "trueNegative": 90
  } as unknown as Sensitivity_specificity_calculatorInput;
    const result = calculateSensitivity_specificity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
