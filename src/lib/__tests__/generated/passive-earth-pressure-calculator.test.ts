// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePassive_earth_pressure_calculator,
  type Passive_earth_pressure_calculatorInput,
} from "@generated/passive-earth-pressure-calculator";

describe("passive-earth-pressure-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "soilUnitWeight": 18,
    "heightOfWall": 5,
    "frictionAngle": 30,
    "cohesion": 0,
    "surcharge": 0
  } as Passive_earth_pressure_calculatorInput;
    const result = calculatePassive_earth_pressure_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
