// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRca_recurring_cost_calculator,
  type Rca_recurring_cost_calculatorInput,
} from "@generated/rca-recurring-cost-calculator";

describe("rca-recurring-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "labor_rate": 25,
    "labor_hours_per_cycle": 0.5,
    "material_cost_per_unit": 12,
    "energy_cost_per_cycle": 1.5,
    "maintenance_cost_per_cycle": 0.75,
    "overhead_rate": 20,
    "cycle_time_minutes": 30,
    "defect_rate": 2
  } as unknown as Rca_recurring_cost_calculatorInput;
    const result = calculateRca_recurring_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
