// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConfidence_interval_for_proportion_calculator,
  type Confidence_interval_for_proportion_calculatorInput,
} from "@generated/confidence-interval-for-proportion-calculator";

describe("confidence-interval-for-proportion-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sampleSize": 100,
    "successes": 50,
    "zValue": 1.96,
    "confidenceLevel": 0.95
  } as Confidence_interval_for_proportion_calculatorInput;
    const result = calculateConfidence_interval_for_proportion_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
