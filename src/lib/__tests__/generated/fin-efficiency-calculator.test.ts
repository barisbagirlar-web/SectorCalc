// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFin_efficiency_calculator,
  type Fin_efficiency_calculatorInput,
} from "@generated/fin-efficiency-calculator";

describe("fin-efficiency-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "finLength": 0.1,
    "finThickness": 0.001,
    "finWidth": 0.05,
    "heatTransferCoefficient": 25,
    "thermalConductivity": 200
  } as unknown as Fin_efficiency_calculatorInput;
    const result = calculateFin_efficiency_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
