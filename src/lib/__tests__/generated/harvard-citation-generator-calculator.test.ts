// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHarvard_citation_generator_calculator,
  type Harvard_citation_generator_calculatorInput,
} from "@generated/harvard-citation-generator-calculator";

describe("harvard-citation-generator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "materialCost": 100,
    "laborHours": 8,
    "hourlyRate": 25,
    "overheadPercentage": 20,
    "profitMarginPercentage": 15
  } as unknown as Harvard_citation_generator_calculatorInput;
    const result = calculateHarvard_citation_generator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
