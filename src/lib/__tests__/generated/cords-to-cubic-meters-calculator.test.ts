// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCords_to_cubic_meters_calculator,
  type Cords_to_cubic_meters_calculatorInput,
} from "@generated/cords-to-cubic-meters-calculator";

describe("cords-to-cubic-meters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length_ft": 8,
    "width_ft": 4,
    "height_ft": 4,
    "stack_count": 1,
    "waste_percent": 0
  } as unknown as Cords_to_cubic_meters_calculatorInput;
    const result = calculateCords_to_cubic_meters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
