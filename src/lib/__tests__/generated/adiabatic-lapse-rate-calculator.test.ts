// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAdiabatic_lapse_rate_calculator,
  type Adiabatic_lapse_rate_calculatorInput,
} from "@generated/adiabatic-lapse-rate-calculator";

describe("adiabatic-lapse-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialAltitude": 1,
    "finalAltitude": 1000,
    "initialTemperature": 293.15,
    "specificHeatCapacity": 1005,
    "gravityAcceleration": 9.81
  } as unknown as Adiabatic_lapse_rate_calculatorInput;
    const result = calculateAdiabatic_lapse_rate_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
