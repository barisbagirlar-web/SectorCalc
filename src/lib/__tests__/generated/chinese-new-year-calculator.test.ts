// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateChinese_new_year_calculator,
  type Chinese_new_year_calculatorInput,
} from "@generated/chinese-new-year-calculator";

describe("chinese-new-year-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "baseSalary": 50000,
    "bonusMonths": 1,
    "yearsOfService": 5,
    "dependents": 0,
    "overtimeHours": 0,
    "taxRate": 0.2
  } as unknown as Chinese_new_year_calculatorInput;
    const result = calculateChinese_new_year_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
