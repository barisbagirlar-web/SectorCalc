// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePokemon_card_calculator,
  type Pokemon_card_calculatorInput,
} from "@generated/pokemon-card-calculator";

describe("pokemon-card-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "marketPrice": 100,
    "conditionFactor": 1,
    "rarityMultiplier": 1,
    "gradingCost": 20,
    "sellingFeePercent": 10,
    "shippingCost": 5,
    "acquisitionCost": 20
  } as Pokemon_card_calculatorInput;
    const result = calculatePokemon_card_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
