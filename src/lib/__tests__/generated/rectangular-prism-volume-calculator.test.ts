// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRectangular_prism_volume_calculator,
  type Rectangular_prism_volume_calculatorInput,
} from "@generated/rectangular-prism-volume-calculator";

describe("rectangular-prism-volume-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length": 1000,
    "width": 500,
    "height": 300,
    "quantity": 1
  } as unknown as Rectangular_prism_volume_calculatorInput;
    const result = calculateRectangular_prism_volume_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
