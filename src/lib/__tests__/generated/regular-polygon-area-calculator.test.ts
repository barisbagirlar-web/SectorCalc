// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRegular_polygon_area_calculator,
  type Regular_polygon_area_calculatorInput,
} from "@generated/regular-polygon-area-calculator";

describe("regular-polygon-area-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numberOfSides": 6,
    "sideLength": 1,
    "unitScale": 1,
    "decimals": 2
  } as unknown as Regular_polygon_area_calculatorInput;
    const result = calculateRegular_polygon_area_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
