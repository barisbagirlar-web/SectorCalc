// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWhole_life_insurance_calculator,
  type Whole_life_insurance_calculatorInput,
} from "@generated/whole-life-insurance-calculator";

describe("whole-life-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sumAssured": 100000,
    "mortalityRate": 0.01,
    "interestRate": 0.05,
    "expenseLoading": 0.1
  } as Whole_life_insurance_calculatorInput;
    const result = calculateWhole_life_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
