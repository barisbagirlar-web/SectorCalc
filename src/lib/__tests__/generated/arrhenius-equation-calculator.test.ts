// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateArrhenius_equation_calculator,
  type Arrhenius_equation_calculatorInput,
} from "@generated/arrhenius-equation-calculator";

describe("arrhenius-equation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "temperature": 298.15,
    "activationEnergy": 50000,
    "preExpFactor": 10000000000,
    "gasConstant": 8.314
  } as Arrhenius_equation_calculatorInput;
    const result = calculateArrhenius_equation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
