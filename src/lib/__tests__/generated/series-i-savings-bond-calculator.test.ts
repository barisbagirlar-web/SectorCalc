// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSeries_i_savings_bond_calculator,
  type Series_i_savings_bond_calculatorInput,
} from "@generated/series-i-savings-bond-calculator";

describe("series-i-savings-bond-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "principal": 10000,
    "fixedRate": 0,
    "inflationRate": 1.5,
    "years": 5
  } as unknown as Series_i_savings_bond_calculatorInput;
    const result = calculateSeries_i_savings_bond_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
