// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFabric_cutting_optimizer_calculator,
  type Fabric_cutting_optimizer_calculatorInput,
} from "@generated/fabric-cutting-optimizer-calculator";

describe("fabric-cutting-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fabric_width": 150,
    "fabric_length": 100,
    "pattern_length": 120,
    "pattern_width": 60,
    "quantity": 500,
    "cutting_method": "single_ply",
    "material_cost_per_m2": 12.5,
    "labor_rate_per_hour": 25
  } as unknown as Fabric_cutting_optimizer_calculatorInput;
    const result = calculateFabric_cutting_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
