// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateScreen_resolution_calculator,
  type Screen_resolution_calculatorInput,
} from "@generated/screen-resolution-calculator";

describe("screen-resolution-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "screenWidth": 1920,
    "screenHeight": 1080,
    "screenDiagonal": 24,
    "aspectRatioX": 16,
    "aspectRatioY": 9
  } as Screen_resolution_calculatorInput;
    const result = calculateScreen_resolution_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
