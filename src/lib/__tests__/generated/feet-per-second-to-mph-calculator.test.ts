// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFeet_per_second_to_mph_calculator,
  type Feet_per_second_to_mph_calculatorInput,
} from "@generated/feet-per-second-to-mph-calculator";

describe("feet-per-second-to-mph-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "feetPerSecond": 1,
    "precision": 2,
    "calibrationOffset": 0,
    "outputUnitScaling": 1
  } as Feet_per_second_to_mph_calculatorInput;
    const result = calculateFeet_per_second_to_mph_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
