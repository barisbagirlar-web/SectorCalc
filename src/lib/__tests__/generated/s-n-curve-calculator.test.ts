// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateS_n_curve_calculator,
  type S_n_curve_calculatorInput,
} from "@generated/s-n-curve-calculator";

describe("s-n-curve-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "stress_amplitude": 200,
    "mean_stress": 0,
    "ultimate_tensile_strength": 600,
    "fatigue_strength_coefficient": 900,
    "fatigue_strength_exponent": -0.1
  } as unknown as S_n_curve_calculatorInput;
    const result = calculateS_n_curve_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
