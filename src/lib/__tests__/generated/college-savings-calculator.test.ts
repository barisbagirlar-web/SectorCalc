// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCollege_savings_calculator,
  type College_savings_calculatorInput,
} from "@generated/college-savings-calculator";

describe("college-savings-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "childAge": 0,
    "yearsToCollege": 18,
    "currentSavings": 0,
    "annualContribution": 0,
    "expectedReturn": 5,
    "inflationRate": 2,
    "annualCollegeCost": 20000,
    "collegeYears": 4
  } as College_savings_calculatorInput;
    const result = calculateCollege_savings_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
