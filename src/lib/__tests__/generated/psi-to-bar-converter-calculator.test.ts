// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePsi_to_bar_converter_calculator,
  type Psi_to_bar_converter_calculatorInput,
} from "@generated/psi-to-bar-converter-calculator";

describe("psi-to-bar-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pressure_psi": 0,
    "temperature_celsius": 20,
    "altitude_meters": 0,
    "fluid_type": "water",
    "include_temperature_correction": true,
    "include_altitude_correction": false
  } as Psi_to_bar_converter_calculatorInput;
    const result = calculatePsi_to_bar_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
