// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOpportunity_cost_calculator,
  type Opportunity_cost_calculatorInput,
} from "@generated/opportunity-cost-calculator";

describe("opportunity-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "investmentAmount": 100000,
    "currentReturnRate": 5,
    "alternativeReturnRate": 8,
    "timePeriod": 1
  } as unknown as Opportunity_cost_calculatorInput;
    const result = calculateOpportunity_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
