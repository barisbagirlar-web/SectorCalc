// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLease_vs_buy_comparator_calculator,
  type Lease_vs_buy_comparator_calculatorInput,
} from "@generated/lease-vs-buy-comparator-calculator";

describe("lease-vs-buy-comparator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "equipmentCost": 100000,
    "leaseTermMonths": 36,
    "monthlyLeasePayment": 2800,
    "residualValue": 20000,
    "annualMaintenanceCost": 5000,
    "leaseMaintenanceIncluded": true,
    "discountRate": 8,
    "utilizationRate": 85
  } as Lease_vs_buy_comparator_calculatorInput;
    const result = calculateLease_vs_buy_comparator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
