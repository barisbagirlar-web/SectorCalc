// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNoi_calculator,
  type Noi_calculatorInput,
} from "@generated/noi-calculator";

describe("noi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "grossRentalIncome": 100000,
    "vacancyLoss": 5000,
    "propertyManagementFees": 8000,
    "repairsMaintenance": 6000,
    "propertyTaxes": 10000,
    "insurance": 4000,
    "utilities": 3000,
    "otherOperatingExpenses": 2000
  } as Noi_calculatorInput;
    const result = calculateNoi_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
