// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRenovation_budget_optimizer_calculator,
  type Renovation_budget_optimizer_calculatorInput,
} from "@generated/renovation-budget-optimizer-calculator";

describe("renovation-budget-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_area_sqft": 2000,
    "scope_complexity": "medium",
    "material_quality": "standard",
    "labor_efficiency_factor": 0.15,
    "waste_factor_pct": 10,
    "contingency_pct": 15,
    "region_cost_index": 1,
    "sustainability_target": "none"
  } as unknown as Renovation_budget_optimizer_calculatorInput;
    const result = calculateRenovation_budget_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
