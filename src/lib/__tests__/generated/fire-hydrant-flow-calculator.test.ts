// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFire_hydrant_flow_calculator,
  type Fire_hydrant_flow_calculatorInput,
} from "@generated/fire-hydrant-flow-calculator";

describe("fire-hydrant-flow-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "static_pressure": 60,
    "residual_pressure": 40,
    "flow_rate_test": 500,
    "pipe_diameter": 6,
    "pipe_length": 500,
    "hazen_williams_coefficient": 120,
    "elevation_difference": 1,
    "number_of_hydrants": 1
  } as unknown as Fire_hydrant_flow_calculatorInput;
    const result = calculateFire_hydrant_flow_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
