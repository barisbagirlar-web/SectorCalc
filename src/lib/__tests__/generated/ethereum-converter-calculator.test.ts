// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEthereum_converter_calculator,
  type Ethereum_converter_calculatorInput,
} from "@generated/ethereum-converter-calculator";

describe("ethereum-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ethAmount": 1,
    "ethPriceUSD": 3000,
    "gasLimit": 21000,
    "maxFeePerGas": 50,
    "networkMultiplier": 1,
    "conversionFeePercent": 0.5
  } as unknown as Ethereum_converter_calculatorInput;
    const result = calculateEthereum_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
