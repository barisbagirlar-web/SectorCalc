// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHydraulic_system_energy_loss_calculator,
  type Hydraulic_system_energy_loss_calculatorInput,
} from "@generated/hydraulic-system-energy-loss-calculator";

describe("hydraulic-system-energy-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "flow_rate": 100,
    "pressure_drop_total": 50,
    "fluid_density": 870,
    "kinematic_viscosity": 46,
    "pipe_length": 50,
    "pipe_diameter": 25,
    "valve_count": 5,
    "fitting_count": 20
  } as unknown as Hydraulic_system_energy_loss_calculatorInput;
    const result = calculateHydraulic_system_energy_loss_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
