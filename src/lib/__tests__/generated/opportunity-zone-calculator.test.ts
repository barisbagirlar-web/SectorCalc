// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOpportunity_zone_calculator,
  type Opportunity_zone_calculatorInput,
} from "@generated/opportunity-zone-calculator";

describe("opportunity-zone-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialCapitalGain": 100000,
    "federalTaxRate": 23.8,
    "stateTaxRate": 5,
    "holdingYears": 10,
    "annualAppreciation": 7
  } as Opportunity_zone_calculatorInput;
    const result = calculateOpportunity_zone_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
