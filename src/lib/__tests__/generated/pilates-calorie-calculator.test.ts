// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePilates_calorie_calculator,
  type Pilates_calorie_calculatorInput,
} from "@generated/pilates-calorie-calculator";

describe("pilates-calorie-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weight": 70,
    "warmupDuration": 5,
    "mainDuration": 30,
    "cooldownDuration": 5,
    "difficultyFactor": 1
  } as unknown as Pilates_calorie_calculatorInput;
    const result = calculatePilates_calorie_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
