// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressibility_factor_calculator,
  type Compressibility_factor_calculatorInput,
} from "@generated/compressibility-factor-calculator";

describe("compressibility-factor-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pressure": 101325,
    "volume": 0.0224,
    "moles": 1,
    "temperature": 273.15,
    "gasConstant": 8.314
  } as Compressibility_factor_calculatorInput;
    const result = calculateCompressibility_factor_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
