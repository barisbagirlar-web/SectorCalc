// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateContribution_margin_calculator,
  type Contribution_margin_calculatorInput,
} from "@generated/contribution-margin-calculator";

describe("contribution-margin-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "salesPricePerUnit": 100,
    "variableCostPerUnit": 60,
    "unitsSold": 1000,
    "fixedCosts": 20000
  } as Contribution_margin_calculatorInput;
    const result = calculateContribution_margin_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
