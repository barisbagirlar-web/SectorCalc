// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePupillary_distance_calculator,
  type Pupillary_distance_calculatorInput,
} from "@generated/pupillary-distance-calculator";

describe("pupillary-distance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rightPd": 30,
    "leftPd": 30,
    "workingDistance": 40,
    "vertexDistance": 12,
    "centerRotationDistance": 15
  } as unknown as Pupillary_distance_calculatorInput;
    const result = calculatePupillary_distance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
