// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCost_benefit_analysis_calculator,
  type Cost_benefit_analysis_calculatorInput,
} from "@generated/cost-benefit-analysis-calculator";

describe("cost-benefit-analysis-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 0,
    "annualBenefits": 0,
    "annualCosts": 0,
    "discountRate": 10,
    "projectLifetime": 5,
    "salvageValue": 0
  } as unknown as Cost_benefit_analysis_calculatorInput;
    const result = calculateCost_benefit_analysis_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
