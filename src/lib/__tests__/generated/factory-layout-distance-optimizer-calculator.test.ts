// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFactory_layout_distance_optimizer_calculator,
  type Factory_layout_distance_optimizer_calculatorInput,
} from "@generated/factory-layout-distance-optimizer-calculator";

describe("factory-layout-distance-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_departments": 6,
    "flow_intensity_matrix": 1,
    "distance_matrix": 1,
    "path_efficiency_factor": 0.85,
    "layout_type": "process",
    "use_weighted_distance": true
  } as unknown as Factory_layout_distance_optimizer_calculatorInput;
    const result = calculateFactory_layout_distance_optimizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
