// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRecast_mortgage_calculator,
  type Recast_mortgage_calculatorInput,
} from "@generated/recast-mortgage-calculator";

describe("recast-mortgage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentBalance": 200000,
    "annualInterestRate": 4.5,
    "remainingTerm": 180,
    "lumpSumPayment": 50000,
    "recastFee": 250
  } as unknown as Recast_mortgage_calculatorInput;
    const result = calculateRecast_mortgage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
