// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLoan_payoff_calculator,
  type Loan_payoff_calculatorInput,
} from "@generated/loan-payoff-calculator";

describe("loan-payoff-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loanAmount": 100000,
    "annualInterestRate": 5.5,
    "monthlyPayment": 1000,
    "extraMonthlyPayment": 1
  } as unknown as Loan_payoff_calculatorInput;
    const result = calculateLoan_payoff_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
