// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBibliography_generator_calculator,
  type Bibliography_generator_calculatorInput,
} from "@generated/bibliography-generator-calculator";

describe("bibliography-generator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pageCount": 10,
    "referencesPerPage": 2,
    "citationDensity": 75,
    "formattingTimePerRef": 5,
    "styleComplexity": 1.2,
    "overheadTime": 15,
    "referencesFromExternal": 5,
    "autoGeneratedPerRef": 1
  } as unknown as Bibliography_generator_calculatorInput;
    const result = calculateBibliography_generator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
