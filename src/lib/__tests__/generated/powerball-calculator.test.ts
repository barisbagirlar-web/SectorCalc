// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePowerball_calculator,
  type Powerball_calculatorInput,
} from "@generated/powerball-calculator";

describe("powerball-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "internalDiameter": 3.5,
    "internalLength": 6,
    "millSpeed": 18,
    "ballFillingFraction": 0.3,
    "ballBulkDensity": 4800,
    "motorEfficiency": 0.95
  } as unknown as Powerball_calculatorInput;
    const result = calculatePowerball_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
