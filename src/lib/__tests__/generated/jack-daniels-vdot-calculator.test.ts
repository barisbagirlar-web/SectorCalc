// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateJack_daniels_vdot_calculator,
  type Jack_daniels_vdot_calculatorInput,
} from "@generated/jack-daniels-vdot-calculator";

describe("jack-daniels-vdot-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "raceDistance": 5000,
    "raceTimeMinutes": 20,
    "raceTimeSeconds": 0,
    "targetDistance": 10000
  } as unknown as Jack_daniels_vdot_calculatorInput;
    const result = calculateJack_daniels_vdot_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
