// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCost_segregation_calculator,
  type Cost_segregation_calculatorInput,
} from "@generated/cost-segregation-calculator";

describe("cost-segregation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalPropertyCost": 1000000,
    "landPercentage": 20,
    "buildingPercentage": 60,
    "personalPropertyPercentage": 15,
    "landImprovementsPercentage": 5,
    "buildingLife": 39,
    "personalPropertyLife": 7,
    "landImprovementsLife": 15
  } as Cost_segregation_calculatorInput;
    const result = calculateCost_segregation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
