// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFlight_cost_calculator,
  type Flight_cost_calculatorInput,
} from "@generated/flight-cost-calculator";

describe("flight-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 1000,
    "avgSpeed": 800,
    "fuelConsumptionRate": 3.5,
    "fuelPrice": 0.8,
    "crewCostPerHour": 200,
    "landingFee": 500,
    "maintenanceCostPerHour": 100
  } as unknown as Flight_cost_calculatorInput;
    const result = calculateFlight_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
