// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProcess_economics_npv_calculator,
  type Process_economics_npv_calculatorInput,
} from "@generated/process-economics-npv-calculator";

describe("process-economics-npv-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 100000,
    "annualCostSavings": 25000,
    "projectLife": 5,
    "discountRate": 8
  } as unknown as Process_economics_npv_calculatorInput;
    const result = calculateProcess_economics_npv_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
