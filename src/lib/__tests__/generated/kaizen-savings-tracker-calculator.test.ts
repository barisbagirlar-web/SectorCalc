// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKaizen_savings_tracker_calculator,
  type Kaizen_savings_tracker_calculatorInput,
} from "@generated/kaizen-savings-tracker-calculator";

describe("kaizen-savings-tracker-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "labor_rate": 25,
    "operators_affected": 10,
    "time_saved_per_operator": 15,
    "shifts_per_day": 2,
    "operating_days_per_year": 250,
    "defect_rate_before": 5,
    "defect_rate_after": 2,
    "annual_production_volume": 100000
  } as Kaizen_savings_tracker_calculatorInput;
    const result = calculateKaizen_savings_tracker_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
