// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProcess_economics_calculator,
  type Process_economics_calculatorInput,
} from "@generated/process-economics-calculator";

describe("process-economics-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "raw_material_cost": 10,
    "labor_cost": 5,
    "overhead_cost": 3,
    "selling_price": 25,
    "production_volume": 1000,
    "defect_rate": 2
  } as Process_economics_calculatorInput;
    const result = calculateProcess_economics_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
