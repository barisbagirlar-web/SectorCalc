// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateShakes_to_nanoseconds_calculator,
  type Shakes_to_nanoseconds_calculatorInput,
} from "@generated/shakes-to-nanoseconds-calculator";

describe("shakes-to-nanoseconds-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "shakes": 1,
    "precision": 2,
    "conversionFactor": 10,
    "outputScale": 1
  } as unknown as Shakes_to_nanoseconds_calculatorInput;
    const result = calculateShakes_to_nanoseconds_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
