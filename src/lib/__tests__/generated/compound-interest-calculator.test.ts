// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompound_interest_calculator,
  type Compound_interest_calculatorInput,
} from "@generated/compound-interest-calculator";

describe("compound-interest-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "principal": 10000,
    "annualInterestRate": 5,
    "compoundingFrequency": "12",
    "timePeriod": 10,
    "additionalContribution": 0,
    "inflationRate": 2,
    "taxRate": 0
  } as Compound_interest_calculatorInput;
    const result = calculateCompound_interest_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
