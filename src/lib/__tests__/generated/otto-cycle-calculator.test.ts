// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOtto_cycle_calculator,
  type Otto_cycle_calculatorInput,
} from "@generated/otto-cycle-calculator";

describe("otto-cycle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "compressionRatio": 8,
    "specificHeatRatio": 1.4,
    "initialTemperature": 300,
    "initialPressure": 100,
    "maxTemperature": 2000,
    "specificHeatCv": 0.718
  } as Otto_cycle_calculatorInput;
    const result = calculateOtto_cycle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
