// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSystem_of_equations_calculator,
  type System_of_equations_calculatorInput,
} from "@generated/system-of-equations-calculator";

describe("system-of-equations-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "machineHoursPerUnitProd1": 2,
    "machineHoursPerUnitProd2": 4,
    "totalMachineHours": 100,
    "materialPerUnitProd1": 3,
    "materialPerUnitProd2": 1,
    "totalMaterial": 60
  } as unknown as System_of_equations_calculatorInput;
    const result = calculateSystem_of_equations_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
