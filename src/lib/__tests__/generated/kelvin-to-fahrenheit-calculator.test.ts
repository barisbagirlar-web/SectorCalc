// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKelvin_to_fahrenheit_calculator,
  type Kelvin_to_fahrenheit_calculatorInput,
} from "@generated/kelvin-to-fahrenheit-calculator";

describe("kelvin-to-fahrenheit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "kelvin": 273.15,
    "precision": 2,
    "calibrationOffset": 0,
    "expectedFahrenheit": 32
  } as unknown as Kelvin_to_fahrenheit_calculatorInput;
    const result = calculateKelvin_to_fahrenheit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
