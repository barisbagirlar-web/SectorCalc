// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFreight_cost_calculator,
  type Freight_cost_calculatorInput,
} from "@generated/freight-cost-calculator";

describe("freight-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "shipment_weight_kg": 100,
    "shipment_volume_cbm": 1,
    "distance_km": 500,
    "transport_mode": "FTL",
    "fuel_surcharge_percent": 15,
    "accessorial_charges_usd": 50,
    "density_factor": 100,
    "is_hazardous": false
  } as Freight_cost_calculatorInput;
    const result = calculateFreight_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
