// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBeam_weight_calculator,
  type Beam_weight_calculatorInput,
} from "@generated/beam-weight-calculator";

describe("beam-weight-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "beam_type": "I-beam",
    "material_density": 7850,
    "length": 6,
    "flange_width": 200,
    "flange_thickness": 12,
    "web_height": 300,
    "web_thickness": 8,
    "quantity": 1
  } as Beam_weight_calculatorInput;
    const result = calculateBeam_weight_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
