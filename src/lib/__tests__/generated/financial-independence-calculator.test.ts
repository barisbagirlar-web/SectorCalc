// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFinancial_independence_calculator,
  type Financial_independence_calculatorInput,
} from "@generated/financial-independence-calculator";

describe("financial-independence-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentAge": 30,
    "currentPortfolio": 50000,
    "monthlySavings": 2000,
    "annualExpenses": 40000,
    "annualReturn": 7,
    "safeWithdrawalRate": 4
  } as Financial_independence_calculatorInput;
    const result = calculateFinancial_independence_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
