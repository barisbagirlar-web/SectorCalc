// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNoise_vibration_cost_calculator,
  type Noise_vibration_cost_calculatorInput,
} from "@generated/noise-vibration-cost-calculator";

describe("noise-vibration-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_workers_exposed": 10,
    "avg_daily_exposure_hours": 8,
    "noise_level_dba": 85,
    "vibration_level_ms2": 0.5,
    "machine_runtime_hours_per_day": 16,
    "maintenance_cost_per_machine_per_year": 5000,
    "number_of_machines": 5,
    "avg_worker_annual_salary": 45000
  } as unknown as Noise_vibration_cost_calculatorInput;
    const result = calculateNoise_vibration_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
