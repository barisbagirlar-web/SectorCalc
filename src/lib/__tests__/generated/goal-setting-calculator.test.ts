// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGoal_setting_calculator,
  type Goal_setting_calculatorInput,
} from "@generated/goal-setting-calculator";

describe("goal-setting-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentOEE": 70,
    "targetOEE": 85,
    "timeframe": 6,
    "currentAvailability": 80,
    "currentPerformance": 90,
    "currentQuality": 95
  } as unknown as Goal_setting_calculatorInput;
    const result = calculateGoal_setting_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
