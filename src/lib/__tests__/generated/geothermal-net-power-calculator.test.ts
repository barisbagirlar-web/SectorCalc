// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGeothermal_net_power_calculator,
  type Geothermal_net_power_calculatorInput,
} from "@generated/geothermal-net-power-calculator";

describe("geothermal-net-power-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "massFlowRate": 50,
    "specificHeat": 4200,
    "inletTemp": 150,
    "outletTemp": 80,
    "conversionEfficiency": 12,
    "parasiticLoad": 100
  } as unknown as Geothermal_net_power_calculatorInput;
    const result = calculateGeothermal_net_power_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
