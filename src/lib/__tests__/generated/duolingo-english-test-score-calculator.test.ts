// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDuolingo_english_test_score_calculator,
  type Duolingo_english_test_score_calculatorInput,
} from "@generated/duolingo-english-test-score-calculator";

describe("duolingo-english-test-score-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "literacy": 120,
    "comprehension": 120,
    "conversation": 120,
    "production": 120
  } as Duolingo_english_test_score_calculatorInput;
    const result = calculateDuolingo_english_test_score_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
