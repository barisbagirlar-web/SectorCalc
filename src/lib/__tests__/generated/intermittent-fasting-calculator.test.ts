// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIntermittent_fasting_calculator,
  type Intermittent_fasting_calculatorInput,
} from "@generated/intermittent-fasting-calculator";

describe("intermittent-fasting-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fastingStartHour": 20,
    "fastingDurationHours": 16,
    "eatingWindowDurationHours": 8,
    "targetDailyCalories": 2000,
    "currentWeight": 70
  } as unknown as Intermittent_fasting_calculatorInput;
    const result = calculateIntermittent_fasting_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
