// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWaist_to_height_ratio_calculator,
  type Waist_to_height_ratio_calculatorInput,
} from "@generated/waist-to-height-ratio-calculator";

describe("waist-to-height-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "waist": 80,
    "waistUnit": 0,
    "height": 170,
    "heightUnit": 0
  } as unknown as Waist_to_height_ratio_calculatorInput;
    const result = calculateWaist_to_height_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
