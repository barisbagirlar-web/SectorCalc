// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVacation_budget_calculator,
  type Vacation_budget_calculatorInput,
} from "@generated/vacation-budget-calculator";

describe("vacation-budget-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numberOfTravelers": 1,
    "numberOfNights": 7,
    "flightCostPerPerson": 500,
    "hotelCostPerNight": 100,
    "dailyFoodCostPerPerson": 50,
    "activitiesCostPerPerson": 200,
    "miscellaneousCost": 100
  } as Vacation_budget_calculatorInput;
    const result = calculateVacation_budget_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
