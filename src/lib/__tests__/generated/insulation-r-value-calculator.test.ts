// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInsulation_r_value_calculator,
  type Insulation_r_value_calculatorInput,
} from "@generated/insulation-r-value-calculator";

describe("insulation-r-value-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "thickness1": 0.1,
    "conductivity1": 0.04,
    "thickness2": 0,
    "conductivity2": 0,
    "thickness3": 0,
    "conductivity3": 0,
    "area": 1,
    "deltaT": 20
  } as Insulation_r_value_calculatorInput;
    const result = calculateInsulation_r_value_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
