// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSocial_security_early_retirement_calculator,
  type Social_security_early_retirement_calculatorInput,
} from "@generated/social-security-early-retirement-calculator";

describe("social-security-early-retirement-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "birthYear": 1960,
    "earlyRetirementAge": 62,
    "monthlyBenefitAtFRA": 1500,
    "scalingFactor": 1
  } as unknown as Social_security_early_retirement_calculatorInput;
    const result = calculateSocial_security_early_retirement_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
