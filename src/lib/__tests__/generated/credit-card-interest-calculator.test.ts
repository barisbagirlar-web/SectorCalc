// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCredit_card_interest_calculator,
  type Credit_card_interest_calculatorInput,
} from "@generated/credit-card-interest-calculator";

describe("credit-card-interest-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "outstandingBalance": 1000,
    "annualPercentageRate": 18,
    "billingDays": 30,
    "minimumPaymentPercent": 2
  } as unknown as Credit_card_interest_calculatorInput;
    const result = calculateCredit_card_interest_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
