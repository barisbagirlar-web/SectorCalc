// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBasement_wall_concrete_calculator,
  type Basement_wall_concrete_calculatorInput,
} from "@generated/basement-wall-concrete-calculator";

describe("basement-wall-concrete-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wallHeight": 3,
    "wallLength": 10,
    "wallThickness": 300,
    "concreteDensity": 2400,
    "wasteFactor": 5,
    "numWalls": 1
  } as Basement_wall_concrete_calculatorInput;
    const result = calculateBasement_wall_concrete_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
