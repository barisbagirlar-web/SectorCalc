// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalorie_burn_by_activity_calculator,
  type Calorie_burn_by_activity_calculatorInput,
} from "@generated/calorie-burn-by-activity-calculator";

describe("calorie-burn-by-activity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weight_kg": 70,
    "duration_min": 30,
    "met_value": 5,
    "efficiency_factor": 1
  } as unknown as Calorie_burn_by_activity_calculatorInput;
    const result = calculateCalorie_burn_by_activity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
