// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHow_long_to_lose_weight_calculator,
  type How_long_to_lose_weight_calculatorInput,
} from "@generated/how-long-to-lose-weight-calculator";

describe("how-long-to-lose-weight-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentWeight_kg": 80,
    "goalWeight_kg": 70,
    "height_cm": 170,
    "age": 30,
    "gender": 1,
    "activityLevel": 1.2,
    "dailyCalorieIntake": 2000
  } as unknown as How_long_to_lose_weight_calculatorInput;
    const result = calculateHow_long_to_lose_weight_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
