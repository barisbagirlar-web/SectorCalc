// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWaste_reduction_calculator,
  type Waste_reduction_calculatorInput,
} from "@generated/waste-reduction-calculator";

describe("waste-reduction-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalProduction": 1000,
    "defectUnits": 50,
    "materialInput": 500,
    "wasteMaterial": 30,
    "materialCostPerKg": 2.5,
    "defectReductionTarget": 20
  } as Waste_reduction_calculatorInput;
    const result = calculateWaste_reduction_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
