// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDeep_work_calculator,
  type Deep_work_calculatorInput,
} from "@generated/deep-work-calculator";

describe("deep-work-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dailyWorkHours": 8,
    "meetingCount": 3,
    "meetingDuration": 30,
    "interruptionCount": 10,
    "recoveryTime": 15,
    "deepWorkBlock": 90,
    "shallowWorkPercent": 30
  } as unknown as Deep_work_calculatorInput;
    const result = calculateDeep_work_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
