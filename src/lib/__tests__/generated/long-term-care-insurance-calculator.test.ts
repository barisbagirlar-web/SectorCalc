// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLong_term_care_insurance_calculator,
  type Long_term_care_insurance_calculatorInput,
} from "@generated/long-term-care-insurance-calculator";

describe("long-term-care-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dailyBenefit": 150,
    "benefitPeriodYears": 3,
    "eliminationDays": 90,
    "inflationProtection": 3
  } as unknown as Long_term_care_insurance_calculatorInput;
    const result = calculateLong_term_care_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
