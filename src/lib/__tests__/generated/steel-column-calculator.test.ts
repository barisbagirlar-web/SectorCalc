// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSteel_column_calculator,
  type Steel_column_calculatorInput,
} from "@generated/steel-column-calculator";

describe("steel-column-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "effectiveLengthFactor": 1,
    "unbracedLength": 3000,
    "width": 100,
    "thickness": 10,
    "elasticModulus": 200,
    "yieldStrength": 250,
    "safetyFactor": 1.67
  } as Steel_column_calculatorInput;
    const result = calculateSteel_column_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
