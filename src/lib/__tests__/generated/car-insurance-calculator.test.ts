// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCar_insurance_calculator,
  type Car_insurance_calculatorInput,
} from "@generated/car-insurance-calculator";

describe("car-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "carValue": 20000,
    "driverAge": 35,
    "drivingExperience": 10,
    "noClaimsYears": 5,
    "annualMileage": 15000,
    "coverageLevel": 3,
    "regionRisk": 5,
    "vehicleAge": 3
  } as Car_insurance_calculatorInput;
    const result = calculateCar_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
