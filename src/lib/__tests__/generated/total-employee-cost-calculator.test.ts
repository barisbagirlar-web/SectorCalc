// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTotal_employee_cost_calculator,
  type Total_employee_cost_calculatorInput,
} from "@generated/total-employee-cost-calculator";

describe("total-employee-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "base_salary": 60000,
    "bonus_percent": 10,
    "benefits_percent": 30,
    "payroll_tax_percent": 7.65,
    "overhead_percent": 20,
    "training_cost": 1500,
    "productivity_factor": 0.85,
    "turnover_rate": 15
  } as Total_employee_cost_calculatorInput;
    const result = calculateTotal_employee_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
