// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAtomik_kutle_hesaplayici_calculator,
  type Atomik_kutle_hesaplayici_calculatorInput,
} from "@generated/atomik-kutle-hesaplayici-calculator";

describe("atomik-kutle-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mass1": 12,
    "abundance1": 98.93,
    "mass2": 13.003,
    "abundance2": 1.07,
    "mass3": 0,
    "abundance3": 0,
    "mass4": 0,
    "abundance4": 0
  } as unknown as Atomik_kutle_hesaplayici_calculatorInput;
    const result = calculateAtomik_kutle_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
