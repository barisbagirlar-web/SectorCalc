// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGibbs_free_energy_reaction_calculator,
  type Gibbs_free_energy_reaction_calculatorInput,
} from "@generated/gibbs-free-energy-reaction-calculator";

describe("gibbs-free-energy-reaction-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "temperature": 298.15,
    "deltaH": 0,
    "S_products": 0,
    "S_reactants": 0
  } as unknown as Gibbs_free_energy_reaction_calculatorInput;
    const result = calculateGibbs_free_energy_reaction_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
