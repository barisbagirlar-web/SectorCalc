// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCubic_yards_to_cubic_meters_calculator,
  type Cubic_yards_to_cubic_meters_calculatorInput,
} from "@generated/cubic-yards-to-cubic-meters-calculator";

describe("cubic-yards-to-cubic-meters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length": 1,
    "width": 1,
    "height": 1,
    "cubicYards": 1,
    "decimalPlaces": 2,
    "conversionConstant": 0.764554857992
  } as unknown as Cubic_yards_to_cubic_meters_calculatorInput;
    const result = calculateCubic_yards_to_cubic_meters_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
