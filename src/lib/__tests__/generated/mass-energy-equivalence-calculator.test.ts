// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMass_energy_equivalence_calculator,
  type Mass_energy_equivalence_calculatorInput,
} from "@generated/mass-energy-equivalence-calculator";

describe("mass-energy-equivalence-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mass_kg": 1,
    "mass_g": 0,
    "mass_lb": 0,
    "mass_oz": 0
  } as unknown as Mass_energy_equivalence_calculatorInput;
    const result = calculateMass_energy_equivalence_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
