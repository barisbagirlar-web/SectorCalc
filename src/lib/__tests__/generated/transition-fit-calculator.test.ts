// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTransition_fit_calculator,
  type Transition_fit_calculatorInput,
} from "@generated/transition-fit-calculator";

describe("transition-fit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "basic-size": 50,
    "hole-upper-dev": 0.025,
    "hole-lower-dev": 0,
    "shaft-upper-dev": 0.018,
    "shaft-lower-dev": 0.002
  } as unknown as Transition_fit_calculatorInput;
    const result = calculateTransition_fit_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
