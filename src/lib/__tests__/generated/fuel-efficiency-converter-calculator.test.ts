// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFuel_efficiency_converter_calculator,
  type Fuel_efficiency_converter_calculatorInput,
} from "@generated/fuel-efficiency-converter-calculator";

describe("fuel-efficiency-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "distance": 100,
    "distanceUnit": 0,
    "fuelUsed": 10,
    "fuelUnit": 0,
    "outputUnit": 0
  } as Fuel_efficiency_converter_calculatorInput;
    const result = calculateFuel_efficiency_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
