// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRadiation_exposure_calculator,
  type Radiation_exposure_calculatorInput,
} from "@generated/radiation-exposure-calculator";

describe("radiation-exposure-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sourceActivity": 1,
    "gammaConstant": 0.5,
    "distance": 1,
    "shieldThickness": 0,
    "hvl": 1,
    "conversionFactor": 8.77
  } as unknown as Radiation_exposure_calculatorInput;
    const result = calculateRadiation_exposure_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
