// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVitamin_d_from_sun_calculator,
  type Vitamin_d_from_sun_calculatorInput,
} from "@generated/vitamin-d-from-sun-calculator";

describe("vitamin-d-from-sun-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "uv_index": 5,
    "exposure_time_minutes": 15,
    "body_area_percent": 10,
    "skin_type": 3,
    "cloud_cover_percent": 0
  } as unknown as Vitamin_d_from_sun_calculatorInput;
    const result = calculateVitamin_d_from_sun_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
