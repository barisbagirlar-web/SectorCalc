// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBaseball_launch_angle_calculator,
  type Baseball_launch_angle_calculatorInput,
} from "@generated/baseball-launch-angle-calculator";

describe("baseball-launch-angle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialVelocity": 42,
    "launchAngle": 30,
    "initialHeight": 1,
    "gravity": 9.81
  } as unknown as Baseball_launch_angle_calculatorInput;
    const result = calculateBaseball_launch_angle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
