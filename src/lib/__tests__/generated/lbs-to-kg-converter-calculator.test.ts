// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLbs_to_kg_converter_calculator,
  type Lbs_to_kg_converter_calculatorInput,
} from "@generated/lbs-to-kg-converter-calculator";

describe("lbs-to-kg-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mass_lbs": 0,
    "precision_level": 2,
    "rounding_method": "standard",
    "include_loss_analysis": false,
    "measurement_uncertainty_pct": 0.5,
    "material_type": "general"
  } as Lbs_to_kg_converter_calculatorInput;
    const result = calculateLbs_to_kg_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
