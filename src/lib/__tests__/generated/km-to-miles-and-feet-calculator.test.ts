// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateKm_to_miles_and_feet_calculator,
  type Km_to_miles_and_feet_calculatorInput,
} from "@generated/km-to-miles-and-feet-calculator";

describe("km-to-miles-and-feet-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "km_value": 1,
    "miles_factor": 0.621371,
    "feet_per_mile": 5280,
    "decimal_places": 2
  } as unknown as Km_to_miles_and_feet_calculatorInput;
    const result = calculateKm_to_miles_and_feet_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
