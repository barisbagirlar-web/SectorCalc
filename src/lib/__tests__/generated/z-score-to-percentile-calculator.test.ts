// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateZ_score_to_percentile_calculator,
  type Z_score_to_percentile_calculatorInput,
} from "@generated/z-score-to-percentile-calculator";

describe("z-score-to-percentile-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "zScore": 0,
    "mean": 0,
    "stdDev": 1,
    "rawScore": 0,
    "rawScoreProvided": 0,
    "tail": 0
  } as Z_score_to_percentile_calculatorInput;
    const result = calculateZ_score_to_percentile_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
