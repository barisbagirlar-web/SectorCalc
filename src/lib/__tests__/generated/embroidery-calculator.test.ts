// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEmbroidery_calculator,
  type Embroidery_calculatorInput,
} from "@generated/embroidery-calculator";

describe("embroidery-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "designWidth": 10,
    "designHeight": 10,
    "stitchDensity": 50,
    "threadFactor": 30,
    "threadCostPerMeter": 0.05,
    "machineEfficiency": 600,
    "laborHourlyRate": 15
  } as unknown as Embroidery_calculatorInput;
    const result = calculateEmbroidery_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
