// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTerabytes_to_petabytes_calculator,
  type Terabytes_to_petabytes_calculatorInput,
} from "@generated/terabytes-to-petabytes-calculator";

describe("terabytes-to-petabytes-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalRawTerabytes": 1,
    "raidUsableMultiplier": 1,
    "formatOverheadPercent": 5,
    "conversionFactor": 1000
  } as Terabytes_to_petabytes_calculatorInput;
    const result = calculateTerabytes_to_petabytes_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
