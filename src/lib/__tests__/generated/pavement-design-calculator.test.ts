// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePavement_design_calculator,
  type Pavement_design_calculatorInput,
} from "@generated/pavement-design-calculator";

describe("pavement-design-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "axleLoad": 80,
    "standardLoad": 80,
    "trafficCount": 500,
    "designLife": 20,
    "subgradeCBR": 5,
    "reliabilityFactor": 1
  } as unknown as Pavement_design_calculatorInput;
    const result = calculatePavement_design_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
