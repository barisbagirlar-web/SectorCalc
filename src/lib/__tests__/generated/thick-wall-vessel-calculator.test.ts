// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateThick_wall_vessel_calculator,
  type Thick_wall_vessel_calculatorInput,
} from "@generated/thick-wall-vessel-calculator";

describe("thick-wall-vessel-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "insideDiameter": 100,
    "outsideDiameter": 200,
    "internalPressure": 10,
    "externalPressure": 0,
    "yieldStrength": 250
  } as unknown as Thick_wall_vessel_calculatorInput;
    const result = calculateThick_wall_vessel_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
