// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEvm_cost_forecast_calculator,
  type Evm_cost_forecast_calculatorInput,
} from "@generated/evm-cost-forecast-calculator";

describe("evm-cost-forecast-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bac": 1000000,
    "ev": 400000,
    "ac": 500000,
    "pv": 600000,
    "percentComplete": 40,
    "cpi": 0.8,
    "spi": 0.67,
    "useWeightedMethod": false
  } as unknown as Evm_cost_forecast_calculatorInput;
    const result = calculateEvm_cost_forecast_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
