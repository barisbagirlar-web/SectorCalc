// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRendering_time_calculator,
  type Rendering_time_calculatorInput,
} from "@generated/rendering-time-calculator";

describe("rendering-time-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "resolutionWidth": 1920,
    "resolutionHeight": 1080,
    "samplesPerPixel": 128,
    "frameCount": 1,
    "renderEngineSpeed": 1000000,
    "complexityFactor": 1,
    "maxBounces": 4
  } as unknown as Rendering_time_calculatorInput;
    const result = calculateRendering_time_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
