// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRecipe_cost_check_calculator,
  type Recipe_cost_check_calculatorInput,
} from "@generated/recipe-cost-check-calculator";

describe("recipe-cost-check-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_cost_per_kg": 0,
    "recipe_yield_percent": 95,
    "labor_rate_per_hour": 25,
    "batch_size_kg": 100,
    "processing_time_minutes": 60,
    "energy_cost_per_kwh": 0.12,
    "energy_consumption_kwh": 50,
    "overhead_rate_percent": 20
  } as unknown as Recipe_cost_check_calculatorInput;
    const result = calculateRecipe_cost_check_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
