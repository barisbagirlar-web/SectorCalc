// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRoi_npv_calculator,
  type Roi_npv_calculatorInput,
} from "@generated/roi-npv-calculator";

describe("roi-npv-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initial_investment": 100000,
    "annual_cash_inflow": 30000,
    "annual_cash_outflow": 5000,
    "discount_rate": 10,
    "project_life_years": 5,
    "salvage_value": 10000,
    "inflation_rate": 2,
    "tax_rate": 25
  } as unknown as Roi_npv_calculatorInput;
    const result = calculateRoi_npv_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
