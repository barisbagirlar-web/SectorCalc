// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSpc_signal_delay_cost_calculator,
  type Spc_signal_delay_cost_calculatorInput,
} from "@generated/spc-signal-delay-cost-calculator";

describe("spc-signal-delay-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "process_cycle_time_min": 60,
    "units_per_cycle": 100,
    "defect_rate_out_of_control": 5,
    "defect_rate_in_control": 0.5,
    "cost_per_defect": 50,
    "detection_delay_hours": 4,
    "response_delay_hours": 2,
    "shift_hours": 8
  } as Spc_signal_delay_cost_calculatorInput;
    const result = calculateSpc_signal_delay_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
