// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWeld_volume_cost_calculator,
  type Weld_volume_cost_calculatorInput,
} from "@generated/weld-volume-cost-calculator";

describe("weld-volume-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "joint_type": "butt",
    "plate_thickness": 10,
    "root_face": 2,
    "root_gap": 3,
    "groove_angle": 60,
    "weld_length": 1000,
    "leg_length": 6,
    "weld_process": "SMAW"
  } as Weld_volume_cost_calculatorInput;
    const result = calculateWeld_volume_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
