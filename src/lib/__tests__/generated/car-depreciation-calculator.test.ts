// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCar_depreciation_calculator,
  type Car_depreciation_calculatorInput,
} from "@generated/car-depreciation-calculator";

describe("car-depreciation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 30000,
    "residualValuePercentage": 20,
    "currentAge": 5,
    "expectedLifespan": 15,
    "mileage": 100000,
    "expectedMileage": 300000
  } as Car_depreciation_calculatorInput;
    const result = calculateCar_depreciation_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
