// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDeciliters_to_cups_calculator,
  type Deciliters_to_cups_calculatorInput,
} from "@generated/deciliters-to-cups-calculator";

describe("deciliters-to-cups-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "deciliters": 1,
    "conversionFactor": 0.422675,
    "precision": 2,
    "batchSize": 1
  } as unknown as Deciliters_to_cups_calculatorInput;
    const result = calculateDeciliters_to_cups_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
