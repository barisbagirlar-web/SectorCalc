// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDairy_profit_detector_calculator,
  type Dairy_profit_detector_calculatorInput,
} from "@generated/dairy-profit-detector-calculator";

describe("dairy-profit-detector-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "milk_volume_liters": 10000,
    "fat_percentage": 3.5,
    "protein_percentage": 3.2,
    "selling_price_per_liter": 0.45,
    "production_cost_per_liter": 0.35,
    "waste_percentage": 2,
    "labor_hours_per_day": 40,
    "labor_rate_per_hour": 15
  } as Dairy_profit_detector_calculatorInput;
    const result = calculateDairy_profit_detector_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
