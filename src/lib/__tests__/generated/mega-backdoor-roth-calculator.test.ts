// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMega_backdoor_roth_calculator,
  type Mega_backdoor_roth_calculatorInput,
} from "@generated/mega-backdoor-roth-calculator";

describe("mega-backdoor-roth-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualSalary": 75000,
    "preTaxDeferral": 22500,
    "employerContribution": 4000,
    "age": 35,
    "overallLimit": 69000,
    "catchUpLimit": 7500
  } as unknown as Mega_backdoor_roth_calculatorInput;
    const result = calculateMega_backdoor_roth_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
