// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEmployee_stock_option_calculator,
  type Employee_stock_option_calculatorInput,
} from "@generated/employee-stock-option-calculator";

describe("employee-stock-option-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "strikePrice": 10,
    "currentPrice": 15,
    "numberOfOptions": 1000,
    "vestedOptions": 500
  } as unknown as Employee_stock_option_calculatorInput;
    const result = calculateEmployee_stock_option_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
