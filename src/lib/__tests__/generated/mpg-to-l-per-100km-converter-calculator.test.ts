// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMpg_to_l_per_100km_converter_calculator,
  type Mpg_to_l_per_100km_converter_calculatorInput,
} from "@generated/mpg-to-l-per-100km-converter-calculator";

describe("mpg-to-l-per-100km-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "mpg_value": 25,
    "fuel_type": "gasoline",
    "driving_cycle": "combined",
    "annual_mileage": 12000,
    "fuel_price_per_gallon": 3.5,
    "co2_per_gallon": 8.887
  } as unknown as Mpg_to_l_per_100km_converter_calculatorInput;
    const result = calculateMpg_to_l_per_100km_converter_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
