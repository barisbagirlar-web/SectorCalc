// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressor_energy_cost_calculator,
  type Compressor_energy_cost_calculatorInput,
} from "@generated/compressor-energy-cost-calculator";

describe("compressor-energy-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "compressor_power_rating": 75,
    "motor_efficiency": 92,
    "compressor_type": "rotary_screw",
    "operating_hours_per_year": 8000,
    "load_factor": 70,
    "electricity_cost_per_kwh": 0.12,
    "leakage_percentage": 20,
    "pressure_setpoint": 7
  } as unknown as Compressor_energy_cost_calculatorInput;
    const result = calculateCompressor_energy_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
