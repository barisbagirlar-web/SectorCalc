// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCapacity_analysis_calculator,
  type Capacity_analysis_calculatorInput,
} from "@generated/capacity-analysis-calculator";

describe("capacity-analysis-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "numMachines": 10,
    "hoursPerDay": 8,
    "daysPerMonth": 22,
    "efficiency": 85,
    "productDemand": 10000,
    "cycleTime": 1.5
  } as Capacity_analysis_calculatorInput;
    const result = calculateCapacity_analysis_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
