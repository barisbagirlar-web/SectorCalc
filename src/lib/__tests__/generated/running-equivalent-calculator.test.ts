// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRunning_equivalent_calculator,
  type Running_equivalent_calculatorInput,
} from "@generated/running-equivalent-calculator";

describe("running-equivalent-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "knownDistance": 5,
    "knownTimeMinutes": 25,
    "knownTimeSeconds": 0,
    "targetDistance": 10,
    "exponent": 1.06
  } as unknown as Running_equivalent_calculatorInput;
    const result = calculateRunning_equivalent_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
