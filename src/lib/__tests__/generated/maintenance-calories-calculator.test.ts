// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMaintenance_calories_calculator,
  type Maintenance_calories_calculatorInput,
} from "@generated/maintenance-calories-calculator";

describe("maintenance-calories-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "gender": 1,
    "age": 30,
    "weight": 70,
    "height": 170,
    "activityLevel": 1.55
  } as unknown as Maintenance_calories_calculatorInput;
    const result = calculateMaintenance_calories_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
