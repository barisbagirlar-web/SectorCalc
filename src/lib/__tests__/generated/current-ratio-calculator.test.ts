// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCurrent_ratio_calculator,
  type Current_ratio_calculatorInput,
} from "@generated/current-ratio-calculator";

describe("current-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cash": 0,
    "receivables": 0,
    "inventory": 0,
    "otherCurrentAssets": 0,
    "payables": 0,
    "shortTermDebt": 0,
    "otherCurrentLiabilities": 0
  } as unknown as Current_ratio_calculatorInput;
    const result = calculateCurrent_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
