// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCrop_yield_loss_analyzer_calculator,
  type Crop_yield_loss_analyzer_calculatorInput,
} from "@generated/crop-yield-loss-analyzer-calculator";

describe("crop-yield-loss-analyzer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "expected_yield": 8000,
    "actual_yield": 6500,
    "harvest_loss_pct": 5,
    "post_harvest_loss_pct": 3,
    "field_condition": "good",
    "irrigation_type": "drip",
    "pest_pressure": 3,
    "disease_index": 2
  } as unknown as Crop_yield_loss_analyzer_calculatorInput;
    const result = calculateCrop_yield_loss_analyzer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
