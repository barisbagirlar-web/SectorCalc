// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMortgage_refinance_calculator,
  type Mortgage_refinance_calculatorInput,
} from "@generated/mortgage-refinance-calculator";

describe("mortgage-refinance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentBalance": 200000,
    "currentRate": 5.5,
    "currentTermRemaining": 240,
    "newRate": 4,
    "newTerm": 180,
    "closingCosts": 3000
  } as unknown as Mortgage_refinance_calculatorInput;
    const result = calculateMortgage_refinance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
