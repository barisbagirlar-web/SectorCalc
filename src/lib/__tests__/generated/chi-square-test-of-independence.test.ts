// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateChi_square_test_of_independence,
  type Chi_square_test_of_independenceInput,
} from "@generated/chi-square-test-of-independence";

describe("chi-square-test-of-independence", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "observed_11": 10,
    "observed_12": 20,
    "observed_21": 30,
    "observed_22": 40
  } as Chi_square_test_of_independenceInput;
    const result = calculateChi_square_test_of_independence(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
