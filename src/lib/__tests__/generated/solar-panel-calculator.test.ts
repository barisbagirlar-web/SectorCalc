// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSolar_panel_calculator,
  type Solar_panel_calculatorInput,
} from "@generated/solar-panel-calculator";

describe("solar-panel-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "panelArea": 1.6,
    "panelEfficiency": 18,
    "solarIrradiance": 1000,
    "systemLosses": 14,
    "tiltAngle": 30,
    "azimuthAngle": 0,
    "latitude": 41,
    "annualSunHours": 2000
  } as Solar_panel_calculatorInput;
    const result = calculateSolar_panel_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
