// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRenewable_energy_roi_calculator,
  type Renewable_energy_roi_calculatorInput,
} from "@generated/renewable-energy-roi-calculator";

describe("renewable-energy-roi-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "system_capacity_kw": 100,
    "annual_energy_output_kwh": 150000,
    "capital_cost_usd": 250000,
    "annual_operating_cost_usd": 5000,
    "electricity_price_usd_per_kwh": 0.12,
    "inflation_rate_percent": 2.5,
    "discount_rate_percent": 8,
    "system_lifetime_years": 25
  } as Renewable_energy_roi_calculatorInput;
    const result = calculateRenewable_energy_roi_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
