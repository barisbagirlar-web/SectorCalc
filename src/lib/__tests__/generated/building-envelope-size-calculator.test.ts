// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBuilding_envelope_size_calculator,
  type Building_envelope_size_calculatorInput,
} from "@generated/building-envelope-size-calculator";

describe("building-envelope-size-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length": 20,
    "width": 10,
    "height": 3,
    "pitch": 30
  } as unknown as Building_envelope_size_calculatorInput;
    const result = calculateBuilding_envelope_size_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
