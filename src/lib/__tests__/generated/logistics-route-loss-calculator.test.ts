// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLogistics_route_loss_calculator,
  type Logistics_route_loss_calculatorInput,
} from "@generated/logistics-route-loss-calculator";

describe("logistics-route-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "route_distance_km": 100,
    "average_speed_kmh": 60,
    "planned_stops": 3,
    "average_stop_time_min": 30,
    "traffic_delay_factor": 0.15,
    "fuel_cost_per_km": 0.35,
    "driver_wage_per_hour": 25,
    "vehicle_operating_cost_per_km": 0.15
  } as Logistics_route_loss_calculatorInput;
    const result = calculateLogistics_route_loss_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
