// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBeer_barrels_to_liters_calculator,
  type Beer_barrels_to_liters_calculatorInput,
} from "@generated/beer-barrels-to-liters-calculator";

describe("beer-barrels-to-liters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "barrels": 1,
    "litersPerBarrel": 117.3478,
    "temperatureC": 20,
    "referenceTemperatureC": 20,
    "thermalExpansionCoefficient": 0.0003,
    "correctionFactor": 1
  } as unknown as Beer_barrels_to_liters_calculatorInput;
    const result = calculateBeer_barrels_to_liters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
