// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProduct_complexity_hidden_cost_calculator,
  type Product_complexity_hidden_cost_calculatorInput,
} from "@generated/product-complexity-hidden-cost-calculator";

describe("product-complexity-hidden-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_sku": 100,
    "avg_volume_per_sku": 5000,
    "num_components": 500,
    "avg_bom_levels": 3,
    "setup_time_minutes": 45,
    "labor_rate": 25,
    "overhead_rate": 150,
    "complexity_type": "mixed"
  } as Product_complexity_hidden_cost_calculatorInput;
    const result = calculateProduct_complexity_hidden_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
