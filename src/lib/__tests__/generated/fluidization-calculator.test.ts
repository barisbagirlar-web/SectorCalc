// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFluidization_calculator,
  type Fluidization_calculatorInput,
} from "@generated/fluidization-calculator";

describe("fluidization-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "particleDiameter": 0.001,
    "particleDensity": 2500,
    "fluidDensity": 1.2,
    "fluidViscosity": 0.000018,
    "voidFraction": 0.4,
    "sphericity": 0.8,
    "gravity": 9.81,
    "bedHeight": 0.5
  } as unknown as Fluidization_calculatorInput;
    const result = calculateFluidization_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
