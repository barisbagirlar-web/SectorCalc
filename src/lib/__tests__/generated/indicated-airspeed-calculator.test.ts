// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIndicated_airspeed_calculator,
  type Indicated_airspeed_calculatorInput,
} from "@generated/indicated-airspeed-calculator";

describe("indicated-airspeed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pitotPressure": 102925,
    "staticPressure": 101325,
    "airDensity": 1.225,
    "positionErrorCorrection": 1
  } as unknown as Indicated_airspeed_calculatorInput;
    const result = calculateIndicated_airspeed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
