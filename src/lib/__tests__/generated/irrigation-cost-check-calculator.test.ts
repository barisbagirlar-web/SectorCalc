// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateIrrigation_cost_check_calculator,
  type Irrigation_cost_check_calculatorInput,
} from "@generated/irrigation-cost-check-calculator";

describe("irrigation-cost-check-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "water_volume": 10000,
    "water_cost_per_m3": 0.5,
    "energy_cost_per_kwh": 0.12,
    "pump_efficiency": 70,
    "distribution_losses": 15,
    "labor_hours_per_year": 500,
    "labor_rate": 25,
    "maintenance_cost": 2000
  } as unknown as Irrigation_cost_check_calculatorInput;
    const result = calculateIrrigation_cost_check_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
