// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRelative_humidity_calculator,
  type Relative_humidity_calculatorInput,
} from "@generated/relative-humidity-calculator";

describe("relative-humidity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "dryBulbTemp": 25,
    "wetBulbTemp": 20,
    "atmosphericPressure": 1013.25,
    "psychrometricConstant": 0.00066
  } as Relative_humidity_calculatorInput;
    const result = calculateRelative_humidity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
