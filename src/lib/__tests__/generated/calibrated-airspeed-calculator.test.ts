// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalibrated_airspeed_calculator,
  type Calibrated_airspeed_calculatorInput,
} from "@generated/calibrated-airspeed-calculator";

describe("calibrated-airspeed-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalPressure": 101325,
    "staticPressure": 101325,
    "referencePressure": 101325,
    "airDensity": 1.225,
    "gamma": 1.4
  } as Calibrated_airspeed_calculatorInput;
    const result = calculateCalibrated_airspeed_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
