// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCbam_exposure_check_calculator,
  type Cbam_exposure_check_calculatorInput,
} from "@generated/cbam-exposure-check-calculator";

describe("cbam-exposure-check-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_production_volume": 100000,
    "carbon_intensity": 1.8,
    "cbam_carbon_price": 90,
    "free_allocation_percentage": 20,
    "production_efficiency_index": 0.85,
    "compliance_readiness_score": 60,
    "industry_benchmark_intensity": 1.2,
    "emission_scope": "Scope 1 & 2"
  } as unknown as Cbam_exposure_check_calculatorInput;
    const result = calculateCbam_exposure_check_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
