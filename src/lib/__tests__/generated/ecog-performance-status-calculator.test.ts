// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEcog_performance_status_calculator,
  type Ecog_performance_status_calculatorInput,
} from "@generated/ecog-performance-status-calculator";

describe("ecog-performance-status-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "vibrationLevel": 2.5,
    "temperature": 60,
    "runtimeHours": 2000,
    "oilQuality": 80,
    "noiseLevel": 85
  } as Ecog_performance_status_calculatorInput;
    const result = calculateEcog_performance_status_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
