// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDifferential_amplifier_calculator,
  type Differential_amplifier_calculatorInput,
} from "@generated/differential-amplifier-calculator";

describe("differential-amplifier-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "V1": 1,
    "V2": 2,
    "R1": 10000,
    "R2": 10000,
    "R3": 10000,
    "R4": 10000
  } as unknown as Differential_amplifier_calculatorInput;
    const result = calculateDifferential_amplifier_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
