// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSeed_rate_calculator,
  type Seed_rate_calculatorInput,
} from "@generated/seed-rate-calculator";

describe("seed-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "target_population": 75000,
    "germination_rate": 95,
    "field_emergence_factor": 90,
    "seed_weight": 250,
    "row_spacing": 50,
    "planter_efficiency": 95,
    "seed_cost_per_unit": 5,
    "expected_yield_value": 200
  } as Seed_rate_calculatorInput;
    const result = calculateSeed_rate_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
