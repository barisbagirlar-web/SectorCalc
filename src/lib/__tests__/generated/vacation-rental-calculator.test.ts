// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVacation_rental_calculator,
  type Vacation_rental_calculatorInput,
} from "@generated/vacation-rental-calculator";

describe("vacation-rental-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "nightlyRate": 150,
    "numberOfNights": 5,
    "cleaningFee": 50,
    "serviceFeePercent": 10,
    "taxRate": 8,
    "discountPercent": 0
  } as Vacation_rental_calculatorInput;
    const result = calculateVacation_rental_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
