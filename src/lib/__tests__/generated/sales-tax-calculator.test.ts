// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSales_tax_calculator,
  type Sales_tax_calculatorInput,
} from "@generated/sales-tax-calculator";

describe("sales-tax-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "net_sales": 10000,
    "tax_rate": 8.25,
    "exempt_ratio": 0,
    "tax_jurisdiction": "state_county",
    "use_sourcing": "origin",
    "include_shipping": true,
    "shipping_charge": 500
  } as unknown as Sales_tax_calculatorInput;
    const result = calculateSales_tax_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
