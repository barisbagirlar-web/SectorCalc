// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStere_to_cubic_meters_calculator,
  type Stere_to_cubic_meters_calculatorInput,
} from "@generated/stere-to-cubic-meters-calculator";

describe("stere-to-cubic-meters-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "quantity": 1,
    "conversionFactor": 1,
    "stackingFactor": 1,
    "pricePerStere": 0,
    "pricePerCubicMeter": 0,
    "roundingPrecision": 2
  } as unknown as Stere_to_cubic_meters_calculatorInput;
    const result = calculateStere_to_cubic_meters_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
