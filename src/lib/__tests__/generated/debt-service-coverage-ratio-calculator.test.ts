// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDebt_service_coverage_ratio_calculator,
  type Debt_service_coverage_ratio_calculatorInput,
} from "@generated/debt-service-coverage-ratio-calculator";

describe("debt-service-coverage-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netOperatingIncome": 120000,
    "loanAmount": 500000,
    "interestRate": 5,
    "loanTerm": 10
  } as unknown as Debt_service_coverage_ratio_calculatorInput;
    const result = calculateDebt_service_coverage_ratio_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
