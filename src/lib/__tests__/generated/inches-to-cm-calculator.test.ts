// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInches_to_cm_calculator,
  type Inches_to_cm_calculatorInput,
} from "@generated/inches-to-cm-calculator";

describe("inches-to-cm-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "inches": 1,
    "calibrationOffset": 0,
    "uncertaintyPercent": 0,
    "conversionFactor": 2.54,
    "toleranceMm": 0.5,
    "measurementTemperature": 20,
    "materialExpansionCoeff": 0.0000115,
    "referenceTemperature": 20
  } as unknown as Inches_to_cm_calculatorInput;
    const result = calculateInches_to_cm_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
