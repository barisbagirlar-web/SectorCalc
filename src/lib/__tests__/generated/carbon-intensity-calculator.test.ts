// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCarbon_intensity_calculator,
  type Carbon_intensity_calculatorInput,
} from "@generated/carbon-intensity-calculator";

describe("carbon-intensity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "electricityConsumption": 1000,
    "naturalGasConsumption": 500,
    "dieselConsumption": 100,
    "productionOutput": 1000,
    "electricityEmissionFactor": 0.5,
    "naturalGasEmissionFactor": 2,
    "dieselEmissionFactor": 2.7
  } as Carbon_intensity_calculatorInput;
    const result = calculateCarbon_intensity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
