// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSleep_cycle_calculator,
  type Sleep_cycle_calculatorInput,
} from "@generated/sleep-cycle-calculator";

describe("sleep-cycle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wake_time": 7,
    "sleep_debt_hours": 2,
    "cycle_length_minutes": 90,
    "fall_asleep_minutes": 15,
    "recovery_efficiency": 1,
    "shift_worker": false
  } as unknown as Sleep_cycle_calculatorInput;
    const result = calculateSleep_cycle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
