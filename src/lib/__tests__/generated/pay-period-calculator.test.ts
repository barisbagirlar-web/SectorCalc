// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePay_period_calculator,
  type Pay_period_calculatorInput,
} from "@generated/pay-period-calculator";

describe("pay-period-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualSalary": 30000,
    "payPeriodsPerYear": 12,
    "overtimeHours": 0,
    "overtimeRateMultiplier": 1.5,
    "hoursPerPeriod": 160,
    "deductions": 0,
    "taxRate": 20,
    "otherWithholdings": 0
  } as Pay_period_calculatorInput;
    const result = calculatePay_period_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
