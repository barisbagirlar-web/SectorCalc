// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCox_proportional_hazards_calculator,
  type Cox_proportional_hazards_calculatorInput,
} from "@generated/cox-proportional-hazards-calculator";

describe("cox-proportional-hazards-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "operatingHours": 1000,
    "temperature": 80,
    "vibration": 5,
    "maintenanceScore": 0.8,
    "beta_hours": 0.0001,
    "beta_temp": 0.03,
    "beta_vibration": 0.1,
    "beta_maintenance": -0.5
  } as unknown as Cox_proportional_hazards_calculatorInput;
    const result = calculateCox_proportional_hazards_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
