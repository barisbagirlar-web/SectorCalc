// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateNft_profit_calculator,
  type Nft_profit_calculatorInput,
} from "@generated/nft-profit-calculator";

describe("nft-profit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "purchasePrice": 1,
    "gasFee": 1,
    "listingFee": 1,
    "salePrice": 1,
    "royaltyPercent": 1
  } as unknown as Nft_profit_calculatorInput;
    const result = calculateNft_profit_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
