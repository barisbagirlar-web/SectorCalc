// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFactor_of_safety_calculator,
  type Factor_of_safety_calculatorInput,
} from "@generated/factor-of-safety-calculator";

describe("factor-of-safety-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "appliedForce": 10000,
    "crossSectionArea": 50,
    "yieldStrength": 250,
    "ultimateTensileStrength": 400,
    "designFactor": 1.5,
    "stressConcentrationFactor": 1
  } as unknown as Factor_of_safety_calculatorInput;
    const result = calculateFactor_of_safety_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
