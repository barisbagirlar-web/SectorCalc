// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFeet_to_yards_calculator,
  type Feet_to_yards_calculatorInput,
} from "@generated/feet-to-yards-calculator";

describe("feet-to-yards-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "length_feet": 1,
    "waste_percent": 0,
    "safety_margin_percent": 0,
    "quantity": 1,
    "cost_per_yard": 0,
    "decimal_places": 2
  } as unknown as Feet_to_yards_calculatorInput;
    const result = calculateFeet_to_yards_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
