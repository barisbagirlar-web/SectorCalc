// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHardwood_flooring_calculator,
  type Hardwood_flooring_calculatorInput,
} from "@generated/hardwood-flooring-calculator";

describe("hardwood-flooring-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "roomLength": 15,
    "roomWidth": 12,
    "plankLength": 48,
    "plankWidth": 6,
    "pricePerSqFt": 5,
    "wasteFactor": 10
  } as unknown as Hardwood_flooring_calculatorInput;
    const result = calculateHardwood_flooring_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
