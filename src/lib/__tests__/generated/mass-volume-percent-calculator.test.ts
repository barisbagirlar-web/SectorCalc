// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMass_volume_percent_calculator,
  type Mass_volume_percent_calculatorInput,
} from "@generated/mass-volume-percent-calculator";

describe("mass-volume-percent-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "massSolute_g": 0,
    "massSolute_kg": 0,
    "volumeSolution_mL": 0,
    "volumeSolution_L": 0
  } as Mass_volume_percent_calculatorInput;
    const result = calculateMass_volume_percent_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
