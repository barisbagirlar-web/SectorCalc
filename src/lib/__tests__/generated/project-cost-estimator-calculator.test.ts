// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProject_cost_estimator_calculator,
  type Project_cost_estimator_calculatorInput,
} from "@generated/project-cost-estimator-calculator";

describe("project-cost-estimator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "labor_hours": 1000,
    "labor_rate": 45,
    "material_cost": 50000,
    "equipment_cost": 15000,
    "overhead_percentage": 15,
    "complexity_factor": "medium",
    "quality_level": "3",
    "use_lean_standardization": false
  } as unknown as Project_cost_estimator_calculatorInput;
    const result = calculateProject_cost_estimator_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
