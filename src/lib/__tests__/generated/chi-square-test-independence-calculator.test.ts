// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateChi_square_test_independence_calculator,
  type Chi_square_test_independence_calculatorInput,
} from "@generated/chi-square-test-independence-calculator";

describe("chi-square-test-independence-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "a": 10,
    "b": 10,
    "c": 10,
    "d": 10
  } as Chi_square_test_independence_calculatorInput;
    const result = calculateChi_square_test_independence_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
