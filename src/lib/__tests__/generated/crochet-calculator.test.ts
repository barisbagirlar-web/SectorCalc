// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCrochet_calculator,
  type Crochet_calculatorInput,
} from "@generated/crochet-calculator";

describe("crochet-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "projectWidth": 100,
    "projectHeight": 150,
    "swatchWidth": 10,
    "swatchHeight": 10,
    "swatchWeight": 10,
    "skeinWeight": 50,
    "skeinLength": 100,
    "yarnCostPerSkein": 5
  } as unknown as Crochet_calculatorInput;
    const result = calculateCrochet_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
