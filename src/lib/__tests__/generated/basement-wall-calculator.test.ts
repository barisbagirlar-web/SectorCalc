// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBasement_wall_calculator,
  type Basement_wall_calculatorInput,
} from "@generated/basement-wall-calculator";

describe("basement-wall-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wall_length": 10,
    "wall_height": 3,
    "wall_thickness": 0.3,
    "concrete_strength": 25,
    "steel_ratio": 1.5,
    "soil_pressure": 30
  } as unknown as Basement_wall_calculatorInput;
    const result = calculateBasement_wall_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
