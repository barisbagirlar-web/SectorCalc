// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAtomic_mass_to_kg_calculator,
  type Atomic_mass_to_kg_calculatorInput,
} from "@generated/atomic-mass-to-kg-calculator";

describe("atomic-mass-to-kg-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "atomicMassU": 1,
    "numberOfEntities": 6.02214076e+23,
    "conversionFactor": 1.6605390666e-27,
    "batchSize": 1,
    "outputUnitMultiplier": 1
  } as Atomic_mass_to_kg_calculatorInput;
    const result = calculateAtomic_mass_to_kg_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
