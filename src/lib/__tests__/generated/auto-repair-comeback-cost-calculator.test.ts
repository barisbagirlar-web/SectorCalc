// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAuto_repair_comeback_cost_calculator,
  type Auto_repair_comeback_cost_calculatorInput,
} from "@generated/auto-repair-comeback-cost-calculator";

describe("auto-repair-comeback-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_repair_orders": 500,
    "comeback_count": 25,
    "avg_labor_rate": 85,
    "avg_hours_per_comeback": 2.5,
    "parts_cost_per_comeback": 45,
    "customer_lifetime_value": 2500,
    "lost_customer_rate": 15,
    "shop_type": "independent"
  } as unknown as Auto_repair_comeback_cost_calculatorInput;
    const result = calculateAuto_repair_comeback_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
