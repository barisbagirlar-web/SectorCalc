// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDiscounted_payback_period_calculator,
  type Discounted_payback_period_calculatorInput,
} from "@generated/discounted-payback-period-calculator";

describe("discounted-payback-period-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 10000,
    "discountRate": 10,
    "annualCashFlow": 3000,
    "maxYears": 50
  } as unknown as Discounted_payback_period_calculatorInput;
    const result = calculateDiscounted_payback_period_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
