// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSplit_bill_with_tip_calculator,
  type Split_bill_with_tip_calculatorInput,
} from "@generated/split-bill-with-tip-calculator";

describe("split-bill-with-tip-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "billAmount": 100,
    "tipPercentage": 15,
    "taxPercentage": 8,
    "numberOfPeople": 2
  } as unknown as Split_bill_with_tip_calculatorInput;
    const result = calculateSplit_bill_with_tip_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
