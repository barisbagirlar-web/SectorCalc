// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVariable_annuity_calculator,
  type Variable_annuity_calculatorInput,
} from "@generated/variable-annuity-calculator";

describe("variable-annuity-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialBalance": 0,
    "annualContribution": 10000,
    "years": 20,
    "annualReturn": 7,
    "annualFee": 1
  } as unknown as Variable_annuity_calculatorInput;
    const result = calculateVariable_annuity_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
