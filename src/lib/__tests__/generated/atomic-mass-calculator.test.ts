// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAtomic_mass_calculator,
  type Atomic_mass_calculatorInput,
} from "@generated/atomic-mass-calculator";

describe("atomic-mass-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "protonCount": 6,
    "neutronCount": 6,
    "electronCount": 6,
    "protonMass": 1.007276,
    "neutronMass": 1.008665,
    "electronMass": 0.00054858,
    "bindingEnergy": 0
  } as unknown as Atomic_mass_calculatorInput;
    const result = calculateAtomic_mass_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
