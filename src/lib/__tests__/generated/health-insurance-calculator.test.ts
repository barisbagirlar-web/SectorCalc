// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHealth_insurance_calculator,
  type Health_insurance_calculatorInput,
} from "@generated/health-insurance-calculator";

describe("health-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "age": 30,
    "sumInsured": 500000,
    "familyMembers": 1,
    "smokerStatus": 0,
    "preExisting": 0,
    "occupationRisk": 1
  } as Health_insurance_calculatorInput;
    const result = calculateHealth_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
