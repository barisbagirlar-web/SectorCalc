// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCarbon_footprint_check_calculator,
  type Carbon_footprint_check_calculatorInput,
} from "@generated/carbon-footprint-check-calculator";

describe("carbon-footprint-check-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "energy_consumption": 100000,
    "energy_source": "grid_mix",
    "fuel_consumption": 50000,
    "waste_generated": 200,
    "recycling_rate": 30,
    "production_volume": 50000,
    "transport_distance": 500,
    "has_carbon_offset_program": false
  } as Carbon_footprint_check_calculatorInput;
    const result = calculateCarbon_footprint_check_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
