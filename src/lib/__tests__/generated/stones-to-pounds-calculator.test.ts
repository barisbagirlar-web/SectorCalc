// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateStones_to_pounds_calculator,
  type Stones_to_pounds_calculatorInput,
} from "@generated/stones-to-pounds-calculator";

describe("stones-to-pounds-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "stones": 0,
    "adjustmentFactor": 14,
    "additionalWeight": 0,
    "subtractWeight": 0,
    "ounces": 0,
    "precision": 2
  } as Stones_to_pounds_calculatorInput;
    const result = calculateStones_to_pounds_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
