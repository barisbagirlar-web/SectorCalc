// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSewing_line_balancer_calculator,
  type Sewing_line_balancer_calculatorInput,
} from "@generated/sewing-line-balancer-calculator";

describe("sewing-line-balancer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_work_content": 3600,
    "number_of_operators": 20,
    "takt_time": 180,
    "bottleneck_time": 200,
    "line_balance_type": "straight",
    "allow_rebalancing": true
  } as unknown as Sewing_line_balancer_calculatorInput;
    const result = calculateSewing_line_balancer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
