// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDowntime_cost_calculator,
  type Downtime_cost_calculatorInput,
} from "@generated/downtime-cost-calculator";

describe("downtime-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "planned_production_rate": 100,
    "downtime_duration": 2,
    "revenue_per_unit": 50,
    "direct_labor_cost_per_hour": 30,
    "energy_cost_per_hour": 15,
    "scrap_rate_during_downtime": 5,
    "recovery_time_factor": 0.3,
    "shift_type": "day"
  } as unknown as Downtime_cost_calculatorInput;
    const result = calculateDowntime_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
