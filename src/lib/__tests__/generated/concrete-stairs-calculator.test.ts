// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConcrete_stairs_calculator,
  type Concrete_stairs_calculatorInput,
} from "@generated/concrete-stairs-calculator";

describe("concrete-stairs-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalRise": 3000,
    "numberOfSteps": 15,
    "treadDepth": 280,
    "stepWidth": 1000,
    "waistThickness": 150,
    "landingLength": 0
  } as unknown as Concrete_stairs_calculatorInput;
    const result = calculateConcrete_stairs_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
