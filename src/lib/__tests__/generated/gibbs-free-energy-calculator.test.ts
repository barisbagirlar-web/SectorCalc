// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGibbs_free_energy_calculator,
  type Gibbs_free_energy_calculatorInput,
} from "@generated/gibbs-free-energy-calculator";

describe("gibbs-free-energy-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "temperature": 298.15,
    "gasConstant": 0.008314,
    "standardDeltaG": 0,
    "reactionQuotient": 1
  } as unknown as Gibbs_free_energy_calculatorInput;
    const result = calculateGibbs_free_energy_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
