// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOperating_margin_calculator,
  type Operating_margin_calculatorInput,
} from "@generated/operating-margin-calculator";

describe("operating-margin-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "revenue": 500000,
    "cogs": 300000,
    "opex": 100000,
    "otherIncome": 1
  } as unknown as Operating_margin_calculatorInput;
    const result = calculateOperating_margin_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
