// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSteam_trap_energy_loss_calculator,
  type Steam_trap_energy_loss_calculatorInput,
} from "@generated/steam-trap-energy-loss-calculator";

describe("steam-trap-energy-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "orifice_diameter": 3,
    "steam_pressure": 7,
    "operating_hours_per_year": 8000,
    "steam_cost": 25,
    "trap_type": "Float & Thermostatic",
    "failure_mode": "Blow-through (fully open)",
    "condensate_recovery": true
  } as unknown as Steam_trap_energy_loss_calculatorInput;
    const result = calculateSteam_trap_energy_loss_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
