// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateProrated_property_tax_calculator,
  type Prorated_property_tax_calculatorInput,
} from "@generated/prorated-property-tax-calculator";

describe("prorated-property-tax-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualPropertyTax": 0,
    "daysInTaxYear": 365,
    "daysOwned": 0,
    "otherFees": 0
  } as Prorated_property_tax_calculatorInput;
    const result = calculateProrated_property_tax_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
