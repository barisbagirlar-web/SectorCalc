// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateInverse_laplace_calculator,
  type Inverse_laplace_calculatorInput,
} from "@generated/inverse-laplace-calculator";

describe("inverse-laplace-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "F_s": 1,
    "numerator_order": 0,
    "denominator_coeffs": "1,2,1",
    "denominator_order": 1,
    "time_value": 1,
    "damping_ratio": 0.5,
    "natural_frequency": 1
  } as unknown as Inverse_laplace_calculatorInput;
    const result = calculateInverse_laplace_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
