// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWarehouse_layout_optimizer_calculator,
  type Warehouse_layout_optimizer_calculatorInput,
} from "@generated/warehouse-layout-optimizer-calculator";

describe("warehouse-layout-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_floor_area": 100000,
    "storage_area_percent": 60,
    "aisle_width": 12,
    "pallet_flow_type": "single_deep",
    "avg_pallet_size": 13.75,
    "avg_inventory_turns": 6,
    "labor_cost_per_hour": 25,
    "equipment_cost_per_hour": 15
  } as unknown as Warehouse_layout_optimizer_calculatorInput;
    const result = calculateWarehouse_layout_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
