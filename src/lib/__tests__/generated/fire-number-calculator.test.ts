// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFire_number_calculator,
  type Fire_number_calculatorInput,
} from "@generated/fire-number-calculator";

describe("fire-number-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualExpenses": 60000,
    "withdrawalRate": 4,
    "currentSavings": 0,
    "annualSavings": 20000,
    "expectedReturn": 7,
    "inflationRate": 2
  } as unknown as Fire_number_calculatorInput;
    const result = calculateFire_number_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
