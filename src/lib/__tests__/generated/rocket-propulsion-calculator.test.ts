// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRocket_propulsion_calculator,
  type Rocket_propulsion_calculatorInput,
} from "@generated/rocket-propulsion-calculator";

describe("rocket-propulsion-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialMass": 50000,
    "finalMass": 10000,
    "specificImpulse": 300,
    "gravitationalAcceleration": 9.81,
    "massFlowRate": 500
  } as Rocket_propulsion_calculatorInput;
    const result = calculateRocket_propulsion_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
