// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBallistic_pendulum_calculator,
  type Ballistic_pendulum_calculatorInput,
} from "@generated/ballistic-pendulum-calculator";

describe("ballistic-pendulum-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "projectile_mass": 0.01,
    "pendulum_mass": 2,
    "pendulum_length": 1.5,
    "angle": 30,
    "gravity": 9.81
  } as unknown as Ballistic_pendulum_calculatorInput;
    const result = calculateBallistic_pendulum_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
