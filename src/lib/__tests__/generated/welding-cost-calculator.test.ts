// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWelding_cost_calculator,
  type Welding_cost_calculatorInput,
} from "@generated/welding-cost-calculator";

describe("welding-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weld_length_mm": 100,
    "weld_throat_mm": 5,
    "material_density_g_per_cm3": 7.85,
    "deposition_efficiency": 85,
    "labor_rate_per_hour": 45,
    "welding_speed_mm_per_min": 300,
    "filler_wire_cost_per_kg": 3.5,
    "gas_cost_per_liter": 0.08
  } as unknown as Welding_cost_calculatorInput;
    const result = calculateWelding_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
