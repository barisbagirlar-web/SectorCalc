// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRental_income_calculator,
  type Rental_income_calculatorInput,
} from "@generated/rental-income-calculator";

describe("rental-income-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "monthlyRent": 1500,
    "occupancyRate": 95,
    "managementFeePercent": 10,
    "maintenanceMonthly": 100,
    "insuranceMonthly": 50,
    "propertyTaxAnnual": 2000
  } as Rental_income_calculatorInput;
    const result = calculateRental_income_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
