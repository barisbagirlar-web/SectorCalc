// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWeld_strength_calculator,
  type Weld_strength_calculatorInput,
} from "@generated/weld-strength-calculator";

describe("weld-strength-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weld_type": "fillet",
    "base_material_yield_strength": 345,
    "weld_material_tensile_strength": 480,
    "throat_thickness": 6,
    "weld_length": 100,
    "applied_load": 50,
    "load_angle": 90,
    "quality_level": "B"
  } as Weld_strength_calculatorInput;
    const result = calculateWeld_strength_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
