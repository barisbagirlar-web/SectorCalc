// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDisability_insurance_calculator,
  type Disability_insurance_calculatorInput,
} from "@generated/disability-insurance-calculator";

describe("disability-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualIncome": 50000,
    "replacementRatio": 60,
    "benefitPeriodYears": 5,
    "waitingPeriodMonths": 3,
    "discountRate": 3,
    "inflationRate": 2
  } as unknown as Disability_insurance_calculatorInput;
    const result = calculateDisability_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
