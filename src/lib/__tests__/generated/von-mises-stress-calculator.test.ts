// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVon_mises_stress_calculator,
  type Von_mises_stress_calculatorInput,
} from "@generated/von-mises-stress-calculator";

describe("von-mises-stress-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sigma_x": 1,
    "sigma_y": 1,
    "tau_xy": 1,
    "yield_strength": 250
  } as unknown as Von_mises_stress_calculatorInput;
    const result = calculateVon_mises_stress_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
