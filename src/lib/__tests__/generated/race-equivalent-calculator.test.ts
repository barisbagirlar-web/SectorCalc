// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRace_equivalent_calculator,
  type Race_equivalent_calculatorInput,
} from "@generated/race-equivalent-calculator";

describe("race-equivalent-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "knownDistance": 10,
    "knownTimeHours": 0,
    "knownTimeMinutes": 50,
    "knownTimeSeconds": 0,
    "targetDistance": 42.195
  } as unknown as Race_equivalent_calculatorInput;
    const result = calculateRace_equivalent_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
