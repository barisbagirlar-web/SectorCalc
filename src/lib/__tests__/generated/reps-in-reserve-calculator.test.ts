// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateReps_in_reserve_calculator,
  type Reps_in_reserve_calculatorInput,
} from "@generated/reps-in-reserve-calculator";

describe("reps-in-reserve-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalLifespan": 10000,
    "currentUsage": 0,
    "dailyUsageRate": 100,
    "safetyFactor": 5,
    "plannedProduction": 5000
  } as Reps_in_reserve_calculatorInput;
    const result = calculateReps_in_reserve_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
