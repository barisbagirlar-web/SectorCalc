// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePayment_term_optimizer_calculator,
  type Payment_term_optimizer_calculatorInput,
} from "@generated/payment-term-optimizer-calculator";

describe("payment-term-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avg_invoice_value": 10000,
    "annual_invoice_count": 1200,
    "current_terms_days": 30,
    "proposed_terms_days": 15,
    "early_payment_discount_pct": 2,
    "discount_window_days": 10,
    "cost_of_capital_pct": 8,
    "customer_acceptance_rate_pct": 40
  } as unknown as Payment_term_optimizer_calculatorInput;
    const result = calculatePayment_term_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
