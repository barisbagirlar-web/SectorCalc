// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateElectricity_usage_calculator,
  type Electricity_usage_calculatorInput,
} from "@generated/electricity-usage-calculator";

describe("electricity-usage-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "power": 1,
    "hoursPerDay": 8,
    "daysPerMonth": 30,
    "numberOfDevices": 1,
    "costPerKwh": 0.15
  } as unknown as Electricity_usage_calculatorInput;
    const result = calculateElectricity_usage_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
