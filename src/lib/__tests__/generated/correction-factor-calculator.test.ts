// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCorrection_factor_calculator,
  type Correction_factor_calculatorInput,
} from "@generated/correction-factor-calculator";

describe("correction-factor-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "measuredVolume": 1000,
    "measuredTemperature": 20,
    "measuredPressure": 1.01325,
    "standardTemperature": 0,
    "standardPressure": 1.01325,
    "compressibilityFactor": 1
  } as unknown as Correction_factor_calculatorInput;
    const result = calculateCorrection_factor_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
