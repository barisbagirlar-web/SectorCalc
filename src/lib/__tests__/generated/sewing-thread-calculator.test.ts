// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSewing_thread_calculator,
  type Sewing_thread_calculatorInput,
} from "@generated/sewing-thread-calculator";

describe("sewing-thread-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "seamLength": 1,
    "stitchesPerCm": 4,
    "threadConsumptionPerStitch": 0.75,
    "numberOfGarments": 1,
    "wastePercentage": 5
  } as unknown as Sewing_thread_calculatorInput;
    const result = calculateSewing_thread_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
