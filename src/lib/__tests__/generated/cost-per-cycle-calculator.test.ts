// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCost_per_cycle_calculator,
  type Cost_per_cycle_calculatorInput,
} from "@generated/cost-per-cycle-calculator";

describe("cost-per-cycle-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "setupCost": 0,
    "cycleCount": 1,
    "materialCostPerCycle": 0,
    "laborCostPerHour": 0,
    "cycleTime": 0.01,
    "overheadRate": 0
  } as unknown as Cost_per_cycle_calculatorInput;
    const result = calculateCost_per_cycle_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
