// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDifferential_equation_solver_calculator,
  type Differential_equation_solver_calculatorInput,
} from "@generated/differential-equation-solver-calculator";

describe("differential-equation-solver-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "y0": 1,
    "a": 0.1,
    "b": 0,
    "t0": 0,
    "t": 1
  } as unknown as Differential_equation_solver_calculatorInput;
    const result = calculateDifferential_equation_solver_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
