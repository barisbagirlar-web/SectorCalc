// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEsg_score_calculator,
  type Esg_score_calculatorInput,
} from "@generated/esg-score-calculator";

describe("esg-score-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "environScore": 1,
    "socialScore": 1,
    "governScore": 1,
    "weightE": 0.33,
    "weightS": 0.33,
    "weightG": 0.34
  } as unknown as Esg_score_calculatorInput;
    const result = calculateEsg_score_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
