// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTaguchi_quality_loss_function_calculator,
  type Taguchi_quality_loss_function_calculatorInput,
} from "@generated/taguchi-quality-loss-function-calculator";

describe("taguchi-quality-loss-function-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "target_value": 10,
    "lower_spec_limit": 9.5,
    "upper_spec_limit": 10.5,
    "process_mean": 10.2,
    "process_std_dev": 0.15,
    "cost_at_limit": 50,
    "deviation_at_limit": 0.5,
    "production_volume": 10000
  } as Taguchi_quality_loss_function_calculatorInput;
    const result = calculateTaguchi_quality_loss_function_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
