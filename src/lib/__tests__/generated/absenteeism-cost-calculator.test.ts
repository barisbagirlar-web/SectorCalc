// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateAbsenteeism_cost_calculator,
  type Absenteeism_cost_calculatorInput,
} from "@generated/absenteeism-cost-calculator";

describe("absenteeism-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_employees": 500,
    "absenteeism_rate": 3.5,
    "avg_hourly_wage": 25,
    "avg_hours_per_day": 8,
    "working_days_per_year": 250,
    "replacement_cost_per_hour": 35,
    "overhead_multiplier": 1.3,
    "industry_type": "Manufacturing"
  } as unknown as Absenteeism_cost_calculatorInput;
    const result = calculateAbsenteeism_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
