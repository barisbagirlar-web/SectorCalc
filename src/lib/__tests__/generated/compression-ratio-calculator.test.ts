// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompression_ratio_calculator,
  type Compression_ratio_calculatorInput,
} from "@generated/compression-ratio-calculator";

describe("compression-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bore": 86,
    "stroke": 86,
    "combustionChamberVolume": 60,
    "gasketThickness": 1.2,
    "gasketBore": 87,
    "deckClearance": 0.5,
    "pistonDishVolume": 0
  } as unknown as Compression_ratio_calculatorInput;
    const result = calculateCompression_ratio_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
