// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSupplement_stack_calculator,
  type Supplement_stack_calculatorInput,
} from "@generated/supplement-stack-calculator";

describe("supplement-stack-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "supp1Amount": 5,
    "supp1Price": 30,
    "supp1ContainerServings": 30,
    "supp1DailyServings": 2,
    "supp2Amount": 3,
    "supp2Price": 25,
    "supp2ContainerServings": 60,
    "supp2DailyServings": 1
  } as Supplement_stack_calculatorInput;
    const result = calculateSupplement_stack_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
