// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDays_between_dates_calculator,
  type Days_between_dates_calculatorInput,
} from "@generated/days-between-dates-calculator";

describe("days-between-dates-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "startYear": 2024,
    "startMonth": 1,
    "startDay": 1,
    "endYear": 2024,
    "endMonth": 12,
    "endDay": 31
  } as Days_between_dates_calculatorInput;
    const result = calculateDays_between_dates_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
