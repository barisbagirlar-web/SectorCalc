// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSheet_pile_weight_cost_calculator,
  type Sheet_pile_weight_cost_calculatorInput,
} from "@generated/sheet-pile-weight-cost-calculator";

describe("sheet-pile-weight-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "pileWidth": 0.6,
    "pileLength": 12,
    "numberOfPiles": 100,
    "weightPerArea": 120,
    "costPerTon": 900,
    "wasteFactor": 5
  } as Sheet_pile_weight_cost_calculatorInput;
    const result = calculateSheet_pile_weight_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
