// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateRenters_insurance_calculator,
  type Renters_insurance_calculatorInput,
} from "@generated/renters-insurance-calculator";

describe("renters-insurance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "personalPropertyValue": 30000,
    "liabilityCoverage": 100000,
    "deductible": 1000,
    "locationRiskFactor": 1,
    "securityDiscount": 5
  } as Renters_insurance_calculatorInput;
    const result = calculateRenters_insurance_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
