// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCleaning_bid_optimizer_calculator,
  type Cleaning_bid_optimizer_calculatorInput,
} from "@generated/cleaning-bid-optimizer-calculator";

describe("cleaning-bid-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_sq_ft": 10000,
    "cleaning_frequency": 5,
    "labor_rate_per_hour": 15,
    "labor_burden_percent": 25,
    "productivity_sqft_per_hour": 2000,
    "material_cost_per_sqft": 0.02,
    "equipment_cost_per_sqft": 0.01,
    "overhead_percent": 15
  } as Cleaning_bid_optimizer_calculatorInput;
    const result = calculateCleaning_bid_optimizer_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
