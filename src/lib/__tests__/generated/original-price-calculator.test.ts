// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOriginal_price_calculator,
  type Original_price_calculatorInput,
} from "@generated/original-price-calculator";

describe("original-price-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "finalPrice": 100,
    "rawMaterialSurcharge": 5,
    "laborSurcharge": 3,
    "overheadPercentage": 15,
    "profitMarginPercentage": 20,
    "vatPercentage": 18
  } as Original_price_calculatorInput;
    const result = calculateOriginal_price_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
