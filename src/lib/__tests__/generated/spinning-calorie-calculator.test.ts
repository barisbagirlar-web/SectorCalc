// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSpinning_calorie_calculator,
  type Spinning_calorie_calculatorInput,
} from "@generated/spinning-calorie-calculator";

describe("spinning-calorie-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "duration_min": 30,
    "speed_kmh": 20,
    "resistance_level": 5,
    "weight_kg": 70,
    "age_years": 30
  } as unknown as Spinning_calorie_calculatorInput;
    const result = calculateSpinning_calorie_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
