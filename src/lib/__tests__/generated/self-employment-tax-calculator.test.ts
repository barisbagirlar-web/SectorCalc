// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSelf_employment_tax_calculator,
  type Self_employment_tax_calculatorInput,
} from "@generated/self-employment-tax-calculator";

describe("self-employment-tax-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netEarnings": 50000,
    "socialSecurityRate": 12.4,
    "medicareRate": 2.9,
    "socialSecurityWageBase": 160200,
    "additionalMedicareThreshold": 200000,
    "additionalMedicareRate": 0.9
  } as unknown as Self_employment_tax_calculatorInput;
    const result = calculateSelf_employment_tax_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
