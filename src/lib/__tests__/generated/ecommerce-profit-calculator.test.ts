// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEcommerce_profit_calculator,
  type Ecommerce_profit_calculatorInput,
} from "@generated/ecommerce-profit-calculator";

describe("ecommerce-profit-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "sellingPrice": 50,
    "costPrice": 30,
    "quantity": 100,
    "shippingCostPerUnit": 5,
    "marketingSpend": 200,
    "taxRate": 10
  } as unknown as Ecommerce_profit_calculatorInput;
    const result = calculateEcommerce_profit_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
