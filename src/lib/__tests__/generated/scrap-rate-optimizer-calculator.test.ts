// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateScrap_rate_optimizer_calculator,
  type Scrap_rate_optimizer_calculatorInput,
} from "@generated/scrap-rate-optimizer-calculator";

describe("scrap-rate-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "total_units_produced": 10000,
    "defective_units": 500,
    "rework_units": 200,
    "material_cost_per_unit": 5.5,
    "labor_cost_per_unit": 2.75,
    "overhead_cost_per_unit": 1.25,
    "process_stage": "final_assembly",
    "defect_type": "dimensional"
  } as unknown as Scrap_rate_optimizer_calculatorInput;
    const result = calculateScrap_rate_optimizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
