// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLed_savings_calculator,
  type Led_savings_calculatorInput,
} from "@generated/led-savings-calculator";

describe("led-savings-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentBulbWattage": 60,
    "ledBulbWattage": 10,
    "numberOfBulbs": 10,
    "dailyUsageHours": 8,
    "electricityRate": 0.12,
    "daysPerYear": 365,
    "currentBulbCost": 1,
    "ledBulbCost": 5
  } as Led_savings_calculatorInput;
    const result = calculateLed_savings_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
