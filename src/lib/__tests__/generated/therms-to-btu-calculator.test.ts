// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTherms_to_btu_calculator,
  type Therms_to_btu_calculatorInput,
} from "@generated/therms-to-btu-calculator";

describe("therms-to-btu-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "quantity": 1,
    "conversionFactor": 100000,
    "energyContentAdjustment": 0,
    "precision": 0,
    "outputUnitFactor": 1
  } as unknown as Therms_to_btu_calculatorInput;
    const result = calculateTherms_to_btu_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
