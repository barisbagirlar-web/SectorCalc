// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWater_usage_optimizer_calculator,
  type Water_usage_optimizer_calculatorInput,
} from "@generated/water-usage-optimizer-calculator";

describe("water-usage-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "totalWaterIn": 10000,
    "productionOutput": 50000,
    "processWater": 7000,
    "coolingWater": 2000,
    "wastewaterDischarge": 6000,
    "recycledWater": 1500,
    "leakageLosses": 500,
    "industryType": "general_manufacturing"
  } as unknown as Water_usage_optimizer_calculatorInput;
    const result = calculateWater_usage_optimizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
