// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBackdoor_roth_ira_calculator,
  type Backdoor_roth_ira_calculatorInput,
} from "@generated/backdoor-roth-ira-calculator";

describe("backdoor-roth-ira-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "traditionalIraBalance": 1000,
    "afterTaxBasis": 0,
    "conversionAmount": 6000,
    "marginalTaxRate": 24
  } as unknown as Backdoor_roth_ira_calculatorInput;
    const result = calculateBackdoor_roth_ira_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
