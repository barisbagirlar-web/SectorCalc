// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMotorcycle_loan_calculator,
  type Motorcycle_loan_calculatorInput,
} from "@generated/motorcycle-loan-calculator";

describe("motorcycle-loan-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "vehiclePrice": 200000,
    "downPayment": 40000,
    "tradeIn": 0,
    "interestRate": 15,
    "loanTerm": 36,
    "salesTaxRate": 18
  } as Motorcycle_loan_calculatorInput;
    const result = calculateMotorcycle_loan_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
