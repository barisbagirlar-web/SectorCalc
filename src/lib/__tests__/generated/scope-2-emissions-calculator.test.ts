// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateScope_2_emissions_calculator,
  type Scope_2_emissions_calculatorInput,
} from "@generated/scope-2-emissions-calculator";

describe("scope-2-emissions-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "electricityKwh": 0,
    "locationFactor": 0.5,
    "marketFactor": 0.4,
    "heatMwh": 0,
    "heatFactor": 200
  } as unknown as Scope_2_emissions_calculatorInput;
    const result = calculateScope_2_emissions_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
