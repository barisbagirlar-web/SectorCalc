// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateJulian_year_to_days_calculator,
  type Julian_year_to_days_calculatorInput,
} from "@generated/julian-year-to-days-calculator";

describe("julian-year-to-days-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "julianYears": 1,
    "daysPerYear": 365.25,
    "precision": 2,
    "roundingMode": 0
  } as unknown as Julian_year_to_days_calculatorInput;
    const result = calculateJulian_year_to_days_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
