// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEvent_carbon_footprint_calculator,
  type Event_carbon_footprint_calculatorInput,
} from "@generated/event-carbon-footprint-calculator";

describe("event-carbon-footprint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "attendees": 100,
    "travelDistance": 100,
    "travelEmissionFactor": 0.2,
    "venueEnergy": 500,
    "eventDays": 1,
    "wastePerAttendee": 0.5,
    "accommodationNights": 1
  } as unknown as Event_carbon_footprint_calculatorInput;
    const result = calculateEvent_carbon_footprint_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
