// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateGallon_to_liter_converter_calculator,
  type Gallon_to_liter_converter_calculatorInput,
} from "@generated/gallon-to-liter-converter-calculator";

describe("gallon-to-liter-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "containerCount": 1,
    "gallonsPerContainer": 1,
    "unitType": 1,
    "customFactor": 1,
    "wastePercentage": 1,
    "outputDecimals": 2
  } as unknown as Gallon_to_liter_converter_calculatorInput;
    const result = calculateGallon_to_liter_converter_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
