// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateQuarterly_compound_interest_calculator,
  type Quarterly_compound_interest_calculatorInput,
} from "@generated/quarterly-compound-interest-calculator";

describe("quarterly-compound-interest-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "principal": 1000,
    "annualInterestRate": 5,
    "years": 10,
    "compoundingFrequency": 4,
    "inflationRate": 2
  } as unknown as Quarterly_compound_interest_calculatorInput;
    const result = calculateQuarterly_compound_interest_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
