// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateConversion_rate_calculator,
  type Conversion_rate_calculatorInput,
} from "@generated/conversion-rate-calculator";

describe("conversion-rate-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rawMaterialInput": 1000,
    "finishedProductOutput": 900,
    "reworkOutput": 50,
    "scrap": 50
  } as unknown as Conversion_rate_calculatorInput;
    const result = calculateConversion_rate_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formuller henuz NaN uretebilir)
    // Gercek formul geldiginde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
