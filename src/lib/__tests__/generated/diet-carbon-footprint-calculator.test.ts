// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDiet_carbon_footprint_calculator,
  type Diet_carbon_footprint_calculatorInput,
} from "@generated/diet-carbon-footprint-calculator";

describe("diet-carbon-footprint-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "meat": 0.5,
    "poultry": 0.5,
    "dairy": 3,
    "plant": 5,
    "grains": 3,
    "foodWaste": 1
  } as Diet_carbon_footprint_calculatorInput;
    const result = calculateDiet_carbon_footprint_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
