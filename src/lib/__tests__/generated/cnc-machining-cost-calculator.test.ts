// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCnc_machining_cost_calculator,
  type Cnc_machining_cost_calculatorInput,
} from "@generated/cnc-machining-cost-calculator";

describe("cnc-machining-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_type": "aluminum_6061",
    "part_weight_kg": 0.5,
    "stock_volume_cm3": 200,
    "machining_time_min": 15,
    "setup_time_min": 60,
    "batch_size": 100,
    "machine_hourly_rate_usd": 85,
    "labor_hourly_rate_usd": 35
  } as unknown as Cnc_machining_cost_calculatorInput;
    const result = calculateCnc_machining_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
