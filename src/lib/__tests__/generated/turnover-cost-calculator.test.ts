// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTurnover_cost_calculator,
  type Turnover_cost_calculatorInput,
} from "@generated/turnover-cost-calculator";

describe("turnover-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "num_employees": 100,
    "annual_turnover_rate": 15,
    "avg_salary": 50000,
    "recruitment_cost_per_hire": 4000,
    "onboarding_cost_per_hire": 3000,
    "training_cost_per_hire": 5000,
    "lost_productivity_months": 3,
    "productivity_factor": 50
  } as unknown as Turnover_cost_calculatorInput;
    const result = calculateTurnover_cost_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
