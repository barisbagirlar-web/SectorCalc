// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateChristmas_club_calculator,
  type Christmas_club_calculatorInput,
} from "@generated/christmas-club-calculator";

describe("christmas-club-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "weeklyDeposit": 50,
    "numberOfWeeks": 50,
    "annualInterestRate": 2,
    "compoundingFrequency": 52,
    "startingBalance": 0
  } as Christmas_club_calculatorInput;
    const result = calculateChristmas_club_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
