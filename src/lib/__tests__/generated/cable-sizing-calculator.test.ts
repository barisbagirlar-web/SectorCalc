// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCable_sizing_calculator,
  type Cable_sizing_calculatorInput,
} from "@generated/cable-sizing-calculator";

describe("cable-sizing-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "loadCurrent": 10,
    "cableLength": 50,
    "supplyVoltage": 230,
    "voltageDropPercent": 3,
    "resistivity": 0.0172,
    "safetyFactor": 1.25,
    "ambientTempFactor": 1
  } as unknown as Cable_sizing_calculatorInput;
    const result = calculateCable_sizing_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
