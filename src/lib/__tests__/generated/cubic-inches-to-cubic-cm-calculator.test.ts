// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCubic_inches_to_cubic_cm_calculator,
  type Cubic_inches_to_cubic_cm_calculatorInput,
} from "@generated/cubic-inches-to-cubic-cm-calculator";

describe("cubic-inches-to-cubic-cm-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cubicInches": 0,
    "conversionFactor": 16.387064,
    "decimalPlaces": 2,
    "roundingMode": 0
  } as unknown as Cubic_inches_to_cubic_cm_calculatorInput;
    const result = calculateCubic_inches_to_cubic_cm_calculator(input);
    expect(result).toBeDefined();
    expect(typeof result.totalWasteCost).toBe("number");
    expect(Number.isFinite(result.totalWasteCost)).toBe(true);
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
