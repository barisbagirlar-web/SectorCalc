/**
 * S3/6 low-risk activation smoke oracles — route/render/input smoke only.
 * GENERATED by scripts/tool-activation/apply-s3-low-risk-activation-batch.mjs
 */
import { describe, expect, test } from "vitest";
import {
  buildDefaultSchemaInputs,
  runPremiumSchemaEngine,
} from "@/lib/premium-schema/premium-schema-engine";
import {
  getPremiumCalculatorSchema,
  getPremiumSchemaForPaidSlug,
} from "@/lib/premium-schema/schema-registry";

const S3_SMOKE_SLUGS = [
  "auto-shop-margin-leak-detector",
  "cabinet-cost-estimator",
  "cleaning-cost-calculator",
  "crop-yield-loss-analyzer",
  "dairy-profit-detector",
  "landscaping-contract-profit-tool",
  "lawn-care-cost-check",
  "meal-planning-verdict",
  "menu-profit-leak-detector",
  "millwork-bid-risk-analyzer",
  "office-cleaning-bid-optimizer",
  "painting-job-profit-verdict",
  "panel-shop-margin-verdict",
  "plumbing-job-margin-verdict",
  "print-job-cost-check",
  "product-margin-calculator",
  "project-cost-calculator",
  "recipe-cost-check",
  "roofing-contract-margin-guard",
  "roofing-square-cost-check",
  "welding-bid-risk-analyzer",
] as const;

describe("s3 low-risk activation smoke registry", () => {
  test("registry contains patched slugs", () => {
    expect(S3_SMOKE_SLUGS.length).toBeGreaterThan(0);
  });
});

describe("s3 activation smoke — auto-shop-margin-leak-detector", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("auto-shop-margin-leak-detector") ?? getPremiumSchemaForPaidSlug("auto-shop-margin-leak-detector");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("auto-shop-margin-leak-detector") ?? getPremiumSchemaForPaidSlug("auto-shop-margin-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("auto-shop-margin-leak-detector") ?? getPremiumSchemaForPaidSlug("auto-shop-margin-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("auto-shop-margin-leak-detector") ?? getPremiumSchemaForPaidSlug("auto-shop-margin-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — cabinet-cost-estimator", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("cabinet-cost-estimator") ?? getPremiumSchemaForPaidSlug("cabinet-cost-estimator");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("cabinet-cost-estimator") ?? getPremiumSchemaForPaidSlug("cabinet-cost-estimator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("cabinet-cost-estimator") ?? getPremiumSchemaForPaidSlug("cabinet-cost-estimator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("cabinet-cost-estimator") ?? getPremiumSchemaForPaidSlug("cabinet-cost-estimator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — cleaning-cost-calculator", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("cleaning-cost-calculator") ?? getPremiumSchemaForPaidSlug("cleaning-cost-calculator");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("cleaning-cost-calculator") ?? getPremiumSchemaForPaidSlug("cleaning-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("cleaning-cost-calculator") ?? getPremiumSchemaForPaidSlug("cleaning-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("cleaning-cost-calculator") ?? getPremiumSchemaForPaidSlug("cleaning-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — crop-yield-loss-analyzer", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("crop-yield-loss-analyzer") ?? getPremiumSchemaForPaidSlug("crop-yield-loss-analyzer");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("crop-yield-loss-analyzer") ?? getPremiumSchemaForPaidSlug("crop-yield-loss-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("crop-yield-loss-analyzer") ?? getPremiumSchemaForPaidSlug("crop-yield-loss-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("crop-yield-loss-analyzer") ?? getPremiumSchemaForPaidSlug("crop-yield-loss-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — dairy-profit-detector", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("dairy-profit-detector") ?? getPremiumSchemaForPaidSlug("dairy-profit-detector");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("dairy-profit-detector") ?? getPremiumSchemaForPaidSlug("dairy-profit-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("dairy-profit-detector") ?? getPremiumSchemaForPaidSlug("dairy-profit-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("dairy-profit-detector") ?? getPremiumSchemaForPaidSlug("dairy-profit-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — landscaping-contract-profit-tool", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("landscaping-contract-profit-tool") ?? getPremiumSchemaForPaidSlug("landscaping-contract-profit-tool");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("landscaping-contract-profit-tool") ?? getPremiumSchemaForPaidSlug("landscaping-contract-profit-tool");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("landscaping-contract-profit-tool") ?? getPremiumSchemaForPaidSlug("landscaping-contract-profit-tool");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("landscaping-contract-profit-tool") ?? getPremiumSchemaForPaidSlug("landscaping-contract-profit-tool");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — lawn-care-cost-check", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("lawn-care-cost-check") ?? getPremiumSchemaForPaidSlug("lawn-care-cost-check");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("lawn-care-cost-check") ?? getPremiumSchemaForPaidSlug("lawn-care-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("lawn-care-cost-check") ?? getPremiumSchemaForPaidSlug("lawn-care-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("lawn-care-cost-check") ?? getPremiumSchemaForPaidSlug("lawn-care-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — meal-planning-verdict", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("meal-planning-verdict") ?? getPremiumSchemaForPaidSlug("meal-planning-verdict");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("meal-planning-verdict") ?? getPremiumSchemaForPaidSlug("meal-planning-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("meal-planning-verdict") ?? getPremiumSchemaForPaidSlug("meal-planning-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("meal-planning-verdict") ?? getPremiumSchemaForPaidSlug("meal-planning-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — menu-profit-leak-detector", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("menu-profit-leak-detector") ?? getPremiumSchemaForPaidSlug("menu-profit-leak-detector");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("menu-profit-leak-detector") ?? getPremiumSchemaForPaidSlug("menu-profit-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("menu-profit-leak-detector") ?? getPremiumSchemaForPaidSlug("menu-profit-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("menu-profit-leak-detector") ?? getPremiumSchemaForPaidSlug("menu-profit-leak-detector");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — millwork-bid-risk-analyzer", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("millwork-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("millwork-bid-risk-analyzer");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("millwork-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("millwork-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("millwork-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("millwork-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("millwork-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("millwork-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — office-cleaning-bid-optimizer", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("office-cleaning-bid-optimizer") ?? getPremiumSchemaForPaidSlug("office-cleaning-bid-optimizer");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("office-cleaning-bid-optimizer") ?? getPremiumSchemaForPaidSlug("office-cleaning-bid-optimizer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("office-cleaning-bid-optimizer") ?? getPremiumSchemaForPaidSlug("office-cleaning-bid-optimizer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("office-cleaning-bid-optimizer") ?? getPremiumSchemaForPaidSlug("office-cleaning-bid-optimizer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — painting-job-profit-verdict", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("painting-job-profit-verdict") ?? getPremiumSchemaForPaidSlug("painting-job-profit-verdict");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("painting-job-profit-verdict") ?? getPremiumSchemaForPaidSlug("painting-job-profit-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("painting-job-profit-verdict") ?? getPremiumSchemaForPaidSlug("painting-job-profit-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("painting-job-profit-verdict") ?? getPremiumSchemaForPaidSlug("painting-job-profit-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — panel-shop-margin-verdict", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("panel-shop-margin-verdict") ?? getPremiumSchemaForPaidSlug("panel-shop-margin-verdict");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("panel-shop-margin-verdict") ?? getPremiumSchemaForPaidSlug("panel-shop-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("panel-shop-margin-verdict") ?? getPremiumSchemaForPaidSlug("panel-shop-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("panel-shop-margin-verdict") ?? getPremiumSchemaForPaidSlug("panel-shop-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — plumbing-job-margin-verdict", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("plumbing-job-margin-verdict") ?? getPremiumSchemaForPaidSlug("plumbing-job-margin-verdict");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("plumbing-job-margin-verdict") ?? getPremiumSchemaForPaidSlug("plumbing-job-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("plumbing-job-margin-verdict") ?? getPremiumSchemaForPaidSlug("plumbing-job-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("plumbing-job-margin-verdict") ?? getPremiumSchemaForPaidSlug("plumbing-job-margin-verdict");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — print-job-cost-check", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("print-job-cost-check") ?? getPremiumSchemaForPaidSlug("print-job-cost-check");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("print-job-cost-check") ?? getPremiumSchemaForPaidSlug("print-job-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("print-job-cost-check") ?? getPremiumSchemaForPaidSlug("print-job-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("print-job-cost-check") ?? getPremiumSchemaForPaidSlug("print-job-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — product-margin-calculator", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("product-margin-calculator") ?? getPremiumSchemaForPaidSlug("product-margin-calculator");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("product-margin-calculator") ?? getPremiumSchemaForPaidSlug("product-margin-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("product-margin-calculator") ?? getPremiumSchemaForPaidSlug("product-margin-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("product-margin-calculator") ?? getPremiumSchemaForPaidSlug("product-margin-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — project-cost-calculator", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("project-cost-calculator") ?? getPremiumSchemaForPaidSlug("project-cost-calculator");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("project-cost-calculator") ?? getPremiumSchemaForPaidSlug("project-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("project-cost-calculator") ?? getPremiumSchemaForPaidSlug("project-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("project-cost-calculator") ?? getPremiumSchemaForPaidSlug("project-cost-calculator");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — recipe-cost-check", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("recipe-cost-check") ?? getPremiumSchemaForPaidSlug("recipe-cost-check");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("recipe-cost-check") ?? getPremiumSchemaForPaidSlug("recipe-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("recipe-cost-check") ?? getPremiumSchemaForPaidSlug("recipe-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("recipe-cost-check") ?? getPremiumSchemaForPaidSlug("recipe-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — roofing-contract-margin-guard", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-contract-margin-guard") ?? getPremiumSchemaForPaidSlug("roofing-contract-margin-guard");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-contract-margin-guard") ?? getPremiumSchemaForPaidSlug("roofing-contract-margin-guard");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-contract-margin-guard") ?? getPremiumSchemaForPaidSlug("roofing-contract-margin-guard");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-contract-margin-guard") ?? getPremiumSchemaForPaidSlug("roofing-contract-margin-guard");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — roofing-square-cost-check", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-square-cost-check") ?? getPremiumSchemaForPaidSlug("roofing-square-cost-check");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-square-cost-check") ?? getPremiumSchemaForPaidSlug("roofing-square-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-square-cost-check") ?? getPremiumSchemaForPaidSlug("roofing-square-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("roofing-square-cost-check") ?? getPremiumSchemaForPaidSlug("roofing-square-cost-check");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});

describe("s3 activation smoke — welding-bid-risk-analyzer", () => {
  test("schema resolves with inputs and outputs", () => {
    const schema =
      getPremiumCalculatorSchema("welding-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("welding-bid-risk-analyzer");
    expect(schema).not.toBeNull();
    expect(schema?.inputs.length).toBeGreaterThan(0);
    expect(schema?.outputs.length).toBeGreaterThan(0);
  });

  test("validation scaffold accepts default input shape (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("welding-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("welding-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    expect(Object.keys(inputs).length).toBe(schema.inputs.length);
  });

  test("engine returns finite outputs (smoke only — not accuracy oracle)", () => {
    const schema =
      getPremiumCalculatorSchema("welding-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("welding-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const result = runPremiumSchemaEngine(schema, inputs);
    expect(result.outputs.length).toBeGreaterThan(0);
    for (const output of result.outputs) {
      if (typeof output.raw === "number") {
        expect(Number.isFinite(output.raw)).toBe(true);
      }
    }
  });

  test("boundary sanity — negative and oversized inputs do not crash engine (smoke only)", () => {
    const schema =
      getPremiumCalculatorSchema("welding-bid-risk-analyzer") ?? getPremiumSchemaForPaidSlug("welding-bid-risk-analyzer");
    if (!schema) throw new Error("schema missing");
    const inputs = buildDefaultSchemaInputs(schema);
    const firstKey = schema.inputs[0]?.id;
    if (firstKey) {
      inputs[firstKey] = -999999;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
      inputs[firstKey] = 1e12;
      expect(() => runPremiumSchemaEngine(schema, inputs)).not.toThrow();
    }
  });
});
