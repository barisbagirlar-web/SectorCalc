/**
 * S5/6 guide oracle UX scaffold smoke — presence only, no calculation oracle.
 * GENERATED by scripts/tool-activation/apply-s5-guide-oracle-ux-scaffold.mjs
 */
import { describe, expect, test } from "vitest";
import { getToolGuideSpec } from "@/lib/tool-guides/premium-input-guide-specs";
import { getPremiumSchemaForPaidSlug } from "@/lib/premium-schema/schema-registry";
import { getFreeTrafficToolBySlug } from "@/lib/tools/free-traffic-catalog";

const S5_SMOKE_SLUGS = [
  "age-calculator",
  "agirlikli-ortalama-hesaplama",
  "akaryakit-fiyati-ile-yol-butcesi",
  "amper-kilowatt-kw-cevirici",
  "arac-amortisman-hesaplama",
  "arac-bakim-periyodu-takip-hesaplama",
  "area-converter",
  "asma-tavan-malzeme-hesabi",
  "average-calculator",
  "aydinlatma-armatur-sayisi-hesaplama",
  "baca-havalandirma-kanali-cap-hesabi",
  "bar-psi-pascal-cevirici",
  "batch-yield-calculator",
  "beygir-gucu-kilowatt-cevirici",
  "boru-agirlik-hesaplama-celik-paslanmaz",
  "boru-capi-akis-hizi-hesaplama",
  "boya-kaplama-sarfiyati-m-basina-hesabi",
  "brick-calculator",
  "cay-kahve-su-tuketim-maliyeti",
  "celik-cati-makas-yaklasik-agirligi",
  "cit-korkuluk-malzeme-hesabi",
  "cleaning-cost-estimator",
  "cnc-minimum-safe-quote-analyzer",
  "concrete-volume-calculator",
  "cop-atik-konteyner-hacim-hesabi",
  "cutting-speed-calculator",
  "daire-alani-cevresi-hesaplama",
  "date-difference-calculator",
  "delivery-cost-calculator",
  "depreciation-calculator",
  "desi-calculator",
  "discount-calculator",
  "disli-modul-cap-hesaplama",
  "dizel-benzin-maliyet-karsilastirma",
  "dogalgaz-tuketimi-hesaplama",
  "drywall-calculator",
  "duvar-kagidi-seramik-adet-hesaplama",
  "excavation-volume-calculator",
  "feed-rate-calculator",
  "flooring-calculator",
  "fotokopi-yazici-toner-sayfa-maliyeti",
  "fuel-consumption-calculator",
  "fuel-consumption-check",
  "fuel-cost-calculator",
  "fuel-travel-calculator",
  "home-budget-calculator",
  "home-renovation-m2",
  "internet-telefon-paketi-karsilastirma",
  "is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati",
  "isi-degistirici-esanjor-kapasite-hesabi",
  "isitma-sogutma-yuku-kcal-kw-hesabi",
  "joule-kalori-watt-saat-cevirici",
  "kesir-ondalik-cevirici",
  "kiris-kolon-yaklasik-agirlik-hesabi",
  "koni-hacmi-hesaplama",
  "kosebent-lama-agirlik-hesaplama",
  "kumlama-raspa-kum-sarfiyati-hesabi",
  "kure-hacmi-hesaplama",
  "kwh-consumption-check",
  "lastik-omru-degisim-km-hesaplama",
  "length-converter",
  "lpg-benzin-tasarruf-karsilastirma",
  "machine-hour-estimator",
  "matkap-kilavuz-delik-capi-tablosu",
  "median-calculator",
  "merdiven-platform-yukseklik-erisim-hesabi",
  "newton-kilogram-kuvvet-cevirici",
  "npu-npi-profil-agirlik-hesaplama",
  "ofis-kirtasiye-sarfiyat-hesabi",
  "paint-coverage-cost-check",
  "palet-ambalaj-kereste-hesabi",
  "paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli",
  "percentage-calculator",
  "percentage-increase-calculator",
  "plaster-calculator",
  "plumbing-fixture-cost-check",
  "pompa-gucu-basma-yuksekligi-hesabi",
  "prefabrik-konteyner-ofis-m-maliyeti",
  "prefabrik-konteyner-olcu-hesaplama",
  "probability-calculator",
  "project-cost-estimator",
  "proportion-calculator",
  "radyator-petek-boyu-hesaplama",
  "rastgele-sayi-ureteci",
  "ratio-calculator",
  "rebar-weight-calculator",
  "repair-time-vs-price-check",
  "return-rate-profit-erosion-tool",
  "roofing-area-calculator",
] as const;

describe("s5 guide oracle ux scaffold smoke registry", () => {
  test("registry contains patched slugs", () => {
    expect(S5_SMOKE_SLUGS.length).toBeGreaterThan(0);
  });
});

describe("s5 guide oracle smoke — age-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("age-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("age-calculator");
    const schema = getPremiumSchemaForPaidSlug("age-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("age-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("age-calculator");
    if (guide) {
      expect(guide.slug).toBe("age-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("age-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — agirlikli-ortalama-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("agirlikli-ortalama-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("agirlikli-ortalama-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("agirlikli-ortalama-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("agirlikli-ortalama-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("agirlikli-ortalama-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("agirlikli-ortalama-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("agirlikli-ortalama-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — akaryakit-fiyati-ile-yol-butcesi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("akaryakit-fiyati-ile-yol-butcesi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("akaryakit-fiyati-ile-yol-butcesi");
    const schema = getPremiumSchemaForPaidSlug("akaryakit-fiyati-ile-yol-butcesi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("akaryakit-fiyati-ile-yol-butcesi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("akaryakit-fiyati-ile-yol-butcesi");
    if (guide) {
      expect(guide.slug).toBe("akaryakit-fiyati-ile-yol-butcesi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("akaryakit-fiyati-ile-yol-butcesi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — amper-kilowatt-kw-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("amper-kilowatt-kw-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("amper-kilowatt-kw-cevirici");
    const schema = getPremiumSchemaForPaidSlug("amper-kilowatt-kw-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("amper-kilowatt-kw-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("amper-kilowatt-kw-cevirici");
    if (guide) {
      expect(guide.slug).toBe("amper-kilowatt-kw-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("amper-kilowatt-kw-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — arac-amortisman-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("arac-amortisman-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("arac-amortisman-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("arac-amortisman-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("arac-amortisman-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("arac-amortisman-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("arac-amortisman-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("arac-amortisman-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — arac-bakim-periyodu-takip-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("arac-bakim-periyodu-takip-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("arac-bakim-periyodu-takip-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("arac-bakim-periyodu-takip-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("arac-bakim-periyodu-takip-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("arac-bakim-periyodu-takip-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("arac-bakim-periyodu-takip-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("arac-bakim-periyodu-takip-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — area-converter", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("area-converter");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("area-converter");
    const schema = getPremiumSchemaForPaidSlug("area-converter");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("area-converter");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("area-converter");
    if (guide) {
      expect(guide.slug).toBe("area-converter");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("area-converter");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — asma-tavan-malzeme-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("asma-tavan-malzeme-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("asma-tavan-malzeme-hesabi");
    const schema = getPremiumSchemaForPaidSlug("asma-tavan-malzeme-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("asma-tavan-malzeme-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("asma-tavan-malzeme-hesabi");
    if (guide) {
      expect(guide.slug).toBe("asma-tavan-malzeme-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("asma-tavan-malzeme-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — average-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("average-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("average-calculator");
    const schema = getPremiumSchemaForPaidSlug("average-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("average-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("average-calculator");
    if (guide) {
      expect(guide.slug).toBe("average-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("average-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — aydinlatma-armatur-sayisi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("aydinlatma-armatur-sayisi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("aydinlatma-armatur-sayisi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("aydinlatma-armatur-sayisi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("aydinlatma-armatur-sayisi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("aydinlatma-armatur-sayisi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("aydinlatma-armatur-sayisi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("aydinlatma-armatur-sayisi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — baca-havalandirma-kanali-cap-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("baca-havalandirma-kanali-cap-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("baca-havalandirma-kanali-cap-hesabi");
    const schema = getPremiumSchemaForPaidSlug("baca-havalandirma-kanali-cap-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("baca-havalandirma-kanali-cap-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("baca-havalandirma-kanali-cap-hesabi");
    if (guide) {
      expect(guide.slug).toBe("baca-havalandirma-kanali-cap-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("baca-havalandirma-kanali-cap-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — bar-psi-pascal-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("bar-psi-pascal-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("bar-psi-pascal-cevirici");
    const schema = getPremiumSchemaForPaidSlug("bar-psi-pascal-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("bar-psi-pascal-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("bar-psi-pascal-cevirici");
    if (guide) {
      expect(guide.slug).toBe("bar-psi-pascal-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("bar-psi-pascal-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — batch-yield-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("batch-yield-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("batch-yield-calculator");
    const schema = getPremiumSchemaForPaidSlug("batch-yield-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("batch-yield-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("batch-yield-calculator");
    if (guide) {
      expect(guide.slug).toBe("batch-yield-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("batch-yield-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — beygir-gucu-kilowatt-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("beygir-gucu-kilowatt-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("beygir-gucu-kilowatt-cevirici");
    const schema = getPremiumSchemaForPaidSlug("beygir-gucu-kilowatt-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("beygir-gucu-kilowatt-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("beygir-gucu-kilowatt-cevirici");
    if (guide) {
      expect(guide.slug).toBe("beygir-gucu-kilowatt-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("beygir-gucu-kilowatt-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — boru-agirlik-hesaplama-celik-paslanmaz", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("boru-agirlik-hesaplama-celik-paslanmaz");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("boru-agirlik-hesaplama-celik-paslanmaz");
    const schema = getPremiumSchemaForPaidSlug("boru-agirlik-hesaplama-celik-paslanmaz");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("boru-agirlik-hesaplama-celik-paslanmaz");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("boru-agirlik-hesaplama-celik-paslanmaz");
    if (guide) {
      expect(guide.slug).toBe("boru-agirlik-hesaplama-celik-paslanmaz");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("boru-agirlik-hesaplama-celik-paslanmaz");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — boru-capi-akis-hizi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("boru-capi-akis-hizi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("boru-capi-akis-hizi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("boru-capi-akis-hizi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("boru-capi-akis-hizi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("boru-capi-akis-hizi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("boru-capi-akis-hizi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("boru-capi-akis-hizi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — boya-kaplama-sarfiyati-m-basina-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("boya-kaplama-sarfiyati-m-basina-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("boya-kaplama-sarfiyati-m-basina-hesabi");
    const schema = getPremiumSchemaForPaidSlug("boya-kaplama-sarfiyati-m-basina-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("boya-kaplama-sarfiyati-m-basina-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("boya-kaplama-sarfiyati-m-basina-hesabi");
    if (guide) {
      expect(guide.slug).toBe("boya-kaplama-sarfiyati-m-basina-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("boya-kaplama-sarfiyati-m-basina-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — brick-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("brick-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("brick-calculator");
    const schema = getPremiumSchemaForPaidSlug("brick-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("brick-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("brick-calculator");
    if (guide) {
      expect(guide.slug).toBe("brick-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("brick-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cay-kahve-su-tuketim-maliyeti", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cay-kahve-su-tuketim-maliyeti");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cay-kahve-su-tuketim-maliyeti");
    const schema = getPremiumSchemaForPaidSlug("cay-kahve-su-tuketim-maliyeti");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cay-kahve-su-tuketim-maliyeti");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cay-kahve-su-tuketim-maliyeti");
    if (guide) {
      expect(guide.slug).toBe("cay-kahve-su-tuketim-maliyeti");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cay-kahve-su-tuketim-maliyeti");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — celik-cati-makas-yaklasik-agirligi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("celik-cati-makas-yaklasik-agirligi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("celik-cati-makas-yaklasik-agirligi");
    const schema = getPremiumSchemaForPaidSlug("celik-cati-makas-yaklasik-agirligi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("celik-cati-makas-yaklasik-agirligi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("celik-cati-makas-yaklasik-agirligi");
    if (guide) {
      expect(guide.slug).toBe("celik-cati-makas-yaklasik-agirligi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("celik-cati-makas-yaklasik-agirligi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cit-korkuluk-malzeme-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cit-korkuluk-malzeme-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cit-korkuluk-malzeme-hesabi");
    const schema = getPremiumSchemaForPaidSlug("cit-korkuluk-malzeme-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cit-korkuluk-malzeme-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cit-korkuluk-malzeme-hesabi");
    if (guide) {
      expect(guide.slug).toBe("cit-korkuluk-malzeme-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cit-korkuluk-malzeme-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cleaning-cost-estimator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cleaning-cost-estimator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cleaning-cost-estimator");
    const schema = getPremiumSchemaForPaidSlug("cleaning-cost-estimator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cleaning-cost-estimator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cleaning-cost-estimator");
    if (guide) {
      expect(guide.slug).toBe("cleaning-cost-estimator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cleaning-cost-estimator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cnc-minimum-safe-quote-analyzer", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cnc-minimum-safe-quote-analyzer");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cnc-minimum-safe-quote-analyzer");
    const schema = getPremiumSchemaForPaidSlug("cnc-minimum-safe-quote-analyzer");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cnc-minimum-safe-quote-analyzer");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cnc-minimum-safe-quote-analyzer");
    if (guide) {
      expect(guide.slug).toBe("cnc-minimum-safe-quote-analyzer");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cnc-minimum-safe-quote-analyzer");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — concrete-volume-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("concrete-volume-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("concrete-volume-calculator");
    const schema = getPremiumSchemaForPaidSlug("concrete-volume-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("concrete-volume-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("concrete-volume-calculator");
    if (guide) {
      expect(guide.slug).toBe("concrete-volume-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("concrete-volume-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cop-atik-konteyner-hacim-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cop-atik-konteyner-hacim-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cop-atik-konteyner-hacim-hesabi");
    const schema = getPremiumSchemaForPaidSlug("cop-atik-konteyner-hacim-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cop-atik-konteyner-hacim-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cop-atik-konteyner-hacim-hesabi");
    if (guide) {
      expect(guide.slug).toBe("cop-atik-konteyner-hacim-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cop-atik-konteyner-hacim-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — cutting-speed-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("cutting-speed-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("cutting-speed-calculator");
    const schema = getPremiumSchemaForPaidSlug("cutting-speed-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("cutting-speed-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("cutting-speed-calculator");
    if (guide) {
      expect(guide.slug).toBe("cutting-speed-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("cutting-speed-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — daire-alani-cevresi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("daire-alani-cevresi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("daire-alani-cevresi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("daire-alani-cevresi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("daire-alani-cevresi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("daire-alani-cevresi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("daire-alani-cevresi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("daire-alani-cevresi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — date-difference-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("date-difference-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("date-difference-calculator");
    const schema = getPremiumSchemaForPaidSlug("date-difference-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("date-difference-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("date-difference-calculator");
    if (guide) {
      expect(guide.slug).toBe("date-difference-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("date-difference-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — delivery-cost-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("delivery-cost-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("delivery-cost-calculator");
    const schema = getPremiumSchemaForPaidSlug("delivery-cost-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("delivery-cost-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("delivery-cost-calculator");
    if (guide) {
      expect(guide.slug).toBe("delivery-cost-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("delivery-cost-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — depreciation-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("depreciation-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("depreciation-calculator");
    const schema = getPremiumSchemaForPaidSlug("depreciation-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("depreciation-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("depreciation-calculator");
    if (guide) {
      expect(guide.slug).toBe("depreciation-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("depreciation-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — desi-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("desi-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("desi-calculator");
    const schema = getPremiumSchemaForPaidSlug("desi-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("desi-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("desi-calculator");
    if (guide) {
      expect(guide.slug).toBe("desi-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("desi-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — discount-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("discount-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("discount-calculator");
    const schema = getPremiumSchemaForPaidSlug("discount-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("discount-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("discount-calculator");
    if (guide) {
      expect(guide.slug).toBe("discount-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("discount-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — disli-modul-cap-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("disli-modul-cap-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("disli-modul-cap-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("disli-modul-cap-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("disli-modul-cap-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("disli-modul-cap-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("disli-modul-cap-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("disli-modul-cap-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — dizel-benzin-maliyet-karsilastirma", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("dizel-benzin-maliyet-karsilastirma");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("dizel-benzin-maliyet-karsilastirma");
    const schema = getPremiumSchemaForPaidSlug("dizel-benzin-maliyet-karsilastirma");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("dizel-benzin-maliyet-karsilastirma");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("dizel-benzin-maliyet-karsilastirma");
    if (guide) {
      expect(guide.slug).toBe("dizel-benzin-maliyet-karsilastirma");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("dizel-benzin-maliyet-karsilastirma");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — dogalgaz-tuketimi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("dogalgaz-tuketimi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("dogalgaz-tuketimi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("dogalgaz-tuketimi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("dogalgaz-tuketimi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("dogalgaz-tuketimi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("dogalgaz-tuketimi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("dogalgaz-tuketimi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — drywall-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("drywall-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("drywall-calculator");
    const schema = getPremiumSchemaForPaidSlug("drywall-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("drywall-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("drywall-calculator");
    if (guide) {
      expect(guide.slug).toBe("drywall-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("drywall-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — duvar-kagidi-seramik-adet-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("duvar-kagidi-seramik-adet-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("duvar-kagidi-seramik-adet-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("duvar-kagidi-seramik-adet-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("duvar-kagidi-seramik-adet-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("duvar-kagidi-seramik-adet-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("duvar-kagidi-seramik-adet-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("duvar-kagidi-seramik-adet-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — excavation-volume-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("excavation-volume-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("excavation-volume-calculator");
    const schema = getPremiumSchemaForPaidSlug("excavation-volume-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("excavation-volume-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("excavation-volume-calculator");
    if (guide) {
      expect(guide.slug).toBe("excavation-volume-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("excavation-volume-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — feed-rate-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("feed-rate-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("feed-rate-calculator");
    const schema = getPremiumSchemaForPaidSlug("feed-rate-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("feed-rate-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("feed-rate-calculator");
    if (guide) {
      expect(guide.slug).toBe("feed-rate-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("feed-rate-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — flooring-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("flooring-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("flooring-calculator");
    const schema = getPremiumSchemaForPaidSlug("flooring-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("flooring-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("flooring-calculator");
    if (guide) {
      expect(guide.slug).toBe("flooring-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("flooring-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — fotokopi-yazici-toner-sayfa-maliyeti", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("fotokopi-yazici-toner-sayfa-maliyeti");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("fotokopi-yazici-toner-sayfa-maliyeti");
    const schema = getPremiumSchemaForPaidSlug("fotokopi-yazici-toner-sayfa-maliyeti");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("fotokopi-yazici-toner-sayfa-maliyeti");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("fotokopi-yazici-toner-sayfa-maliyeti");
    if (guide) {
      expect(guide.slug).toBe("fotokopi-yazici-toner-sayfa-maliyeti");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("fotokopi-yazici-toner-sayfa-maliyeti");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — fuel-consumption-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("fuel-consumption-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("fuel-consumption-calculator");
    const schema = getPremiumSchemaForPaidSlug("fuel-consumption-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("fuel-consumption-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("fuel-consumption-calculator");
    if (guide) {
      expect(guide.slug).toBe("fuel-consumption-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("fuel-consumption-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — fuel-consumption-check", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("fuel-consumption-check");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("fuel-consumption-check");
    const schema = getPremiumSchemaForPaidSlug("fuel-consumption-check");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("fuel-consumption-check");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("fuel-consumption-check");
    if (guide) {
      expect(guide.slug).toBe("fuel-consumption-check");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("fuel-consumption-check");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — fuel-cost-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("fuel-cost-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("fuel-cost-calculator");
    const schema = getPremiumSchemaForPaidSlug("fuel-cost-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("fuel-cost-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("fuel-cost-calculator");
    if (guide) {
      expect(guide.slug).toBe("fuel-cost-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("fuel-cost-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — fuel-travel-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("fuel-travel-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("fuel-travel-calculator");
    const schema = getPremiumSchemaForPaidSlug("fuel-travel-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("fuel-travel-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("fuel-travel-calculator");
    if (guide) {
      expect(guide.slug).toBe("fuel-travel-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("fuel-travel-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — home-budget-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("home-budget-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("home-budget-calculator");
    const schema = getPremiumSchemaForPaidSlug("home-budget-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("home-budget-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("home-budget-calculator");
    if (guide) {
      expect(guide.slug).toBe("home-budget-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("home-budget-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — home-renovation-m2", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("home-renovation-m2");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("home-renovation-m2");
    const schema = getPremiumSchemaForPaidSlug("home-renovation-m2");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("home-renovation-m2");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("home-renovation-m2");
    if (guide) {
      expect(guide.slug).toBe("home-renovation-m2");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("home-renovation-m2");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — internet-telefon-paketi-karsilastirma", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("internet-telefon-paketi-karsilastirma");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("internet-telefon-paketi-karsilastirma");
    const schema = getPremiumSchemaForPaidSlug("internet-telefon-paketi-karsilastirma");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("internet-telefon-paketi-karsilastirma");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("internet-telefon-paketi-karsilastirma");
    if (guide) {
      expect(guide.slug).toBe("internet-telefon-paketi-karsilastirma");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("internet-telefon-paketi-karsilastirma");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
    const schema = getPremiumSchemaForPaidSlug("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
    if (guide) {
      expect(guide.slug).toBe("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("is-elbisesi-kkd-kisisel-koruyucu-donanim-sarfiyati");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — isi-degistirici-esanjor-kapasite-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("isi-degistirici-esanjor-kapasite-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("isi-degistirici-esanjor-kapasite-hesabi");
    const schema = getPremiumSchemaForPaidSlug("isi-degistirici-esanjor-kapasite-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("isi-degistirici-esanjor-kapasite-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("isi-degistirici-esanjor-kapasite-hesabi");
    if (guide) {
      expect(guide.slug).toBe("isi-degistirici-esanjor-kapasite-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("isi-degistirici-esanjor-kapasite-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — isitma-sogutma-yuku-kcal-kw-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("isitma-sogutma-yuku-kcal-kw-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("isitma-sogutma-yuku-kcal-kw-hesabi");
    const schema = getPremiumSchemaForPaidSlug("isitma-sogutma-yuku-kcal-kw-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("isitma-sogutma-yuku-kcal-kw-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("isitma-sogutma-yuku-kcal-kw-hesabi");
    if (guide) {
      expect(guide.slug).toBe("isitma-sogutma-yuku-kcal-kw-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("isitma-sogutma-yuku-kcal-kw-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — joule-kalori-watt-saat-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("joule-kalori-watt-saat-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("joule-kalori-watt-saat-cevirici");
    const schema = getPremiumSchemaForPaidSlug("joule-kalori-watt-saat-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("joule-kalori-watt-saat-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("joule-kalori-watt-saat-cevirici");
    if (guide) {
      expect(guide.slug).toBe("joule-kalori-watt-saat-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("joule-kalori-watt-saat-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kesir-ondalik-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kesir-ondalik-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kesir-ondalik-cevirici");
    const schema = getPremiumSchemaForPaidSlug("kesir-ondalik-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kesir-ondalik-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kesir-ondalik-cevirici");
    if (guide) {
      expect(guide.slug).toBe("kesir-ondalik-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kesir-ondalik-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kiris-kolon-yaklasik-agirlik-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kiris-kolon-yaklasik-agirlik-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kiris-kolon-yaklasik-agirlik-hesabi");
    const schema = getPremiumSchemaForPaidSlug("kiris-kolon-yaklasik-agirlik-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kiris-kolon-yaklasik-agirlik-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kiris-kolon-yaklasik-agirlik-hesabi");
    if (guide) {
      expect(guide.slug).toBe("kiris-kolon-yaklasik-agirlik-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kiris-kolon-yaklasik-agirlik-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — koni-hacmi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("koni-hacmi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("koni-hacmi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("koni-hacmi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("koni-hacmi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("koni-hacmi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("koni-hacmi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("koni-hacmi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kosebent-lama-agirlik-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kosebent-lama-agirlik-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kosebent-lama-agirlik-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("kosebent-lama-agirlik-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kosebent-lama-agirlik-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kosebent-lama-agirlik-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("kosebent-lama-agirlik-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kosebent-lama-agirlik-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kumlama-raspa-kum-sarfiyati-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kumlama-raspa-kum-sarfiyati-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kumlama-raspa-kum-sarfiyati-hesabi");
    const schema = getPremiumSchemaForPaidSlug("kumlama-raspa-kum-sarfiyati-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kumlama-raspa-kum-sarfiyati-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kumlama-raspa-kum-sarfiyati-hesabi");
    if (guide) {
      expect(guide.slug).toBe("kumlama-raspa-kum-sarfiyati-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kumlama-raspa-kum-sarfiyati-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kure-hacmi-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kure-hacmi-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kure-hacmi-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("kure-hacmi-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kure-hacmi-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kure-hacmi-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("kure-hacmi-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kure-hacmi-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — kwh-consumption-check", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("kwh-consumption-check");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("kwh-consumption-check");
    const schema = getPremiumSchemaForPaidSlug("kwh-consumption-check");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("kwh-consumption-check");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("kwh-consumption-check");
    if (guide) {
      expect(guide.slug).toBe("kwh-consumption-check");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("kwh-consumption-check");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — lastik-omru-degisim-km-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("lastik-omru-degisim-km-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("lastik-omru-degisim-km-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("lastik-omru-degisim-km-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("lastik-omru-degisim-km-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("lastik-omru-degisim-km-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("lastik-omru-degisim-km-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("lastik-omru-degisim-km-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — length-converter", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("length-converter");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("length-converter");
    const schema = getPremiumSchemaForPaidSlug("length-converter");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("length-converter");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("length-converter");
    if (guide) {
      expect(guide.slug).toBe("length-converter");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("length-converter");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — lpg-benzin-tasarruf-karsilastirma", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("lpg-benzin-tasarruf-karsilastirma");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("lpg-benzin-tasarruf-karsilastirma");
    const schema = getPremiumSchemaForPaidSlug("lpg-benzin-tasarruf-karsilastirma");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("lpg-benzin-tasarruf-karsilastirma");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("lpg-benzin-tasarruf-karsilastirma");
    if (guide) {
      expect(guide.slug).toBe("lpg-benzin-tasarruf-karsilastirma");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("lpg-benzin-tasarruf-karsilastirma");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — machine-hour-estimator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("machine-hour-estimator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("machine-hour-estimator");
    const schema = getPremiumSchemaForPaidSlug("machine-hour-estimator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("machine-hour-estimator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("machine-hour-estimator");
    if (guide) {
      expect(guide.slug).toBe("machine-hour-estimator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("machine-hour-estimator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — matkap-kilavuz-delik-capi-tablosu", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("matkap-kilavuz-delik-capi-tablosu");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("matkap-kilavuz-delik-capi-tablosu");
    const schema = getPremiumSchemaForPaidSlug("matkap-kilavuz-delik-capi-tablosu");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("matkap-kilavuz-delik-capi-tablosu");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("matkap-kilavuz-delik-capi-tablosu");
    if (guide) {
      expect(guide.slug).toBe("matkap-kilavuz-delik-capi-tablosu");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("matkap-kilavuz-delik-capi-tablosu");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — median-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("median-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("median-calculator");
    const schema = getPremiumSchemaForPaidSlug("median-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("median-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("median-calculator");
    if (guide) {
      expect(guide.slug).toBe("median-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("median-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — merdiven-platform-yukseklik-erisim-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("merdiven-platform-yukseklik-erisim-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("merdiven-platform-yukseklik-erisim-hesabi");
    const schema = getPremiumSchemaForPaidSlug("merdiven-platform-yukseklik-erisim-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("merdiven-platform-yukseklik-erisim-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("merdiven-platform-yukseklik-erisim-hesabi");
    if (guide) {
      expect(guide.slug).toBe("merdiven-platform-yukseklik-erisim-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("merdiven-platform-yukseklik-erisim-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — newton-kilogram-kuvvet-cevirici", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("newton-kilogram-kuvvet-cevirici");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("newton-kilogram-kuvvet-cevirici");
    const schema = getPremiumSchemaForPaidSlug("newton-kilogram-kuvvet-cevirici");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("newton-kilogram-kuvvet-cevirici");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("newton-kilogram-kuvvet-cevirici");
    if (guide) {
      expect(guide.slug).toBe("newton-kilogram-kuvvet-cevirici");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("newton-kilogram-kuvvet-cevirici");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — npu-npi-profil-agirlik-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("npu-npi-profil-agirlik-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("npu-npi-profil-agirlik-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("npu-npi-profil-agirlik-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("npu-npi-profil-agirlik-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("npu-npi-profil-agirlik-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("npu-npi-profil-agirlik-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("npu-npi-profil-agirlik-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — ofis-kirtasiye-sarfiyat-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("ofis-kirtasiye-sarfiyat-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("ofis-kirtasiye-sarfiyat-hesabi");
    const schema = getPremiumSchemaForPaidSlug("ofis-kirtasiye-sarfiyat-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("ofis-kirtasiye-sarfiyat-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("ofis-kirtasiye-sarfiyat-hesabi");
    if (guide) {
      expect(guide.slug).toBe("ofis-kirtasiye-sarfiyat-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("ofis-kirtasiye-sarfiyat-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — paint-coverage-cost-check", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("paint-coverage-cost-check");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("paint-coverage-cost-check");
    const schema = getPremiumSchemaForPaidSlug("paint-coverage-cost-check");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("paint-coverage-cost-check");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("paint-coverage-cost-check");
    if (guide) {
      expect(guide.slug).toBe("paint-coverage-cost-check");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("paint-coverage-cost-check");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — palet-ambalaj-kereste-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("palet-ambalaj-kereste-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("palet-ambalaj-kereste-hesabi");
    const schema = getPremiumSchemaForPaidSlug("palet-ambalaj-kereste-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("palet-ambalaj-kereste-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("palet-ambalaj-kereste-hesabi");
    if (guide) {
      expect(guide.slug).toBe("palet-ambalaj-kereste-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("palet-ambalaj-kereste-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
    const schema = getPremiumSchemaForPaidSlug("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
    if (guide) {
      expect(guide.slug).toBe("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("paslanmaz-celik-aluminyum-ayirt-etme-hesaplayicisi-yogunluk-bazli");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — percentage-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("percentage-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("percentage-calculator");
    const schema = getPremiumSchemaForPaidSlug("percentage-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("percentage-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("percentage-calculator");
    if (guide) {
      expect(guide.slug).toBe("percentage-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("percentage-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — percentage-increase-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("percentage-increase-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("percentage-increase-calculator");
    const schema = getPremiumSchemaForPaidSlug("percentage-increase-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("percentage-increase-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("percentage-increase-calculator");
    if (guide) {
      expect(guide.slug).toBe("percentage-increase-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("percentage-increase-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — plaster-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("plaster-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("plaster-calculator");
    const schema = getPremiumSchemaForPaidSlug("plaster-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("plaster-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("plaster-calculator");
    if (guide) {
      expect(guide.slug).toBe("plaster-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("plaster-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — plumbing-fixture-cost-check", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("plumbing-fixture-cost-check");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("plumbing-fixture-cost-check");
    const schema = getPremiumSchemaForPaidSlug("plumbing-fixture-cost-check");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("plumbing-fixture-cost-check");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("plumbing-fixture-cost-check");
    if (guide) {
      expect(guide.slug).toBe("plumbing-fixture-cost-check");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("plumbing-fixture-cost-check");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — pompa-gucu-basma-yuksekligi-hesabi", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("pompa-gucu-basma-yuksekligi-hesabi");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("pompa-gucu-basma-yuksekligi-hesabi");
    const schema = getPremiumSchemaForPaidSlug("pompa-gucu-basma-yuksekligi-hesabi");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("pompa-gucu-basma-yuksekligi-hesabi");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("pompa-gucu-basma-yuksekligi-hesabi");
    if (guide) {
      expect(guide.slug).toBe("pompa-gucu-basma-yuksekligi-hesabi");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("pompa-gucu-basma-yuksekligi-hesabi");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — prefabrik-konteyner-ofis-m-maliyeti", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("prefabrik-konteyner-ofis-m-maliyeti");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("prefabrik-konteyner-ofis-m-maliyeti");
    const schema = getPremiumSchemaForPaidSlug("prefabrik-konteyner-ofis-m-maliyeti");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("prefabrik-konteyner-ofis-m-maliyeti");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("prefabrik-konteyner-ofis-m-maliyeti");
    if (guide) {
      expect(guide.slug).toBe("prefabrik-konteyner-ofis-m-maliyeti");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("prefabrik-konteyner-ofis-m-maliyeti");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — prefabrik-konteyner-olcu-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("prefabrik-konteyner-olcu-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("prefabrik-konteyner-olcu-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("prefabrik-konteyner-olcu-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("prefabrik-konteyner-olcu-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("prefabrik-konteyner-olcu-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("prefabrik-konteyner-olcu-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("prefabrik-konteyner-olcu-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — probability-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("probability-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("probability-calculator");
    const schema = getPremiumSchemaForPaidSlug("probability-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("probability-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("probability-calculator");
    if (guide) {
      expect(guide.slug).toBe("probability-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("probability-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — project-cost-estimator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("project-cost-estimator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("project-cost-estimator");
    const schema = getPremiumSchemaForPaidSlug("project-cost-estimator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("project-cost-estimator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("project-cost-estimator");
    if (guide) {
      expect(guide.slug).toBe("project-cost-estimator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("project-cost-estimator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — proportion-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("proportion-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("proportion-calculator");
    const schema = getPremiumSchemaForPaidSlug("proportion-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("proportion-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("proportion-calculator");
    if (guide) {
      expect(guide.slug).toBe("proportion-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("proportion-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — radyator-petek-boyu-hesaplama", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("radyator-petek-boyu-hesaplama");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("radyator-petek-boyu-hesaplama");
    const schema = getPremiumSchemaForPaidSlug("radyator-petek-boyu-hesaplama");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("radyator-petek-boyu-hesaplama");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("radyator-petek-boyu-hesaplama");
    if (guide) {
      expect(guide.slug).toBe("radyator-petek-boyu-hesaplama");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("radyator-petek-boyu-hesaplama");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — rastgele-sayi-ureteci", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("rastgele-sayi-ureteci");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("rastgele-sayi-ureteci");
    const schema = getPremiumSchemaForPaidSlug("rastgele-sayi-ureteci");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("rastgele-sayi-ureteci");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("rastgele-sayi-ureteci");
    if (guide) {
      expect(guide.slug).toBe("rastgele-sayi-ureteci");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("rastgele-sayi-ureteci");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — ratio-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("ratio-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("ratio-calculator");
    const schema = getPremiumSchemaForPaidSlug("ratio-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("ratio-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("ratio-calculator");
    if (guide) {
      expect(guide.slug).toBe("ratio-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("ratio-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — rebar-weight-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("rebar-weight-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("rebar-weight-calculator");
    const schema = getPremiumSchemaForPaidSlug("rebar-weight-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("rebar-weight-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("rebar-weight-calculator");
    if (guide) {
      expect(guide.slug).toBe("rebar-weight-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("rebar-weight-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — repair-time-vs-price-check", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("repair-time-vs-price-check");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("repair-time-vs-price-check");
    const schema = getPremiumSchemaForPaidSlug("repair-time-vs-price-check");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("repair-time-vs-price-check");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("repair-time-vs-price-check");
    if (guide) {
      expect(guide.slug).toBe("repair-time-vs-price-check");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("repair-time-vs-price-check");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — return-rate-profit-erosion-tool", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("return-rate-profit-erosion-tool");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("return-rate-profit-erosion-tool");
    const schema = getPremiumSchemaForPaidSlug("return-rate-profit-erosion-tool");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("return-rate-profit-erosion-tool");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("return-rate-profit-erosion-tool");
    if (guide) {
      expect(guide.slug).toBe("return-rate-profit-erosion-tool");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("return-rate-profit-erosion-tool");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});

describe("s5 guide oracle smoke — roofing-area-calculator", () => {
  test("slug is registered in S5 smoke batch", () => {
    expect(S5_SMOKE_SLUGS).toContain("roofing-area-calculator");
  });

  test("free traffic catalog or premium schema resolves when backing exists", () => {
    const freeTool = getFreeTrafficToolBySlug("roofing-area-calculator");
    const schema = getPremiumSchemaForPaidSlug("roofing-area-calculator");
    if (!freeTool && !schema) {
      expect(S5_SMOKE_SLUGS).toContain("roofing-area-calculator");
      return;
    }
    expect(freeTool ?? schema).toBeTruthy();
  });

  test("guide spec is readable when wired", () => {
    const guide = getToolGuideSpec("roofing-area-calculator");
    if (guide) {
      expect(guide.slug).toBe("roofing-area-calculator");
      expect(guide.titleKey.length).toBeGreaterThan(0);
    }
  });

  test("schema resolves when backing exists (presence only)", () => {
    const schema = getPremiumSchemaForPaidSlug("roofing-area-calculator");
    if (schema) {
      expect(schema.inputs.length).toBeGreaterThan(0);
    }
  });
});
