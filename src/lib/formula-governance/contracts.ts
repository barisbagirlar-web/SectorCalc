import type { FormulaContract } from "./types";

export const FORMULA_CONTRACTS: readonly FormulaContract[] = [];

export function getFormulaContractBySlug(_slug: string): FormulaContract | undefined {
  return undefined;
}
