/**
 * Free tool i18n - permanently purged. All exports return empty.
 */

export function resolveFreeToolDisplayTitle(_slug: string, _locale: string, fallback: string): string {
  return fallback;
}

export function resolveFreeToolLocalizedCopy(_slug: string, _locale: string): { title?: string; description?: string } {
  return {};
}
