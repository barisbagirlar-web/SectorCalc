/**
 * Traffic catalog oracle compute - independent reference implementations.
 * GENERATED by scripts/generate-batch-traffic-catalog-governance.ts - do not edit by hand.
 */

import { OracleValidationError } from "@/lib/features/formula-governance/oracle/oracle-types";

export type TrafficOracleValues = Readonly<Record<string, number>>;

export type TrafficCatalogOracleOutput = Readonly<Record<string, number>>;

function n(values: TrafficOracleValues, key: string): number {
  const value = values[key];
  if (!Number.isFinite(value)) {
    throw new OracleValidationError("INVALID_QUANTITY", `${key} must be a finite number.`);
  }
  return value;
}

function safeDiv(numerator: number, denominator: number): number {
  if (!Number.isFinite(denominator) || denominator === 0) {
    throw new OracleValidationError("INVALID_QUANTITY", "Division requires a non-zero finite denominator.");
  }
  return numerator / denominator;
}

function collectValues(values: TrafficOracleValues): number[] {
  const keys = ["value1", "value2", "value3", "value4", "value5"] as const;
  const nums: number[] = [];
  for (const key of keys) {
    const value = values[key];
    if (Number.isFinite(value)) {
      nums.push(value);
    }
  }
  return nums;
}


export function calculateAgeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const ageYears = n(values, "currentYear") - n(values, "birthYear");
  return {
    ageYears,
  };
}

export function calculateAverageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const count = collectValues(values).length;
  const sum = collectValues(values).reduce((a, b) => a + b, 0);
  const average = count > 0 ? safeDiv(sum, count) : 0;
  return {
    count,
    sum,
    average,
  };
}

export function calculateBatchYieldCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const yieldPercent = safeDiv(n(values, "goodOutputQty"), n(values, "inputQty")) * 100;
  return {
    yieldPercent,
  };
}

export function calculateBoilerEfficiencyCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const efficiencyPercent = safeDiv(n(values, "usefulOutputKwh"), n(values, "fuelInputKwh")) * 100;
  return {
    efficiencyPercent,
  };
}

export function calculateBrickCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const bricks = Math.ceil(n(values, "wallAreaM2") * n(values, "bricksPerM2") * (1 + n(values, "wastePercent") / 100));
  return {
    bricks,
  };
}

export function calculateCarbonFootprintQuickOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kgCo2 = n(values, "energyKwh") * n(values, "emissionFactorKgPerKwh");
  return {
    kgCo2,
  };
}

export function calculateCbamExposureQuickCheckOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const exposure = n(values, "emissionsTon") * n(values, "carbonPrice");
  return {
    exposure,
  };
}

export function calculateCncCycleTimeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const perPartSeconds = n(values, "machiningSeconds") + n(values, "loadUnloadSeconds") + n(values, "inspectionSeconds");
  const totalSeconds = perPartSeconds * n(values, "quantity");
  return {
    perPartSeconds,
    totalSeconds,
  };
}

export function calculateCompressorEnergyCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kwh = n(values, "powerKw") * n(values, "operatingHours");
  const energyCost = kwh * n(values, "rate");
  return {
    kwh,
    energyCost,
  };
}

export function calculateConcreteBagCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const adjustedVolume = n(values, "volumeM3") * (1 + n(values, "wastePercent") / 100);
  const bags = Math.ceil(safeDiv(adjustedVolume, n(values, "bagYieldM3")));
  return {
    adjustedVolume,
    bags,
  };
}

export function calculateConcreteVolumeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const volume = n(values, "length") * n(values, "width") * n(values, "depth");
  return {
    volume,
  };
}

export function calculateCropYieldCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const tons = n(values, "areaHa") * n(values, "yieldTonPerHa");
  const revenue = tons * n(values, "pricePerTon");
  return {
    tons,
    revenue,
  };
}

export function calculateCuttingSpeedCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const cuttingSpeed = safeDiv(Math.PI * n(values, "diameterMm") * n(values, "rpm"), 1000);
  return {
    cuttingSpeed,
  };
}

export function calculateDateDifferenceCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const daysBetween = n(values, "endDayNumber") - n(values, "startDayNumber");
  return {
    daysBetween,
  };
}

export function calculateDeliveryCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalCost = n(values, "distanceKm") * n(values, "costPerKm") + n(values, "stops") * n(values, "costPerStop");
  return {
    totalCost,
  };
}

export function calculateDepreciationCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const annualDepreciation = safeDiv(n(values, "assetCost") - n(values, "salvageValue"), n(values, "usefulLifeYears"));
  return {
    annualDepreciation,
  };
}

export function calculateDesiCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const volumetricWeight = safeDiv(n(values, "lengthCm") * n(values, "widthCm") * n(values, "heightCm"), n(values, "divisor"));
  return {
    volumetricWeight,
  };
}

export function calculateDiscountCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const salePrice = n(values, "originalPrice") * (1 - n(values, "discountPercent") / 100);
  const saved = n(values, "originalPrice") - salePrice;
  return {
    salePrice,
    saved,
  };
}

export function calculateDrywallCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const sheets = Math.ceil(safeDiv(n(values, "wallAreaM2"), n(values, "sheetAreaM2")) * (1 + n(values, "wastePercent") / 100));
  return {
    sheets,
  };
}

export function calculateElectricityBillCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const variableCharge = n(values, "kwh") * n(values, "rate");
  const totalBill = variableCharge + n(values, "fixedCharge");
  return {
    variableCharge,
    totalBill,
  };
}

export function calculateEnergyConsumptionCheckOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kwh = n(values, "powerKw") * n(values, "hoursPerDay") * n(values, "days");
  const energyCost = kwh * n(values, "rate");
  return {
    kwh,
    energyCost,
  };
}

export function calculateExcavationVolumeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const volume = n(values, "lengthM") * n(values, "widthM") * n(values, "depthM");
  return {
    volume,
  };
}

export function calculateFeedCostEstimatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalCost = n(values, "feedKg") * n(values, "pricePerKg") + n(values, "transportCost");
  return {
    totalCost,
  };
}

export function calculateFeedRateCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const feedRate = n(values, "rpm") * n(values, "teeth") * n(values, "feedPerToothMm");
  return {
    feedRate,
  };
}

export function calculateFertilizerDosageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const fertilizerKg = n(values, "areaHa") * n(values, "dosageKgPerHa");
  return {
    fertilizerKg,
  };
}

export function calculateFlooringCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const netArea = n(values, "roomLengthM") * n(values, "roomWidthM");
  const materialArea = netArea * (1 + n(values, "wastePercent") / 100);
  return {
    netArea,
    materialArea,
  };
}

export function calculateFreightCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalCost = n(values, "weightKg") * n(values, "ratePerKg") + n(values, "fixedFee");
  return {
    totalCost,
  };
}

export function calculateFuelConsumptionCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const fuelLiters = safeDiv(n(values, "distanceKm"), 100) * n(values, "consumptionPer100Km");
  const fuelCost = fuelLiters * n(values, "fuelPrice");
  return {
    fuelLiters,
    fuelCost,
  };
}

export function calculateFuelCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const fuelLiters = safeDiv(n(values, "distanceKm"), 100) * n(values, "consumptionPer100Km");
  const fuelCost = fuelLiters * n(values, "fuelPrice");
  return {
    fuelLiters,
    fuelCost,
  };
}

export function calculateFuelEmissionCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kgCo2 = n(values, "litersFuel") * n(values, "kgCo2PerLiter");
  return {
    kgCo2,
  };
}

export function calculateFuelTravelCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const fuelLiters = safeDiv(n(values, "distanceKm"), 100) * n(values, "consumptionPer100Km");
  const fuelCost = fuelLiters * n(values, "fuelPrice");
  return {
    fuelLiters,
    fuelCost,
  };
}

export function calculateHeatLossCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const watts = n(values, "uValue") * n(values, "areaM2") * n(values, "tempDifferenceC");
  return {
    watts,
  };
}

export function calculateHomeBudgetCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const expenses = n(values, "rent") + n(values, "food") + n(values, "transport") + n(values, "utilities");
  const remaining = n(values, "income") - expenses;
  return {
    expenses,
    remaining,
  };
}

export function calculateHomeRenovationM2CalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const baseCost = n(values, "areaM2") * n(values, "costPerM2");
  const totalCost = baseCost * (1 + n(values, "contingencyPercent") / 100);
  return {
    baseCost,
    totalCost,
  };
}

export function calculateHourlyRateCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const hourlyRate = safeDiv(n(values, "monthlyIncome"), n(values, "billableHours"));
  return {
    hourlyRate,
  };
}

export function calculateIrrigationCostCheckOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const energyCost = n(values, "pumpKw") * n(values, "hours") * n(values, "energyRate");
  const totalCost = energyCost + n(values, "waterCost");
  return {
    energyCost,
    totalCost,
  };
}

export function calculateKwhCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const cost = n(values, "kwh") * n(values, "rate");
  return {
    cost,
  };
}

export function calculateLinearRegressionCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const sumX = n(values, "x1") + n(values, "x2") + n(values, "x3");
  const sumY = n(values, "y1") + n(values, "y2") + n(values, "y3");
  const sumXY = n(values, "x1") * n(values, "y1") + n(values, "x2") * n(values, "y2") + n(values, "x3") * n(values, "y3");
  const sumX2 = n(values, "x1") * n(values, "x1") + n(values, "x2") * n(values, "x2") + n(values, "x3") * n(values, "x3");
  const denom = 3 * sumX2 - sumX * sumX;
  const slope = safeDiv(3 * sumXY - sumX * sumY, denom);
  const intercept = safeDiv(sumY - slope * sumX, 3);
  return {
    sumX,
    sumY,
    sumXY,
    sumX2,
    denom,
    slope,
    intercept,
  };
}

export function calculateMachineHourRateCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const fixedHourly = safeDiv(n(values, "fixedMonthlyCost"), n(values, "monthlyMachineHours"));
  const hourlyRate = fixedHourly + n(values, "variableCostPerHour");
  return {
    fixedHourly,
    hourlyRate,
  };
}

export function calculateMaterialWasteCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const wasteKg = n(values, "inputMaterialKg") - n(values, "goodOutputKg");
  const wastePercent = safeDiv(wasteKg, n(values, "inputMaterialKg")) * 100;
  return {
    wasteKg,
    wastePercent,
  };
}

export function calculateMedianCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const median = (() => { const sorted = [...collectValues(values)].sort((a, b) => a - b); const n = sorted.length; if (n === 0) return 0; const mid = Math.floor(n / 2); return n % 2 === 0 ? (sorted[mid - 1] + sorted[mid]) / 2 : sorted[mid]; })();
  return {
    median,
  };
}

export function calculateMilkYieldCheckOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const liters = n(values, "cows") * n(values, "litersPerCow");
  const revenue = liters * n(values, "milkPrice");
  return {
    liters,
    revenue,
  };
}

export function calculateOeeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const oeePercent = (n(values, "availabilityPercent") / 100) * (n(values, "performancePercent") / 100) * (n(values, "qualityPercent") / 100) * 100;
  return {
    oeePercent,
  };
}

export function calculatePaintCoverageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const liters = safeDiv(n(values, "areaM2") * n(values, "coats"), n(values, "coveragePerLiter"));
  return {
    liters,
  };
}

export function calculatePercentageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const result = n(values, "value") * (n(values, "percent") / 100);
  return {
    result,
  };
}

export function calculatePercentageIncreaseCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const absoluteChange = n(values, "newValue") - n(values, "oldValue");
  const changePercent = safeDiv(absoluteChange, n(values, "oldValue")) * 100;
  return {
    absoluteChange,
    changePercent,
  };
}

export function calculatePlasterCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const volumeM3 = n(values, "areaM2") * safeDiv(n(values, "thicknessMm"), 1000);
  const weightKg = volumeM3 * n(values, "densityKgM3");
  return {
    volumeM3,
    weightKg,
  };
}

export function calculatePortionCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const portionCost = safeDiv(n(values, "totalBatchCost"), n(values, "portions"));
  return {
    portionCost,
  };
}

export function calculateProbabilityCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const probability = safeDiv(n(values, "favorableOutcomes"), n(values, "totalOutcomes"));
  return {
    probability,
  };
}

export function calculateProportionCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const missingValue = safeDiv(n(values, "b") * n(values, "c"), n(values, "a"));
  return {
    missingValue,
  };
}

export function calculateRebarWeightCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kgPerM = safeDiv(n(values, "diameterMm") * n(values, "diameterMm"), 162);
  const totalKg = kgPerM * n(values, "lengthM") * n(values, "quantity");
  return {
    kgPerM,
    totalKg,
  };
}

export function calculateRecipeCostCheckOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const portionCost = safeDiv(n(values, "ingredientCost"), n(values, "portions"));
  return {
    portionCost,
  };
}

export function calculateRoiCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const netGain = n(values, "gain") - n(values, "cost");
  const roiPercent = safeDiv(netGain, n(values, "cost")) * 100;
  return {
    netGain,
    roiPercent,
  };
}

export function calculateRoofingAreaCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const roofArea = n(values, "lengthM") * n(values, "widthM") * n(values, "pitchFactor");
  return {
    roofArea,
  };
}

export function calculateRouteCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalCost = n(values, "distanceKm") * n(values, "fuelCostPerKm") + n(values, "driverHours") * n(values, "hourlyRate") + n(values, "tolls");
  return {
    totalCost,
  };
}

export function calculateSavingsGoalCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const monthsToGoal = safeDiv(n(values, "targetAmount"), n(values, "monthlySaving"));
  return {
    monthsToGoal,
  };
}

export function calculateScrapRateCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const scrapRatePercent = safeDiv(n(values, "scrapUnits"), n(values, "totalUnits")) * 100;
  return {
    scrapRatePercent,
  };
}

export function calculateSeedRateCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const seedKg = n(values, "areaHa") * n(values, "seedKgPerHa");
  return {
    seedKg,
  };
}

export function calculateSheetMetalWeightCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const thicknessM = safeDiv(n(values, "thicknessMm"), 1000);
  const weightKg = n(values, "lengthM") * n(values, "widthM") * thicknessM * n(values, "densityKgM3");
  return {
    thicknessM,
    weightKg,
  };
}

export function calculateShoppingBudgetCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const total = n(values, "item1") + n(values, "item2") + n(values, "item3") + n(values, "item4") + n(values, "item5");
  return {
    total,
  };
}

export function calculateSolarPanelOutputCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const kwh = n(values, "systemKw") * n(values, "sunHoursPerDay") * n(values, "days") * (n(values, "efficiencyPercent") / 100);
  return {
    kwh,
  };
}

export function calculateSquareFootageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const area = n(values, "length") * n(values, "width");
  return {
    area,
  };
}

export function calculateSquareMeterCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const area = n(values, "length") * n(values, "width");
  return {
    area,
  };
}

export function calculateStairCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const steps = Math.ceil(safeDiv(n(values, "totalRiseCm"), n(values, "riserHeightCm")));
  const runM = safeDiv(steps * n(values, "treadDepthCm"), 100);
  return {
    steps,
    runM,
  };
}

export function calculateStandardDeviationCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const standardDeviation = (() => { const nums = collectValues(values); const count = nums.length; if (count === 0) return 0; const mean = safeDiv(nums.reduce((a, b) => a + b, 0), count); const variance = safeDiv(nums.reduce((acc, x) => acc + (x - mean) ** 2, 0), count); return Math.sqrt(variance); })();
  return {
    standardDeviation,
  };
}

export function calculateTileCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const tileAreaM2 = safeDiv(n(values, "tileLengthCm") * n(values, "tileWidthCm"), 10000);
  const tiles = Math.ceil(safeDiv(n(values, "areaM2"), tileAreaM2) * (1 + n(values, "wastePercent") / 100));
  return {
    tileAreaM2,
    tiles,
  };
}

export function calculateTimeDurationCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const startMin = n(values, "startHour") * 60 + n(values, "startMinute");
  const endMin = n(values, "endHour") * 60 + n(values, "endMinute");
  const durationMinutes = endMin >= startMin ? endMin - startMin : endMin + 1440 - startMin;
  return {
    startMin,
    endMin,
    durationMinutes,
  };
}

export function calculateTipCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const tipAmount = n(values, "billAmount") * (n(values, "tipPercent") / 100);
  const totalWithTip = n(values, "billAmount") + tipAmount;
  return {
    tipAmount,
    totalWithTip,
  };
}

export function calculateToleranceDriftCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const drift = n(values, "actual") - n(values, "target");
  const driftPercent = safeDiv(drift, n(values, "target")) * 100;
  return {
    drift,
    driftPercent,
  };
}

export function calculateToolLifeCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const costPerPart = safeDiv(n(values, "totalToolCost"), n(values, "partsProduced"));
  return {
    costPerPart,
  };
}

export function calculateTripBudgetCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalBudget = n(values, "fuelCost") + n(values, "accommodationCost") + n(values, "foodCost") + n(values, "tolls") + n(values, "otherCost");
  return {
    totalBudget,
  };
}

export function calculateUnitCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const unitCost = safeDiv(n(values, "totalCost"), n(values, "quantity"));
  return {
    unitCost,
  };
}

export function calculateUnitPriceCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const unitPrice = safeDiv(n(values, "totalPrice"), n(values, "quantity"));
  return {
    unitPrice,
  };
}

export function calculateVatCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const vat = n(values, "net") * (n(values, "vatRate") / 100);
  const gross = n(values, "net") + vat;
  return {
    vat,
    gross,
  };
}

export function calculateVehicleDepreciationCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const annualDepreciation = safeDiv(n(values, "purchasePrice") - n(values, "resaleValue"), n(values, "years"));
  return {
    annualDepreciation,
  };
}

export function calculateVolumetricWeightCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const volumetricWeight = safeDiv(n(values, "lengthCm") * n(values, "widthCm") * n(values, "heightCm"), n(values, "divisor"));
  return {
    volumetricWeight,
  };
}

export function calculateWarehouseStorageCostCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const totalCost = n(values, "palletCount") * n(values, "costPerPalletPerDay") * n(values, "days");
  return {
    totalCost,
  };
}

export function calculateWaterUsageCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const liters = n(values, "flowRateLiterMin") * n(values, "minutes");
  return {
    liters,
  };
}

export function calculateZScoreCalculatorOracle(values: TrafficOracleValues): TrafficCatalogOracleOutput {
  const zScore = safeDiv(n(values, "value") - n(values, "mean"), n(values, "standardDeviation"));
  return {
    zScore,
  };
}
