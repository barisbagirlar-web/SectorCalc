---
description: Cursor (DeepSeek) full authority over entire codebase — Google Antigravity revoked
globs: 
alwaysApply: true
---
# AI Territory — Cursor (DeepSeek) Tam Yetki

**Google Antigravity'nin yetkileri iptal edilmiştir.**

Cursor (DeepSeek) tüm dizinlerde tam yetkiye sahiptir:
oku + değiştir + sil + ekle.

## ✅ Tam Yetki Dizinleri

```
src/**
public/**
messages/**
functions/**
scripts/**
next.config.ts
src/middleware.ts
firestore.rules
.cursor/rules/**
```

## ❌ Korumalı alanlar (sadece kullanıcı yetkisiyle dokun)

```
.env.local
.env
*service-account*
*firebase-adminsdk*
*credentials*
*private-key*
```

## Dev Server — Static Build + Serve

```bash
sh dev.sh
```

## Her görev öncesi

```bash
git status
```

## Her görev sonrası

```bash
npm run lint
npm run build
git status --short
git diff --stat
```
