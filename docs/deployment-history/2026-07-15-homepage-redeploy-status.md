# Homepage redeploy status

Pending merge to `main`. The deployment workflow must verify that the live Firebase release SHA matches the merged commit and that the homepage hero renders the proportional-layout implementation.
