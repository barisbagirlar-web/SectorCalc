# SEO Security Hardening Rollback

ROLLBACK: revert the security hardening PR to restore the prior repository guard behavior. Do not restore any credential value to PR/issue text. Provider credential rotation, once completed, is intentionally non-reversible and must not be rolled back to an exposed credential.
