// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCalibration_drift_risk_calculator,
  type Calibration_drift_risk_calculatorInput,
} from "@generated/calibration-drift-risk-calculator";

describe("calibration-drift-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "calibration_interval_days": 90,
    "days_since_last_calibration": 45,
    "measurement_tolerance_pct": 5,
    "observed_drift_pct": 2.3,
    "environmental_stress_factor": "moderate",
    "criticality_level": "medium",
    "historical_failure_rate": 3,
    "data_confidence_score": 85
  } as unknown as Calibration_drift_risk_calculatorInput;
    const result = calculateCalibration_drift_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
