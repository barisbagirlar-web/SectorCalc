// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBusiness_debt_service_coverage_ratio_calculator,
  type Business_debt_service_coverage_ratio_calculatorInput,
} from "@generated/business-debt-service-coverage-ratio-calculator";

describe("business-debt-service-coverage-ratio-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "netOperatingIncome": 100000,
    "principalRepayments": 20000,
    "interestPayments": 10000,
    "leasePayments": 5000
  } as unknown as Business_debt_service_coverage_ratio_calculatorInput;
    const result = calculateBusiness_debt_service_coverage_ratio_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
