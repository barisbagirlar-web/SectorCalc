// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateContract_incentive_calculator,
  type Contract_incentive_calculatorInput,
} from "@generated/contract-incentive-calculator";

describe("contract-incentive-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "actual_throughput": 1000,
    "target_throughput": 1200,
    "defect_rate": 5000,
    "on_time_delivery_percent": 95,
    "cost_per_unit": 50,
    "target_cost_per_unit": 45,
    "incentive_base_rate": 2.5,
    "quality_threshold_ppm": 10000
  } as unknown as Contract_incentive_calculatorInput;
    const result = calculateContract_incentive_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
