// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateMtbf_mttr_financial_calculator,
  type Mtbf_mttr_financial_calculatorInput,
} from "@generated/mtbf-mttr-financial-calculator";

describe("mtbf-mttr-financial-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "operating_hours_per_year": 8760,
    "number_of_units": 10,
    "mtbf": 500,
    "mttr": 4,
    "cost_per_downtime_hour": 1000,
    "cost_per_failure_event": 500,
    "maintenance_labor_rate": 75,
    "reliability_improvement_investment": 1
  } as unknown as Mtbf_mttr_financial_calculatorInput;
    const result = calculateMtbf_mttr_financial_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
