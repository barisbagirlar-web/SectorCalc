// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSalary_to_hourly_calculator,
  type Salary_to_hourly_calculatorInput,
} from "@generated/salary-to-hourly-calculator";

describe("salary-to-hourly-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annual_salary": 50000,
    "work_hours_per_week": 40,
    "paid_weeks_per_year": 52,
    "overtime_multiplier": 1.5,
    "benefits_percentage": 30,
    "shift_type": "day",
    "include_productivity_factor": true
  } as unknown as Salary_to_hourly_calculatorInput;
    const result = calculateSalary_to_hourly_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
