// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSmall_business_grants_calculator,
  type Small_business_grants_calculatorInput,
} from "@generated/small-business-grants-calculator";

describe("small-business-grants-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "avgAnnualRevenue": 100000,
    "numEmployees": 10,
    "projectCost": 50000,
    "matchingFunds": 10000,
    "isMinorityOwned": 1,
    "isWomanOwned": 1
  } as unknown as Small_business_grants_calculatorInput;
    const result = calculateSmall_business_grants_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
