// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePrime_factorization_builder_calculator,
  type Prime_factorization_builder_calculatorInput,
} from "@generated/prime-factorization-builder-calculator";

describe("prime-factorization-builder-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "prime1": 2,
    "exponent1": 1,
    "prime2": 3,
    "exponent2": 1,
    "prime3": 5,
    "exponent3": 1,
    "prime4": 7,
    "exponent4": 1
  } as unknown as Prime_factorization_builder_calculatorInput;
    const result = calculatePrime_factorization_builder_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
