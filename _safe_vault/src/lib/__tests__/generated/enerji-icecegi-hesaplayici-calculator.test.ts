// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateEnerji_icecegi_hesaplayici_calculator,
  type Enerji_icecegi_hesaplayici_calculatorInput,
} from "@generated/enerji-icecegi-hesaplayici-calculator";

describe("enerji-icecegi-hesaplayici-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "servings": 1,
    "caffeinePerServing": 80,
    "sugarPerServing": 27,
    "costPerCan": 2.5,
    "servingsPerCan": 1
  } as unknown as Enerji_icecegi_hesaplayici_calculatorInput;
    const result = calculateEnerji_icecegi_hesaplayici_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
