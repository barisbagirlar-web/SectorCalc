// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateTraveling_salesman_route_distance_calculator,
  type Traveling_salesman_route_distance_calculatorInput,
} from "@generated/traveling-salesman-route-distance-calculator";

describe("traveling-salesman-route-distance-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "ax": 1,
    "ay": 1,
    "bx": 1,
    "by": 1,
    "cx": 1,
    "cy": 1,
    "dx": 1,
    "dy": 1
  } as unknown as Traveling_salesman_route_distance_calculatorInput;
    const result = calculateTraveling_salesman_route_distance_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
