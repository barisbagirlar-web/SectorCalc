// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCut_fill_balance_optimizer_calculator,
  type Cut_fill_balance_optimizer_calculatorInput,
} from "@generated/cut-fill-balance-optimizer-calculator";

describe("cut-fill-balance-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "cutVolume": 10000,
    "fillVolume": 8000,
    "swellFactor": 1.25,
    "shrinkageFactor": 0.85,
    "haulDistance": 5,
    "unitHaulCost": 0.5,
    "wasteDisposalCost": 3,
    "borrowCost": 5
  } as unknown as Cut_fill_balance_optimizer_calculatorInput;
    const result = calculateCut_fill_balance_optimizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
