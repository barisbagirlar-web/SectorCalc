// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSupplier_currency_risk_calculator,
  type Supplier_currency_risk_calculatorInput,
} from "@generated/supplier-currency-risk-calculator";

describe("supplier-currency-risk-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "contract_value": 1000000,
    "foreign_exposure_pct": 50,
    "volatility_annual": 10,
    "hedge_ratio": 70,
    "time_horizon_months": 12,
    "confidence_level": "95",
    "supplier_credit_rating": "BBB",
    "use_iso_31000": true
  } as unknown as Supplier_currency_risk_calculatorInput;
    const result = calculateSupplier_currency_risk_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
