// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePetabytes_to_exabytes_calculator,
  type Petabytes_to_exabytes_calculatorInput,
} from "@generated/petabytes-to-exabytes-calculator";

describe("petabytes-to-exabytes-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "rawPetabytes": 100,
    "replicationFactor": 3,
    "redundancyOverhead": 30,
    "provisioningBuffer": 20,
    "compressionRatio": 2,
    "decimalPrecision": 3
  } as unknown as Petabytes_to_exabytes_calculatorInput;
    const result = calculatePetabytes_to_exabytes_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
