// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHaccp_deviation_cost_calculator,
  type Haccp_deviation_cost_calculatorInput,
} from "@generated/haccp-deviation-cost-calculator";

describe("haccp-deviation-cost-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "deviation_type": "temperature",
    "affected_batch_kg": 1000,
    "unit_cost_per_kg": 5,
    "rework_percentage": 30,
    "rework_cost_per_kg": 2,
    "downtime_hours": 2,
    "hourly_overhead_rate": 500,
    "regulatory_penalty_flag": false
  } as unknown as Haccp_deviation_cost_calculatorInput;
    const result = calculateHaccp_deviation_cost_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
