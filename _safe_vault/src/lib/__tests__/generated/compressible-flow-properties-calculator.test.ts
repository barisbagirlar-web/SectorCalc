// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCompressible_flow_properties_calculator,
  type Compressible_flow_properties_calculatorInput,
} from "@generated/compressible-flow-properties-calculator";

describe("compressible-flow-properties-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "machNumber": 1.5,
    "gamma": 1.4,
    "staticTemperature": 300,
    "staticPressure": 101325,
    "gasConstant": 287
  } as unknown as Compressible_flow_properties_calculatorInput;
    const result = calculateCompressible_flow_properties_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
