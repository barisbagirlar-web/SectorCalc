// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateLearning_curve_time_estimator_calculator,
  type Learning_curve_time_estimator_calculatorInput,
} from "@generated/learning-curve-time-estimator-calculator";

describe("learning-curve-time-estimator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "first_unit_time": 100,
    "learning_rate": 85,
    "cumulative_units": 100,
    "target_units": 200,
    "shift_length": 8,
    "learning_model": "wright",
    "include_break_in": false,
    "break_in_penalty": 10
  } as unknown as Learning_curve_time_estimator_calculatorInput;
    const result = calculateLearning_curve_time_estimator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
