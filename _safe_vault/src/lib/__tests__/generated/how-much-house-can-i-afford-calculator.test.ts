// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHow_much_house_can_i_afford_calculator,
  type How_much_house_can_i_afford_calculatorInput,
} from "@generated/how-much-house-can-i-afford-calculator";

describe("how-much-house-can-i-afford-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "annualIncome": 80000,
    "monthlyDebts": 500,
    "downPayment": 20000,
    "interestRate": 6.5,
    "loanTerm": 30,
    "propertyTaxRate": 1.2,
    "insuranceRate": 0.5,
    "monthlyHOA": 1
  } as unknown as How_much_house_can_i_afford_calculatorInput;
    const result = calculateHow_much_house_can_i_afford_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
