// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateVacuum_leak_energy_loss_calculator,
  type Vacuum_leak_energy_loss_calculatorInput,
} from "@generated/vacuum-leak-energy-loss-calculator";

describe("vacuum-leak-energy-loss-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "leak_diameter_mm": 2,
    "system_pressure_bar": 7,
    "operating_hours_per_year": 8000,
    "electricity_cost_per_kwh": 0.12,
    "compressor_specific_power": 18,
    "ambient_temperature_c": 25,
    "leak_type": "round",
    "include_carbon_cost": true
  } as unknown as Vacuum_leak_energy_loss_calculatorInput;
    const result = calculateVacuum_leak_energy_loss_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
