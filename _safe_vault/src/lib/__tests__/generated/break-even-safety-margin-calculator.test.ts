// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBreak_even_safety_margin_calculator,
  type Break_even_safety_margin_calculatorInput,
} from "@generated/break-even-safety-margin-calculator";

describe("break-even-safety-margin-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fixedCosts": 500000,
    "variableCostPerUnit": 15,
    "sellingPricePerUnit": 25,
    "actualSalesVolume": 80000,
    "capacityVolume": 120000,
    "demandUncertainty": 10,
    "costEscalationRate": 3,
    "includeTax": false
  } as unknown as Break_even_safety_margin_calculatorInput;
    const result = calculateBreak_even_safety_margin_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
