// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateDividend_reinvestment_calculator,
  type Dividend_reinvestment_calculatorInput,
} from "@generated/dividend-reinvestment-calculator";

describe("dividend-reinvestment-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "initialInvestment": 10000,
    "annualDividendYield": 4,
    "dividendGrowthRate": 5,
    "stockPriceGrowthRate": 7,
    "years": 10,
    "taxRate": 15
  } as unknown as Dividend_reinvestment_calculatorInput;
    const result = calculateDividend_reinvestment_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
