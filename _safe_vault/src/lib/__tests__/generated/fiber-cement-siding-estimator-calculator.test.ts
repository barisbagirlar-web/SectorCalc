// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFiber_cement_siding_estimator_calculator,
  type Fiber_cement_siding_estimator_calculatorInput,
} from "@generated/fiber-cement-siding-estimator-calculator";

describe("fiber-cement-siding-estimator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "wallArea": 100,
    "panelLength": 3.66,
    "panelWidth": 0.2,
    "wasteFactor": 10,
    "costPerPanel": 5,
    "laborHoursPerPanel": 0.1,
    "laborCostPerHour": 50
  } as unknown as Fiber_cement_siding_estimator_calculatorInput;
    const result = calculateFiber_cement_siding_estimator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
