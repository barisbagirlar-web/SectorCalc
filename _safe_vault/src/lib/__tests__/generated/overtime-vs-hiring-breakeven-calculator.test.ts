// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateOvertime_vs_hiring_breakeven_calculator,
  type Overtime_vs_hiring_breakeven_calculatorInput,
} from "@generated/overtime-vs-hiring-breakeven-calculator";

describe("overtime-vs-hiring-breakeven-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "current_headcount": 50,
    "avg_hourly_wage": 25,
    "overtime_premium": 1.5,
    "overtime_hours_per_week": 10,
    "hiring_cost_per_fte": 5000,
    "annual_benefits_cost_per_fte": 12000,
    "productivity_factor_overtime": 85,
    "productivity_factor_new_hire": 70
  } as unknown as Overtime_vs_hiring_breakeven_calculatorInput;
    const result = calculateOvertime_vs_hiring_breakeven_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
