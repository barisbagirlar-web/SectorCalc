// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePiotroski_f_score_calculator,
  type Piotroski_f_score_calculatorInput,
} from "@generated/piotroski-f-score-calculator";

describe("piotroski-f-score-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "roaPositive": 1,
    "cfoPositive": 1,
    "roaIncreased": 1,
    "accrualLow": 1,
    "leverageDecreased": 1,
    "currentRatioIncreased": 1,
    "grossMarginIncreased": 1,
    "assetTurnoverIncreased": 1
  } as unknown as Piotroski_f_score_calculatorInput;
    const result = calculatePiotroski_f_score_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
