// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCpm_delay_penalty_optimizer_calculator,
  type Cpm_delay_penalty_optimizer_calculatorInput,
} from "@generated/cpm-delay-penalty-optimizer-calculator";

describe("cpm-delay-penalty-optimizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "planned_duration_days": 100,
    "actual_duration_days": 110,
    "daily_penalty_rate": 5000,
    "critical_path_float_total": 5,
    "productivity_factor": 0.85,
    "rework_percentage": 12,
    "delay_type": "excusable_compensable",
    "use_earned_schedule": true
  } as unknown as Cpm_delay_penalty_optimizer_calculatorInput;
    const result = calculateCpm_delay_penalty_optimizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
