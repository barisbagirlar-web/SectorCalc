// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculatePregnancy_due_date_calculator,
  type Pregnancy_due_date_calculatorInput,
} from "@generated/pregnancy-due-date-calculator";

describe("pregnancy-due-date-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "lmp_date": 946684800,
    "cycle_length": 28,
    "luteal_phase_length": 14,
    "ultrasound_ga_weeks": 5,
    "ultrasound_ga_days": 1,
    "cycle_variability": 2,
    "parity": 1,
    "risk_factors": "none"
  } as unknown as Pregnancy_due_date_calculatorInput;
    const result = calculatePregnancy_due_date_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
