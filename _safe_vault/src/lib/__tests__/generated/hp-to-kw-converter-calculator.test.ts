// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateHp_to_kw_converter_calculator,
  type Hp_to_kw_converter_calculatorInput,
} from "@generated/hp-to-kw-converter-calculator";

describe("hp-to-kw-converter-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "horsepower": 100,
    "hp_type": "mechanical",
    "motor_efficiency": 90,
    "load_factor": 100,
    "power_factor": 0.85,
    "operating_hours_per_year": 8000,
    "electricity_cost_per_kwh": 0.12
  } as unknown as Hp_to_kw_converter_calculatorInput;
    const result = calculateHp_to_kw_converter_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
