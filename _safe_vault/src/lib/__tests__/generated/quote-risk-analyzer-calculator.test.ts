// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateQuote_risk_analyzer_calculator,
  type Quote_risk_analyzer_calculatorInput,
} from "@generated/quote-risk-analyzer-calculator";

describe("quote-risk-analyzer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "order_volume": 1000,
    "unit_price": 50,
    "production_lead_time_days": 30,
    "material_cost_variability": 5,
    "labor_skill_level": "medium",
    "quality_defect_rate": 2,
    "logistics_complexity": 5,
    "customer_credit_rating": "BBB"
  } as unknown as Quote_risk_analyzer_calculatorInput;
    const result = calculateQuote_risk_analyzer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
