// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateBottleneck_investment_prioritizer_calculator,
  type Bottleneck_investment_prioritizer_calculatorInput,
} from "@generated/bottleneck-investment-prioritizer-calculator";

describe("bottleneck-investment-prioritizer-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "bottleneck_utilization": 85,
    "demand_rate": 100,
    "bottleneck_capacity": 120,
    "investment_cost": 50000,
    "expected_capacity_increase": 20,
    "operating_hours_per_year": 4000,
    "profit_margin_per_unit": 5,
    "quality_yield": 95
  } as unknown as Bottleneck_investment_prioritizer_calculatorInput;
    const result = calculateBottleneck_investment_prioritizer_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
