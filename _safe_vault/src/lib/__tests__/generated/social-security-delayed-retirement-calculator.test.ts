// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSocial_security_delayed_retirement_calculator,
  type Social_security_delayed_retirement_calculatorInput,
} from "@generated/social-security-delayed-retirement-calculator";

describe("social-security-delayed-retirement-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "fullRetirementAge": 67,
    "plannedRetirementAge": 70,
    "primaryInsuranceAmount": 2000,
    "annualDelayCreditRate": 8
  } as unknown as Social_security_delayed_retirement_calculatorInput;
    const result = calculateSocial_security_delayed_retirement_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
