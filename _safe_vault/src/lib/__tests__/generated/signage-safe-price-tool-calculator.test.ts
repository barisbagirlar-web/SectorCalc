// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateSignage_safe_price_tool_calculator,
  type Signage_safe_price_tool_calculatorInput,
} from "@generated/signage-safe-price-tool-calculator";

describe("signage-safe-price-tool-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "material_cost": 100,
    "labor_hours": 2.5,
    "labor_rate": 35,
    "overhead_rate": 25,
    "complexity_factor": "medium",
    "quality_level": "standard",
    "quantity": 100,
    "waste_factor": 5
  } as unknown as Signage_safe_price_tool_calculatorInput;
    const result = calculateSignage_safe_price_tool_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
