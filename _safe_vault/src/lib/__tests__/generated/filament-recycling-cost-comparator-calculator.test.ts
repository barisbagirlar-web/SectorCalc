// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateFilament_recycling_cost_comparator_calculator,
  type Filament_recycling_cost_comparator_calculatorInput,
} from "@generated/filament-recycling-cost-comparator-calculator";

describe("filament-recycling-cost-comparator-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "recycling_machine_cost": 50000,
    "machine_life_years": 10,
    "annual_maintenance_cost": 5000,
    "electricity_price": 0.12,
    "recycling_energy_consumption": 2.5,
    "labor_rate": 25,
    "labor_hours_per_kg": 0.05,
    "waste_collection_cost": 0.3
  } as unknown as Filament_recycling_cost_comparator_calculatorInput;
    const result = calculateFilament_recycling_cost_comparator_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
