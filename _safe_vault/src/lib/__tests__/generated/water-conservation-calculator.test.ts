// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateWater_conservation_calculator,
  type Water_conservation_calculatorInput,
} from "@generated/water-conservation-calculator";

describe("water-conservation-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "currentConsumption": 1000,
    "baselineConsumption": 1200,
    "waterCostPerCubicMeter": 2.5,
    "leakagePercentage": 5,
    "efficiencyImprovementPercentage": 10
  } as unknown as Water_conservation_calculatorInput;
    const result = calculateWater_conservation_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
