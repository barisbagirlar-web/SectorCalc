// Auto-generated by scripts/generate-tests.js — do not edit manually
import { describe, it, expect } from "vitest";
import {
  calculateCredit_card_minimum_payment_calculator,
  type Credit_card_minimum_payment_calculatorInput,
} from "@generated/credit-card-minimum-payment-calculator";

describe("credit-card-minimum-payment-calculator", () => {
  it("calculates with schema default inputs", () => {
    const input = {
    "outstandingBalance": 1000,
    "annualInterestRate": 18,
    "minPaymentPercent": 2,
    "fixedMinAmount": 25,
    "pastDueAmount": 1,
    "overLimitAmount": 1
  } as unknown as Credit_card_minimum_payment_calculatorInput;
    const result = calculateCredit_card_minimum_payment_calculator(input);
    expect(result).toBeDefined();
    // Stub-tolerant: NaN kabul edilir (stub formüller henüz NaN üretebilir)
    // Gerçek formül geldiğinde Number.isFinite eklenebilir
    expect(typeof result.totalWasteCost).toBe("number");
    expect(result.breakdown).toBeDefined();
    expect(Array.isArray(result.hiddenLossDrivers)).toBe(true);
    expect(Array.isArray(result.suggestedActions)).toBe(true);
  });
});
