{
  "id": "korelasyon_ve_regresyon",
  "name": "Korelasyon ve Regresyon Analizi",
  "description": "İki değişken arasındaki ilişkinin gücünü (Pearson korelasyon katsayısı r) ve doğrusal regresyon eğimini hesaplar.",
  "inputs": [
    {
      "id": "X",
      "name": "X Değişkeni (Bağımsız)",
      "unit": "Sayı Dizisi",
      "type": "array",
      "required": true,
      "confidence_label": "KESİN",
      "absolute_min": -99999,
      "absolute_max": 99999
    },
    {
      "id": "Y",
      "name": "Y Değişkeni (Bağımlı)",
      "unit": "Sayı Dizisi",
      "type": "array",
      "required": true,
      "confidence_label": "KESİN",
      "absolute_min": -99999,
      "absolute_max": 99999
    }
  ],
  "outputs": [
    {
      "id": "r",
      "name": "Pearson Korelasyon Katsayısı (r)",
      "unit": "-1 ile 1 arası",
      "type": "number",
      "formula": "function(X, Y) { if (X.length !== Y.length || X.length < 2) return NaN; let n = X.length; let sumX = X.reduce((a,b) => a+b, 0); let sumY = Y.reduce((a,b) => a+b, 0); let sumXY = X.reduce((a,b,i) => a+b*Y[i], 0); let sumX2 = X.reduce((a,b) => a+b*b, 0); let sumY2 = Y.reduce((a,b) => a+b*b, 0); let num = n*sumXY - sumX*sumY; let den = SQRT((n*sumX2 - sumX*sumX) * (n*sumY2 - sumY*sumY)); return den === 0 ? NaN : num/den; }",
      "output_type": "KORELASYON"
    },
    {
      "id": "Egim",
      "name": "Regresyon Eğimi (b)",
      "unit": "Sayı",
      "type": "number",
      "formula": "function(X, Y) { if (X.length !== Y.length || X.length < 2) return NaN; let n = X.length; let meanX = X.reduce((a,b) => a+b,0)/n; let meanY = Y.reduce((a,b) => a+b,0)/n; let cov = X.reduce((a,b,i) => a+(b-meanX)*(Y[i]-meanY),0)/(n-1); let varX = X.reduce((a,b) => a+(b-meanX)*(b-meanX),0)/(n-1); return varX === 0 ? NaN : cov/varX; }",
      "output_type": "EGIM"
    }
  ],
  "standards": [
    "ISO 5725-1 (Doğruluk ve Kesinlik)",
    "NIST/SEMATECH e-Handbook of Statistical Methods"
  ],
  "smart_warnings": [
    {
      "condition": "X.length < 3 || Y.length < 3",
      "severity": "WARNING",
      "source": "İstatistiksel Güvenirlik",
      "message": "Veri noktası sayısı 3'ten az. Korelasyon katsayısı güvenilir olmayabilir."
    },
    {
      "condition": "X.length !== Y.length",
      "severity": "WARNING",
      "source": "Eşleşme Hatası",
      "message": "X ve Y dizileri aynı uzunlukta olmalıdır. Hesaplama durduruldu."
    },
    {
      "condition": "X.every(v => v === X[0]) || Y.every(v => v === Y[0])",
      "severity": "WARNING",
      "source": "Sabit Değişken",
      "message": "Bir değişkende varyasyon yok (sabit dizi). Korelasyon tanımsız."
    },
    {
      "condition": "Math.abs(r) > 0.95",
      "severity": "INFO",
      "source": "Çok Güçlü İlişki",
      "message": "Korelasyon katsayısı mutlak değer olarak 0.95'ten büyük. Neredeyse doğrusal bir ilişki var."
    },
    {
      "condition": "Math.abs(r) < 0.1",
      "severity": "INFO",
      "source": "Zayıf İlişki",
      "message": "Korelasyon katsayısı 0.1'den küçük. Değişkenler arasında doğrusal bir bağ yok."
    }
  ],
  "validation": {
    "X_length_hata": {
      "absolute_min": 2,
      "error_msg": "Korelasyon hesaplamak için en az 2 veri noktası gereklidir."
    },
    "Y_length_hata": {
      "absolute_min": 2,
      "error_msg": "Korelasyon hesaplamak için en az 2 veri noktası gereklidir."
    },
    "X_eslesme_hata": {
      "condition": "X.length !== Y.length",
      "error_msg": "X ve Y dizilerinin uzunlukları eşit olmalı."
    }
  }
}