/**
 * SectorCalc — Pages Router _document stub.
 * Required by Next.js 15.x standalone output for deterministic Cloud Build.
 */
import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en">
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
